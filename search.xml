<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>React Notes</title>
    <url>/2020/03/02/react-notes/</url>
    <content><![CDATA[<h3 id="Introduction">Introduction</h3><p>Some concepts I don't know from backend programming languages, some places I might make mistakes.</p><p>React:</p><blockquote><p>&gt;= v16.13.0</p></blockquote><a id="more"></a><h3 id="When-to-use">When to use</h3><p>React is for developing <em>big</em> client-side web applications.</p><p>React is a library that keeps the DOM in sync with your data. It is not equal to Web Components, they solve different problems.</p><h3 id="Kinds-of-Web-Applications">Kinds of Web Applications</h3><ul><li>Round-Trip<br>Clicking a link or button leads the browser to load a new page, the browser shows HTML pages, most of application logics and data are resident on the server.</li><li>Single-Page<br>SPA(Single-Page Applications) is a another approach. The browser loads an initial HTML page, the page will not be reloaded or replaced later, user interactions lead to HTTP requests for parts of the page or data, then update the existing HMTL elements.</li></ul><p>React is well-suited to SPAs.</p><h3 id="Other-Solutions">Other Solutions</h3><p>React is a library that is the 'V' of MVC.</p><ul><li><p>Angular<br>A framework. You need to walk along the direction that it specified.</p></li><li><p>Vue.js<br>It is between Angular and React.</p></li></ul><h3 id="JSX">JSX</h3><ul><li><p><code>true</code>, <code>false</code>, <code>null</code>, <code>undefined</code> are ignored;</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;&#123;<span class="literal">true</span>&#125;&lt;<span class="regexp">/div&gt;</span></span><br></pre></td></tr></table></figure><p>This code is valid, but doesn't render anything.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;</span><br><span class="line">    &#123;showHeader &amp;&amp; <span class="xml"><span class="tag">&lt;<span class="name">Header</span> /&gt;</span></span>&#125;</span><br><span class="line">&lt;<span class="regexp">/div&gt;</span></span><br></pre></td></tr></table></figure><p>JSX renders the <code>&lt;Header /&gt;</code> component only if <code>showHeader</code> is true.</p><p>But</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;</span><br><span class="line">    &#123;props.messages.length &amp;&amp; <span class="xml"><span class="tag">&lt;<span class="name">MessageList</span> <span class="attr">messages</span>=<span class="string">&#123;props.messages/</span>&gt;</span></span>&#125;</span><br><span class="line">&lt;<span class="regexp">/div&gt;</span></span><br></pre></td></tr></table></figure><p>JSX still renders the <code>MessageList</code> component if the length is <code>0</code>. Need to use the condition <code>props.messages.length &gt; 0</code>.</p></li><li><p>Always start component names with a capital letter, otherwise React treats the components as DOM tags.</p></li><li><p>React requires components to return a single top-level element. Use the following special React element to return multiple elements:</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">React.Fragment</span>&gt;</span></span></span><br><span class="line"><span class="xml">       /* ... */</span></span><br><span class="line"><span class="xml">       <span class="tag">&lt;/<span class="name">React.Fragment</span>&gt;</span></span></span><br><span class="line"></span><br><span class="line"> <span class="comment">// OR </span></span><br><span class="line"></span><br><span class="line"> <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;&gt;</span></span></span><br><span class="line"><span class="xml">        /* ... */</span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/&gt;</span></span></span><br></pre></td></tr></table></figure></li><li><p>Multiple line JSX statements should be enclosed by parentheses, it isn't required, but should be, to avoid the pitfalls of JavaScript's automatic semicolon insertion.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// "element" is a React component</span></span><br><span class="line"><span class="keyword">const</span> element = <span class="xml"><span class="tag">&lt;<span class="name">h1</span>&gt;</span>Hello, &#123;name&#125;<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// OR </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> element = (</span><br><span class="line">    &lt;h1&gt;</span><br><span class="line">        Hello, &#123;name&#125;</span><br><span class="line">    &lt;<span class="regexp">/h1&gt;</span></span><br><span class="line"><span class="regexp">);</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="CSS">CSS</h3><p>The methods to style:</p><ul><li>Plain CSS sytlesheet (as usual), just import the CSS files and use the <code>class</code>(<code>className</code> in React) attribute on HTML elements. Could use <a href="http://getbem.com/" target="_blank" rel="noopener">BEM</a> (Block Element Modifier) to solve the global namespace issue and use other existing libraries and tools, such as <a href="https://sass-lang.com" target="_blank" rel="noopener">Sass</a>, <a href="https://tailwindcss.com" target="_blank" rel="noopener">Tailwind CSS</a>;</li><li>CSS modules. It works with <em>webpack</em> or <em>Browserify</em> and is not an official spec. Create a <code>button.module.css</code> or <code>button.module.scss</code>, only import it into <code>button.js</code>, the rules only apply to the components within <code>button.js</code>;</li><li>Web Components. The CSS styles of the components are local scoped.</li><li>CSS-in-JS, like the <a href="https://github.com/styled-components/styled-components" target="_blank" rel="noopener">styled-components</a> third-party library. This method unlocks some features, such as theme nesting, dynamic styles;</li><li>Inline styling;</li></ul><h4 id="Sass-and-Styled-components">Sass and Styled-components</h4><p>Sometimes we use a CSS framework and also want to use styled-components to customize it. The example created by <code>create-react-app</code> uses <a href="https://github.com/jgthms/bulma" target="_blank" rel="noopener">bulma</a>:</p><ul><li><p>Install node-sass, bulma, styled-components;</p></li><li><p>Import the bulma button element. button.scss:</p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@import</span> <span class="string">"bulma/sass/utilities/_all.sass"</span>;</span><br><span class="line"><span class="keyword">@import</span> <span class="string">"bulma/sass/elements/button.sass"</span>;</span><br></pre></td></tr></table></figure></li><li><p>App.js</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="comment">// import styled from 'styled-components/macro';</span></span><br><span class="line"><span class="keyword">import</span> styled <span class="keyword">from</span> <span class="string">'styled-components'</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">"./button.scss"</span></span><br><span class="line"><span class="keyword">import</span> <span class="string">'./App.css'</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Button</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// =&gt; className = 'sc-AxjCm fzAihE'</span></span><br><span class="line">    <span class="keyword">const</span> &#123;className, ...rest&#125; = &#123;...props&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// =&gt; &lt;button class="button sc-AxjCm fzAihE"&gt;Click&lt;/button&gt;</span></span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">button</span> <span class="attr">className</span>=<span class="string">&#123;</span>`<span class="attr">button</span> $&#123;<span class="attr">className</span>&#125;`&#125; &#123;<span class="attr">...rest</span>&#125;&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> StyledButton = styled(Button)<span class="string">`</span></span><br><span class="line"><span class="string">    color: red;</span></span><br><span class="line"><span class="string">    `</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">App</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// return &lt;Button css="color: red"/&gt;;  // another way to style</span></span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">StyledButton</span> /&gt;</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> App;</span><br></pre></td></tr></table></figure><p><em>CSS modules</em> could work together with Sass and styled-components, but it treats all class names from the two common files and customized class names within <code>button.module.scss</code>(renamed <code>button.css</code> to <code>button.module.css</code>) as local class names. This is not good, it is very hard to avoid using common CSS files completely in practice. Exported common class names like this:</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">exports.locals = &#123;</span><br><span class="line">    <span class="string">"button"</span>: <span class="string">"button_button__13ZnZ"</span>,</span><br><span class="line">    <span class="string">"is-loading"</span>: <span class="string">"button_is-loading__3N-29"</span>, </span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>Web Components</em> also could isolate CSS styles via the Shadow DOM. No random generated class names, more natural.</p></li></ul><h3 id="Props">Props</h3><ul><li><p>The value of React Props can be a literal string or an expression(in <code>{}</code>)</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;button text=<span class="string">"Click"</span> disabled=&#123;<span class="literal">false</span>&#125;&gt;</span><br></pre></td></tr></table></figure></li><li><p>React components could pass data props or function props to its child components. If the callback functions have arguments, the expression includes parentheses that leads a function call, must put them into a function.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;button onClick=&#123; () =&gt; props.promoteCallback(props.name) &#125;&gt;</span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line">&lt;<span class="regexp">/button&gt;</span></span><br></pre></td></tr></table></figure><p>not like this:</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// React will invoke the function when the component renders its content, not when the user </span></span><br><span class="line"><span class="comment">// clicks the button.</span></span><br><span class="line">&lt;button onClick=&#123; props.promoteCallback(props.name) &#125;&gt;</span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line">&lt;<span class="regexp">/button&gt;</span></span><br></pre></td></tr></table></figure></li><li><p>Props could have type checking and default values. Be performed only during development.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">SimpleButton</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SimpleButton.defaultProps = &#123;</span><br><span class="line">    disabled: <span class="literal">false</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">SimpleButton.propTypes = &#123;</span><br><span class="line">    text: PropTypes.string,</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    disabled: PropTypes.oneOfType([PropTypes.bool, PropTypes.string])</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// class-based components also support these features</span></span><br></pre></td></tr></table></figure></li><li><p>Elements generated from arrays are required to have a <code>key</code> property, React is able to minimize the number of changes to display a change. Its value must be unique and stable.</p></li></ul><h3 id="State">State</h3><ul><li><p>Stateless and stateful components</p><p>stateless</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">Clock</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">    );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>stateful</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Only one Clock instance for a same DOM node, but call the render method each time an</span></span><br><span class="line"><span class="comment">// update happens</span></span><br><span class="line"><span class="keyword">export</span> <span class="class"><span class="keyword">class</span> <span class="title">Clock</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">        <span class="keyword">super</span>(props);</span><br><span class="line">        <span class="keyword">this</span>.state = &#123;<span class="attr">date</span>: <span class="keyword">new</span> <span class="built_in">Date</span>()&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">        );</span><br><span class="line">    &#125;    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Also could use the <code>useState</code> Hook to implement a stateful function-based component.</p></li><li><p>Only modify state data through the <code>setState</code> function. React will batch individual updates to the same state data together for efficiency and the process is asynchronous. Pass a function as the first argument if having a series of dependent changes.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">setState(updater[, callback])</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * the updater could be an object or a function</span></span><br><span class="line"><span class="comment"> * the callback is a function which will be executed once setState is completed and the component</span></span><br><span class="line"><span class="comment"> * is re-rendered. Recommend using componentDidUpdate() instead of it.</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// callback function</span></span><br><span class="line"><span class="keyword">this</span>.setState(&#123; <span class="attr">counter</span>: <span class="keyword">this</span>.state.counter + <span class="number">1</span> &#125;,</span><br><span class="line">            () =&gt; <span class="keyword">this</span>.setState(&#123; <span class="attr">buttonClicked</span>: <span class="keyword">this</span>.state.counter &gt; <span class="number">0</span> &#125;));</span><br><span class="line">            </span><br><span class="line"><span class="comment">// the updater is a function for a same data property of the state</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">this</span>.setState(<span class="function">(<span class="params">state, props</span>) =&gt;</span> &#123; <span class="keyword">return</span> &#123; <span class="attr">counter</span>: state.counter + <span class="number">1</span> &#125;&#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><p>More details here:<br>https://reactjs.org/docs/react-component.html#setstate</p><h3 id="Shallow-Copy">Shallow Copy</h3><p>Props, <code>setState</code> returned objects, <code>useState</code>'s updater function returned objects all are shallow-copied objects. Like this:</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = &#123;<span class="attr">x</span>: &#123;<span class="attr">num</span>: <span class="number">1</span>&#125;&#125;;</span><br><span class="line"><span class="keyword">const</span> clonedObj = <span class="built_in">Object</span>.assign(&#123;&#125;, obj);</span><br><span class="line"><span class="built_in">console</span>.log(obj === clonedObj, obj.x === clonedObj.x);  <span class="comment">// =&gt; false true</span></span><br></pre></td></tr></table></figure><p>The <code>x</code> object is same and <em>mutable</em>, the cloned object is different every time. If all properties of an object are primitive values, it is simple.</p><p>Class-based components could use <code>shouldComponentUpdate(nextProps, nextState)</code> to do deep checks to prevent further updates as a performance optimization.</p><p>Function-based components could use <code>React.memo</code> to check for prop changes, no state checks.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> WrappedFnComponent = React.memo(FnComponent, areEqual);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// usage</span></span><br><span class="line">&lt;WrappedFnComponent /&gt;</span><br></pre></td></tr></table></figure><p>However React doesn't recommend doing deep checks.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyLabel</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/* .... */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line">&lt;MyLabel msg = &#123; anObject &#125;/&gt;</span><br></pre></td></tr></table></figure><p><code>props.msg</code> is read-only, but objects within it are <em>mutable</em>.</p><p>The following example would like to say don't think the deferred operation, such as <code>useEffect</code>, already captured a <em>props</em> or <em>state</em> snapshot, especially when don't know whether they contain mutable objects. The <em>props</em> and <em>state</em> objects aren't <em>immutable</em> objects.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// App.js</span></span><br><span class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="keyword">import</span> &#123;useState, useEffect, useRef&#125; <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">'./App.css'</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Count</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [number, setNumber] = useState(<span class="number">-1</span>);</span><br><span class="line">    <span class="keyword">let</span> timer = useRef(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    useEffect(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="comment">// const savedNumber = props.count.obj.num;  // make a snapshot</span></span><br><span class="line">        setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">            timer.current += <span class="number">1</span>;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="string">'timer'</span>, timer.current);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// setNumber(savedNumber);</span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">// In a closure, use the final value of the variable</span></span><br><span class="line">            setNumber(props.count.obj.num);</span><br><span class="line">        &#125;, <span class="number">10000</span>);</span><br><span class="line">    &#125;, [props]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">p</span>&gt;</span>&#123;`Timer: $&#123;timer.current&#125;, count: $&#123;number&#125;`&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyButton</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [count, setCount] = useState(&#123;<span class="attr">obj</span>: &#123;<span class="attr">num</span>: <span class="number">0</span>&#125;&#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> handleClick = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'click'</span>, count.obj.num, <span class="string">'=&gt;'</span>, count.obj.num + <span class="number">1</span>);</span><br><span class="line">        count.obj.num += <span class="number">1</span>;</span><br><span class="line">        <span class="comment">// make sure the object is different, let React render the component again</span></span><br><span class="line">        setCount(<span class="function"><span class="params">count</span> =&gt;</span> (&#123;...count&#125;));</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">Count</span> <span class="attr">count</span>=<span class="string">&#123;count&#125;</span> /&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;handleClick&#125;</span>&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/&gt;</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">App</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">MyButton</span> /&gt;</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> App;</span><br></pre></td></tr></table></figure><p>Quickly click several times within 10 seconds, would see the span element shows the final number of clicks, then keep it. Except the first timer(no click), other timers see and set a same state value, so don't re-render the <code>Count</code> component. Saving the variable that will be used when firing a effect fixed the problem.</p><p>More details here:<br>https://reactjs.org/docs/react-component.html#shouldcomponentupdate<br>https://reactjs.org/docs/react-api.html#reactmemo</p><h3 id="Sharing-data">Sharing data</h3><p>Sharing data between react components:</p><ul><li>Lift the shared state up to the closest common ancestor, then distribute the state as <em>props</em></li><li><em>context</em> feature</li><li>data store, such as Redux</li><li>URL routing, some state data encoded in the URL</li></ul><h3 id="Events">Events</h3><ul><li><p>Accessing component data in an event handing method</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="class"><span class="keyword">class</span> <span class="title">App</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    handleEvent() &#123;</span><br><span class="line">        <span class="keyword">this</span>.setState(&#123; <span class="attr">message</span>: <span class="string">"Clicked!"</span>&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">// don't work, "this" is undefined when calling the event handler</span></span><br><span class="line">            &lt;button onClick=&#123; <span class="keyword">this</span>.handleEvent &#125; /&gt;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Three alternative ways to fix the problem:</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// To change handleEvent, the arrow function finds the "this" from its enclosing </span></span><br><span class="line"><span class="comment">// scope(automatic binding).</span></span><br><span class="line">handleEvent = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">this</span>.setState(&#123; <span class="attr">message</span>: <span class="string">"Clicked!"</span>&#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// To change onClick</span></span><br><span class="line">onClick=&#123; () =&gt; <span class="keyword">this</span>.handleEvent() &#125;</span><br></pre></td></tr></table></figure><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// To bind handleEvent</span></span><br><span class="line"><span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">    <span class="keyword">super</span>(props);</span><br><span class="line">    <span class="keyword">this</span>.handleEvent = <span class="keyword">this</span>.handleEvent.bind(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>React doesn't support all DOM events and doesn't allow components to create and publish custom events.</p></li><li><p>React always provides a <code>SyntheticEvent</code> object for an event handler, but it will reuse <code>SyntheticEvent</code> objects and reset all the properties to <code>null</code> once an event has been handled, can't use its properties within asynchronous operations.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">handleEvent = <span class="function">(<span class="params">event</span>) =&gt;</span> &#123;</span><br><span class="line">    event.persist();  <span class="comment">// must add, otherwise event.type is null</span></span><br><span class="line">    <span class="keyword">this</span>.setState(&#123; <span class="attr">counter</span>: <span class="keyword">this</span>.state.counter + <span class="number">1</span>&#125;,</span><br><span class="line">        () =&gt; <span class="keyword">this</span>.setState(&#123; <span class="attr">message</span>: <span class="string">`<span class="subst">$&#123;event.type&#125;</span>: <span class="subst">$&#123;<span class="keyword">this</span>.state.counter&#125;</span>`</span>&#125;));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>Event handing excludes the custom HTML elements that are used to apply components.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// CustomButton is a React component that custom the &lt;button&gt; element</span></span><br><span class="line"><span class="comment">// won't invoke the handleClick, no errors</span></span><br><span class="line">&lt;CustomButton onClick=&#123; <span class="keyword">this</span>.handleClick &#125;/&gt;</span><br></pre></td></tr></table></figure></li><li><p>Event phase</p><ul><li>Capture phase: The event is being propagated through the target's ancestor objects, This process starts with the <code>Window</code>, and through descendent elements until the target's parent is reached. Would invoke the event handles that are added by <code>addEventListener()</code> with <code>useCapture</code> option. In React, use the <code>onClickCapture</code> property of an element to specify the event handler.</li><li>Bubble phase: The event is being propagated in reverse order of the capture phase, staring with the parent and eventually reaching the <code>Window</code>. Once the event is handled by the target element, it enters the bubble phase.</li></ul><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Event/eventPhase</p></li></ul><h3 id="Component-Lifecycle">Component Lifecycle</h3><p>Class-based components:</p><ul><li>Mount phase: <code>constructor()</code> -&gt; <code>getDerivedStateFromProps()</code> -&gt; <code>render()</code> -&gt; <code>componentDidMount()</code></li><li>Update phase: <code>getDerivedStateFromProps()</code> -&gt; <code>shouldComponentUpdate()</code> -&gt; <code>render()</code> -&gt; <code>getSnapshotBeforeUpdate()</code> -&gt; <code>componentDidUpdate()</code></li><li>Umount phase: <code>componentWillUnmount()</code></li></ul><p><img src="/images/react-lifecycle.png" alt="React Lifecycles"></p><p>Initializing a component and changing the state or props will call the render method.</p><p>When a component’s <em>props</em> or <em>state</em> change, React compares the newly returned element with the previous rendered one. When they are not equal, React will update the DOM. This process is called <em>reconciliation</em>. The process uses React's diffing algorithm to minimize changes to the DOM on every update.</p><p>The main use of the <code>componentDidUpdate</code> method is to directly manipulate the HTML elements in the DOM using the React refs feature, the refs feature provides access to the HTML elements rendered by a component after they have been added to the DOM, the method is called even if the <em>reconciliation</em> process determines that the content generated by the component has not changed.</p><p>A component's state can be passed to its decendant components as props, its state is changed, its decendant components are rendered as well. If the update is unnecessary, let its decendant components' <code>shouldComponentUpdate</code> method return false, React won't call these components' <code>render</code> and <code>componentDidUpdate</code> methods.</p><p>React will trigger the component's update phase when an ancestor's state has changed, the <code>getDerivedStateFromProps</code> method may be called even though none of the prop values on which the component depends has changed.</p><p>Use the <code>getSnapshotBeforeUpdate</code> method to capture information from the DOM before it is potentially changed. Could be used in conjunction with the refs feature, such as to remember and restore the scroll postion.</p><p>Changing the top-level element within a component will cause React to replace its decendant components without the <em>reconciliation</em> process. Don't do it whenever possible.</p><p>Don't make HTTP requests in the <code>render</code> method that is called often, move the requests to the <code>componentDidMount</code> method, but if the component would be mounted and unmounted frequently, lifting the requests up to a stable component to avoid unnecessary data requests. Once the data arrives in the <code>componentDidMount</code> method, it changes the state, the update phase comes into play, the <code>render</code> method is called again.</p><p>More deatils here:<br>https://reactjs.org/docs/react-component.html<br>https://github.com/wojtekmaj/react-lifecycle-methods-diagram</p><p>Function-based components:</p><ul><li>Function-based components can't participate in the component lifecycle in the same way like class-based components. The <code>React.useEffect</code> Hook is used to register a function that will be invoked when the component is mounted, updated, and unmounted, it is a three-in-one function.</li></ul><h3 id="Composition-vs-Inheritance">Composition vs Inheritance</h3><p>Composition instead of inheritance to reuse components.</p><ul><li><p>Use the special <code>children</code> prop</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">FancyBorder</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        &#123; props.children &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">    );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">WelcomeDialog</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        &lt;FancyBorder&gt;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        &lt;<span class="regexp">/FancyBorder&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure><p>The <code>WelcomeDialog</code> component reuses the <code>FancyBorder</code> component. The <code>props.children</code> will be replaced by the elements included by <code>FancyBorder</code> in the <code>WelcomeDialog</code> component.</p><p>There are <code>map</code>, <code>forEach</code>, <code>toArray</code> etc. methods in <code>React.Children</code>, could use them to get advanced knowledge about these children, with <code>React.cloneElement</code> to modify them, such as adding new props.</p></li><li><p>Pass components as props</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">App</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        &lt;SplitPane left = &#123; &lt;Contacts /&gt; &#125; right = &#123; &lt;Chat /&gt; &#125; /&gt;</span><br><span class="line">    );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>Contacts</code> and <code>Chat</code> are components, and are also objects, could pass them as props.</p></li><li><p>A <em>specific</em> component renders a <em>generic</em> one</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">WelcomeDialog</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">Dialog</span> <span class="attr">title</span>=<span class="string">'Welcome'</span> <span class="attr">message</span>=<span class="string">'Hi'</span> /&gt;</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The <code>WelcomeDialog</code> component configures a generic <code>Dialog</code> with props, making it specific.</p></li></ul><h3 id="Higher-order-components-HOCs">Higher-order components (HOCs)</h3><p>A HOC is a function that takes a component and returns a new component. It composes the original component by wrapping it in a container component.</p><p>Some tasks, like logging, data retrieval, have same logical process flow. HOCs abstract this logic in the container component and share it across many components, the wrapped component receives data that generated by this logic as props to do its own jobs.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">withSubscription</span>(<span class="params">WrappedComponent, selectData</span>) </span>&#123;  <span class="comment">// takes a component</span></span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;                 <span class="comment">// return a component</span></span><br><span class="line">        <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">            <span class="keyword">super</span>(props);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">            </span><br><span class="line">            <span class="keyword">this</span>.state = &#123;</span><br><span class="line">                data: selectData(DataSource, props)</span><br><span class="line">            &#125;;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        render() &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">WrappedComponent</span> <span class="attr">data</span>=<span class="string">&#123;this.state.data&#125;</span> &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Put HOCs outside the component definition so that it is created only once, otherwise a new version of the component is returned and umount/remount it each time.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> BlogPostWithSubscription = withSubscription(</span><br><span class="line">    BlogPost,</span><br><span class="line">    (DataSource, props) =&gt; DataSource.getBlogPost(props.id)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="class"><span class="keyword">class</span> <span class="title">App</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The wrapped components or container components could be function-base components or class-based components, stateless components or stateful components.</p><p>The wrapped components could be wrapped many times.</p><h3 id="Render-Props">Render Props</h3><p>This is a technique for sharing code between React components using a prop whose value is a function.</p><p>Alternative model of wrapping one component in another. It could implement most HOCs using a regular component with a <code>render</code> prop. It is simpler than HOCs.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Mouse</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">        <span class="keyword">super</span>(props);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">this</span>.state = &#123; <span class="attr">x</span>: <span class="number">0</span>, <span class="attr">y</span>: <span class="number">0</span> &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">             </span><br><span class="line">            &#123;<span class="keyword">this</span>.props.render(<span class="keyword">this</span>.state)&#125;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MouseTracker</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">/* The place of the render prop needs a function,  </span></span><br><span class="line"><span class="comment">             * could use other names rather than "render", but</span></span><br><span class="line"><span class="comment">             * make sure to change the name of the line 12</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            &lt;Mouse render = &#123; mouse =&gt; ( <span class="xml"><span class="tag">&lt;<span class="name">Cat</span> <span class="attr">mouse</span> =  <span class="string">&#123;mouse&#125;</span> /&gt;</span></span> ) &#125; /&gt;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Context">Context</h3><p>Context provides a way to pass data through the top-down component tree without having to pass props down manually at the intermediate components.</p><p>Only context consumers inside the context provider could consume the data. Context is also top-down hierarchy.</p><p>Context makes component reuse more difficult, use it if the same data needs to be accessible by many components in the tree, and at different nesting levels.</p><p>If only to avoid passing props, component composition is a simpler alternative solution: lifting the components that use these props up to the top component that have these props, then pass them as one or more props through the intermediate components, but this method also makes the high-level components to focus on the low-level components.</p><p>In function-based components, use the <code>React.useContext</code> Hook to get the context value.</p><p>More details here:<br>https://reactjs.org/docs/context.html</p><h3 id="Error-Boundaries">Error Boundaries</h3><p>Error boundaries are React components that handle and recover JavaScript errors anywhere in their child component tree instead of crash the component tree.</p><p>Error boundaries apply only to errors that are thrown in:</p><ul><li>constructors</li><li>lifecycle methods</li><li>during rendering</li></ul><p>But don't catch errors from:</p><ul><li>event handlers</li><li>asynchronous code(such as HTTP requests, setTimeout)</li><li>the error boundary itself</li></ul><p>Error handling: <code>getDerivedStateFromError()</code> -&gt; <code>componentDidCatch()</code> -&gt; <code>render()</code>.</p><p>The <code>getDerivedStateFromError</code> method receives the error and returns a value to update state, don't make something that have side-effects, if have to do, use the <code>componentDidCatch</code> method instead. The rendered content is handled using the mounting phase of the component lifecycle, so could show error messages and include the original components with the special <code>children</code> prop, or do something to recover.</p><h3 id="Refs">Refs</h3><p>The Refs feature provides access to the HTML elements rendered by a component after they have been added to the DOM, such as invoking the <code>focus</code> method on the <code>input</code> element. This is another way except props that parent components interact with their children.</p><p>Refs are not assigned until React invokes the <code>render</code> method. ref updates happen before <code>componentDidMount</code> or <code>componentDidUpdate</code>, the two lifecycle methods can access the elements with a ref. If the component is re-created, the <code>componentWillUnmount</code> can be used to accesss the refs.</p><p>When to use</p><ul><li><p>Use features of a specific HTML element, such as focus, selection</p></li><li><p>Triggering imperative animations</p></li><li><p>Integrating with third-party DOM libraries</p><p>Use refs only as the last resort, a component that makes excessive use of refs is difficult to manage.</p></li></ul><p>What it is</p><ul><li><p>A reference to a DOM node</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="class"><span class="keyword">class</span> <span class="title">Profile</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">        <span class="keyword">super</span>(props);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">this</span>.nameRef = React.createRef();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    focusTextInput = <span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">this</span>.nameRef.current.focus();</span><br><span class="line">    </span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> (</span><br><span class="line">            &lt;&gt;</span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">            </span><br><span class="line">             &lt;input type=<span class="string">"text"</span> ref = &#123; <span class="keyword">this</span>.nameRef &#125; /&gt;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">/* ... */</span></span><br><span class="line">            &lt;<span class="regexp">/&gt;</span></span><br><span class="line"><span class="regexp">        );    </span></span><br><span class="line"><span class="regexp">    &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure><p><code>this.nameRef.current</code> is the <code>&lt;input&gt;</code> element.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">FunctionBasedProfile</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> nameRef = React.useRef(<span class="literal">null</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">click</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        nameRef.current.focus();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        &lt;input type=<span class="string">"text"</span> ref=&#123;nameRef&#125; /&gt;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">    );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>nameRef.current</code> is the <code>&lt;input&gt;</code> element.</p></li><li><p>The mounted instance of a class-based component</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">constructor</span>(props) &#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">this</span>.textInput = React.createRef();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">componentDidMount () &#123;</span><br><span class="line">    <span class="comment">// call the Profile component's focusTextInput method</span></span><br><span class="line">    <span class="keyword">this</span>.textInput.current.focusTextInput();</span><br><span class="line">&#125;        </span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line">render() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">Profile</span> <span class="attr">ref</span>=<span class="string">&#123;this.textInput&#125;</span> /&gt;</span></span></span><br><span class="line">&#125;    </span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br></pre></td></tr></table></figure><p><code>this.textInput.current</code> is an instance of the <code>Profile</code> component rather than a DOM node.</p></li><li><p>Function-based components don't have instances, can't use refs on them directly. Could use <code>forwardRef</code> (possibly in conjunction with <code>useImperativeHandle</code>) to take a ref.</p></li></ul><p>Exposing DOM Refs to Parent Components</p><ul><li><p>Receive the ref through the <code>ref</code> attribute, like the above two examples, can't work with function-based components if no <code>forwardRef</code>;</p></li><li><p>Receive the ref as a normal prop whose name isn't <code>ref</code> from parent components, then pass the ref to children;</p></li><li><p>Use <code>React.forwardRef</code>;</p><p>More details here:<br>https://reactjs.org/docs/refs-and-the-dom.html<br>https://reactjs.org/docs/hooks-reference.html#useimperativehandle</p></li></ul><p>Callback Refs</p><p>Could pass a function to the <code>ref</code> attribute. The function receives a React component or an HTML element as its argument which is guaranteed to be up-to-date before <code>componentDidMount</code> or <code>componentDidUpdate</code> fires. React will call the callback function when the component mounts and unmounts. If the callback function is inline, it would be call twice during updates: one is to clear the old ref, one is to set up the new ref, could define the callback function as a class method and bind the method to the class, this shouldn't matter in most cases.</p><h3 id="Portals">Portals</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.createPortal(child, container)</span><br></pre></td></tr></table></figure><p><code>child</code> is any component returned by the <code>render</code> method of class-based components or the function-based components;</p><p><code>container</code> is a DOM element;</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// class-based</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ClsPortal</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> ReactDOM.createPortal(<span class="keyword">this</span>.props.children, container);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    render() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">ClsPortal</span>&gt;</span> <span class="tag">&lt;<span class="name">AnyChildComponent</span> /&gt;</span> <span class="tag">&lt;/<span class="name">ClsPortal</span>&gt;</span></span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// function-based</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">FnPortal</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> ReactDOM.createPortal(props.children, container);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Parent</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">FnPortal</span>&gt;</span> <span class="tag">&lt;<span class="name">AnyChildComponent</span> /&gt;</span> <span class="tag">&lt;/<span class="name">FnPortal</span>&gt;</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>They render <code>AnyChildComponent</code> to the <code>container</code> DOM node instead of the parent component.</p><p>A portal allows a component to render its content into a specific DOM node that exists outside the DOM hierarchy of the parent component.</p><p>If placing a portal in a component, the portal behaves like a normal child component, features like context, event bubbling work normally and don't care it is a portal, the component would catch uncaught event from the portal, not the portal attached DOM node.</p><p>Can't use a portal with an element using a ref. Portals are used during the render process, Refs are assigned after the process.</p><h3 id="Hooks">Hooks</h3><p>Hooks are a new <strong>addition</strong> in React 16.8, it doesn't break anything. We could use state and other React features without writing a class.</p><p>It is hard to reuse stateful logic between components, <em>render props</em> and <em>higher-order components(HOC)</em> try to solve the problem, but the two patterns require to restructure the components, Hooks allow to reuse stateful logic without changing the compoent hierarchy.</p><p>Hooks are <strong>functions</strong> that let you &quot;hook into&quot; React state and lifecycle features from function-based components.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Clock</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [date, setDate]=useState(<span class="keyword">new</span> <span class="built_in">Date</span>());</span><br><span class="line">    </span><br><span class="line">    useEffect(<span class="function"><span class="params">()</span> =&gt;</span> <span class="built_in">document</span>.title = <span class="string">`Current time: <span class="subst">$&#123;date.toDateString()&#125;</span>`</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>useState</code> and <code>useEffect</code> are Hooks. Use <code>useState</code> to get and set state. <code>useEffect</code> will be invoked after every render(mount phase, update phase).</p><h4 id="useState">useState</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [XXX, setXXX] = useState(initialXXX);</span><br><span class="line"><span class="keyword">const</span> [XXX, setXXX] = useState(<span class="function"><span class="params">()</span> =&gt;</span> &#123; <span class="comment">/* expensive computation */</span> &#125;);  <span class="comment">// Lazy initialization</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Example:</span></span><br><span class="line"><span class="keyword">const</span> [count, setCount] = useState(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// XXX is a state, setXXX is the function to update it;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// setXXX syntax:</span></span><br><span class="line">setXXX( expression );</span><br><span class="line"></span><br><span class="line">setXXX(<span class="function"><span class="params">prevState</span> =&gt;</span> nextState);</span><br></pre></td></tr></table></figure><ul><li>The function returned by <code>useState</code> doesn't automatically merge updated objects, it simply accepts its arguemnt as the new state and overwrite the old one. <code>useReducer</code> is more suited for managing multiple sub-values state objects;</li><li>If the new state is computed using the previous state, pass a function instead of an expression to the function returned by <code>useState</code> when updating state;</li><li>If the initial value of a state is an expensive computation, pass a function instead of a normal expression to <code>useState</code>;</li><li>If <code>setXXX</code> returns a same value as the current state, React doesn't render the children and fire the effects, but it doesn't mean that React doesn't render any component before making the decision. Use <code>useMemo</code> to optimize expensive calculations when rendering.</li></ul><h4 id="useEffect">useEffect</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">useEffect(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* ... */</span></span><br><span class="line">        </span><br><span class="line">    &#125;    </span><br><span class="line">&#125;, [conditon1, condition2])</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * The returned cleanup function and the condition array are optional;</span></span><br><span class="line"><span class="comment"> * The condition array is not passed as arguments to the effect function;</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><ul><li>Do works that have side effects during rendering, such as timer, logging. React might pause, abort, restart the render;</li><li>It runs both after the first render and after every update;</li><li>Unlike <code>componentDidMount</code> or <code>componentDidUpdate</code>, effects scheduled with <code>useEffect</code> don’t block the browser from updating the screen, they are deferred. For effects that maintain DOM to show immediately, use <code>useLayoutEffect</code> instead;</li><li>If the effect returns a function, the function is a cleanup function that does things like <code>componentWillUnmount</code>(unmount phase), the function is invoked <strong>before running the next effect</strong> and <strong>during unmounting</strong>;</li><li><code>useEffect</code> has a optional second argument, pass an array to it as a condition to determine whether to run the effect and its cleanup function. If the array is an empty array, the effect only runs once and its cleanup function is only invoked during unmounting;</li><li>Could call <code>useEffect</code> multiple times in a function-based component, and they are independent, so invoke the cleanup function before running the next effect to avoid bugs;</li><li>Every referenced value inside the effect function should appear in the condition array, including props and the state;</li><li>Should declare functions needed by the effect function inside of it, it is easy to see what values are depended on. Functions that don't use any value from the component scope could be moved out. Also could use <code>useCallback</code> to wrap functions, then the wrapper appears in the condition array, the effect depend on it;</li></ul><h4 id="useLayoutEffect">useLayoutEffect</h4><ul><li>The signature is identical to <code>useEffect</code>;</li><li><code>useLayoutEffect</code> fires after a component is mounted and before the browser updates the screen. It is in the same phase as <code>componentDidMount</code> and <code>componentDidUpdate</code>;</li></ul><h4 id="useContext">useContext</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> value = useContext(MyContext);</span><br></pre></td></tr></table></figure><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> React, &#123;useContext&#125; <span class="keyword">from</span> <span class="string">'react'</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">'./App.css'</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Text</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> curTheme = useContext(ThemeContext);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&#123;&#123;top:</span> <span class="attr">0</span>, <span class="attr">left:</span> <span class="attr">0</span>, <span class="attr">bottom:</span> <span class="attr">0</span>, <span class="attr">right:</span> <span class="attr">0</span>, <span class="attr">position:</span> '<span class="attr">fixed</span>',</span></span></span><br><span class="line"><span class="xml">        zIndex: -1, background: curTheme.background&#125;&#125;/&gt;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> themes = &#123;</span><br><span class="line">    light: &#123;</span><br><span class="line">        background: <span class="string">"#eeeeee"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    dark: &#123;</span><br><span class="line">        background: <span class="string">"#222222"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> ThemeContext = React.createContext(themes.light);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">App</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (</span><br><span class="line">        &lt;ThemeContext.Provider value=&#123;themes.dark&#125;&gt;</span><br><span class="line">            &lt;Text /&gt;</span><br><span class="line">        &lt;<span class="regexp">/ThemeContext.Provider&gt;</span></span><br><span class="line"><span class="regexp">    );</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">export default App;</span></span><br></pre></td></tr></table></figure><p>A changed context value trigger a rerender of the <code>Text</code> component. <code>React.memo</code> or <code>shouldComponentUpdate</code> checks props and/or state to prevent a unnecessary rerender, they can't prevent context.</p><p>Putting a callback function into the context lets child components to call it and update the context values.</p><h4 id="useReducer">useReducer</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> [state, dispatch] = useReducer(reducer, initialArg, init);</span><br><span class="line"></span><br><span class="line">reducer = <span class="function">(<span class="params">state, action</span>) =&gt;</span> nextState;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Calling dispatch(action) will call the reducer;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * initialArg is the initial state;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * init(initialArg) will be used to create the initial state lazily (the </span></span><br><span class="line"><span class="comment"> * state is the result of an expensive computation, and will be </span></span><br><span class="line"><span class="comment"> * executed only on the initial render) or to reset the state later.</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><ul><li>An alternative to <code>useState</code>;</li><li>Use it when having complex state logic that involves multiple sub-values;</li><li>Use it when the next state depends on the previous one;</li><li>Use it when components trigger deep updates. Could pass the <code>dispatch</code> function down as <strong>context</strong> or <strong>props</strong>. The function is stable and won't change on re-renders, reading it doesn't cause a re-render, unlike state;</li><li>If the <code>dispatch</code> function returns a same value as the current state, React doesn't render the children and fire the effects, but it doesn't mean that React doesn't render any component before making the decision. Use <code>useMemo</code> to optimize expensive calculations when rendering.</li></ul><h4 id="useMemo">useMemo</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> memoizedValue = useMemo(<span class="function"><span class="params">()</span> =&gt;</span> computeExpensiveValue(a, b), [a, b]);</span><br><span class="line"></span><br><span class="line"><span class="comment">// The condition array is not passed as arguments to the function;</span></span><br></pre></td></tr></table></figure><ul><li>Returns a <strong>memoized value</strong>, it can be reused on the next render;</li><li>Use it to <strong>optimize performance</strong> when have expensive computations <strong>during rendering</strong>, the code should also work without it;</li><li>If no condition array is provided, a new value will be computed on every render;</li><li>Every value referenced inside the <code>computeExpensiveValue</code> function should appear in the condition array, like <code>useEffect</code>;</li></ul><h4 id="useCallback">useCallback</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> memoizedCallback = useCallback(<span class="function">(<span class="params">[<span class="built_in">arguments</span>]</span>) =&gt;</span> &#123; <span class="comment">/* ... */</span> &#125;, [a, b]);</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * useCallback(fn, deps) is equivalent to useMemo(() =&gt; fn, deps)</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * The condition array is not passed as arguments to the callback function;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * The callback might have arguments;</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><ul><li>Returns a <strong>memoized version</strong> of the callback if one of the conditions has changed;</li><li>Every value referenced inside the callback function should appear in the condition array, like <code>useEffect</code>;</li><li>It could be passed as a prop to child components, re-render the child components only when its dependencies and itself are changed. Leave the logic and data in the parent component.</li></ul><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyButton</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [count, setCount] = useState(<span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// create a new handleClick function every time when </span></span><br><span class="line">    <span class="comment">// entering the MyButton function</span></span><br><span class="line">    <span class="keyword">const</span> handleClick = <span class="function"><span class="params">()</span> =&gt;</span> setCount(<span class="function"><span class="params">c</span> =&gt;</span> c + <span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">p</span>&gt;</span>count: &#123;count&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;handleClick&#125;</span>&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// use useCallback</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyButton</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [count, setCount] = useState(<span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Only create the handleClick function once</span></span><br><span class="line">    <span class="keyword">const</span> handleClick = useCallback(<span class="function"><span class="params">()</span> =&gt;</span> setCount(<span class="function"><span class="params">c</span> =&gt;</span> c + <span class="number">1</span>), []);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">p</span>&gt;</span>count: &#123;count&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;handleClick&#125;</span>&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Sometimes don't use useCallback in this way</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MyButton</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [count2, setCount2] = useState(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">const</span> [count3, setCount3] = useState(<span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">const</span> handleClick = useCallback(<span class="function"><span class="params">()</span> =&gt;</span> </span><br><span class="line">        &#123;setCount2(<span class="function"><span class="params">c</span> =&gt;</span> c + <span class="number">1</span>); setCount3(<span class="function"><span class="params">c</span> =&gt;</span> c + count2&#125;</span><br><span class="line">        , []);</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">p</span>&gt;</span>count2: &#123;count2&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">p</span>&gt;</span>count3: &#123;count3&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;<span class="name">button</span> <span class="attr">onClick</span>=<span class="string">&#123;handleClick&#125;</span>&gt;</span>Click<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="xml">        <span class="tag">&lt;/&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * the value of count2 in the setCount3 function is always 1, </span></span><br><span class="line"><span class="comment"> * useCallback returns a memoized callback. If change </span></span><br><span class="line"><span class="comment"> * count2 to this style:</span></span><br><span class="line"><span class="comment"> * const [count2, setCount2] = useState(&#123;obj: &#123;num: 1&#125;&#125;), </span></span><br><span class="line"><span class="comment"> * it works, React does shallow copy for count2 every time, but</span></span><br><span class="line"><span class="comment"> * the &#123;num: 1&#125; object is always same, the callback function </span></span><br><span class="line"><span class="comment"> * can read the increased value of num.</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><h4 id="useRef">useRef</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> refContainer = useRef(initialValue);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * refContainer is mutable;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * refContainer has a .current property whose value is initialized to initialValue;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * refContainer will persist for the full lifetime of the component;</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><ul><li>Could pass any normal value to the <code>.current</code> property, as well as a DOM node on the <code>ref</code> attribute of a component;</li><li>The ref object is stable and won't change on re-renders;</li><li><code>useRef</code> doesn’t notify when the <code>.current</code> property changes and doesn't cause a re-render;</li></ul><p>Callback Refs:</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">CallbackRef</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> cbRef = useCallback(<span class="function"><span class="params">node</span> =&gt;</span> &#123; <span class="comment">/* ... */</span> &#125;, []);</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">h1</span> <span class="attr">ref</span> = <span class="string">&#123;</span> <span class="attr">cbRef</span> &#125;/&gt;</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The callback ref will be called only when the component mounts(attach a DOM node) and unmounts(detach a DOM node).</p><h4 id="useImperativeHandle">useImperativeHandle</h4><ul><li><code>useImperativeHandle</code> customizes the element instance that is exposed to parent components, such as replacing the native focus function with your own, changing the returned ref of a table to a table cell;</li><li><code>useImperativeHandle</code> should be used with <code>forwardRef</code>. <code>forwardRef</code> lets a component pass the received ref further down to a child;</li></ul><h4 id="useDebugValue">useDebugValue</h4><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">useDebugValue(value);</span><br><span class="line"></span><br><span class="line">useDebugValue(value, value =&gt; &#123; <span class="comment">/* ... */</span> &#125;);</span><br></pre></td></tr></table></figure><ul><li><code>useDebugValue</code> displays a label for custom hooks in React DevTools;</li></ul><h4 id="Hook-Rules">Hook Rules</h4><ul><li>Only call Hooks at the top level of function-based components, not inside loops, conditions, or nested functions. React relies on the order of Hooks to restore the state during re-render.</li><li>Only call Hooks from function-based components or custom Hooks;</li></ul><h4 id="Custom-Hooks">Custom Hooks</h4><ul><li>The Hook name <strong>should</strong> always start with <strong>use</strong>;</li></ul><p>There are no Hook equivalents to <code>getSnapshotBeforeUpdate</code>, <code>getDerivedStateFromError</code> and <code>componentDidCatch</code> lifecycles yet.</p><h3 id="Strict-Mode">Strict Mode</h3><p>Strict mode checks potential problems. It doesn't render any visible UI and runs in development mode only.</p><p>An important functionality is to detect unexcepted side effects. The frequently used methods, constructor, <code>render</code> and <code>setState</code>, might be called more than once. These methods should not contain side-effects, it means giving same arguments to these functions should get a same result.</p><p>For finding side-effects early, React invokes contructor, <code>render</code>, <code>setState</code>, functions passed to <code>useState</code>, <code>useMemo</code> and <code>useReducer</code>, function-based component body and other methods or functions(<a href="https://reactjs.org/docs/strict-mode.html#detecting-unexpected-side-effects" target="_blank" rel="noopener">here</a>) twice.</p><p><code>create-react-app</code> created projects have the following code in <code>index.js</code>:</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">ReactDOM.render(</span><br><span class="line">    &lt;React.StrictMode&gt;</span><br><span class="line">        &lt;App /&gt;</span><br><span class="line">    &lt;<span class="regexp">/React.StrictMode&gt;,</span></span><br><span class="line"><span class="regexp">    document.getElementById('root')</span></span><br><span class="line"><span class="regexp">);</span></span><br></pre></td></tr></table></figure><p>It opens the strict mode by default.</p><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">useCount</span>(<span class="params">props</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> [count, setCount] = useState(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`call useCount: <span class="subst">$&#123;count&#125;</span>`</span>);</span><br><span class="line"></span><br><span class="line">    useEffect(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">`set count from <span class="subst">$&#123;count&#125;</span> to <span class="subst">$&#123;count + <span class="number">1</span>&#125;</span>`</span>);</span><br><span class="line">    &#125;, [props]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">MySpan</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> val = useCount(<span class="string">'nothing'</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`call MySpan: <span class="subst">$&#123;val&#125;</span>`</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">span</span>&gt;</span>&#123;val&#125;<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">App</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">MySpan</span>/&gt;</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The code in <code>App.js</code> will print the following log:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">call useCount: 0</span><br><span class="line">call MySpan: 0</span><br><span class="line">call useCount: 0</span><br><span class="line">call MySpan: 0</span><br><span class="line">set count from 0 to 1</span><br><span class="line">call useCount: 1</span><br><span class="line">call MySpan: 1</span><br><span class="line">call useCount: 1</span><br><span class="line">call MySpan: 1</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>React</tag>
        <tag>Notes</tag>
      </tags>
  </entry>
  <entry>
    <title>Know JavaScript</title>
    <url>/2020/01/20/know-javascript/</url>
    <content><![CDATA[<h2 id="Introduction">Introduction</h2><p>This is a JavaScript guide for developers who already know other programming languages.</p><a id="more"></a><h2 id="What-it-is">What it is</h2><ul><li>Untyped, prototype-based, single-threaded scripting language</li><li>Native Unicode (UTF-16)</li><li>Case-sensitive</li><li>First-class functions (support functional programming styles)</li><li>A minimal API for working with text, arrays, dates, and regular expressions, but no Input/Output, networking, storage, and graphics, these are supported by web browsers.</li></ul><h2 id="Identifiers">Identifiers</h2><p>Begin with letter, <code>_</code>, <code>$</code>, then followed by letters, digits, <code>_</code>, <code>$</code></p><h2 id="Comments">Comments</h2><ul><li><code>//</code>, for single line</li><li><code>/* ... */</code>, for single line or multiple lines</li></ul><h2 id="Statements">Statements</h2><p>The semicolon (<code>;</code>) is used to separate statements, it is optional, but no surprising cases when using it</p><h2 id="Data-types">Data types</h2><ul><li><p>Primitive types</p><ul><li>number</li><li>string</li><li>boolean</li><li>null</li><li>undefined</li><li>Symbol</li><li>BigInt</li></ul><p>They are immutable and are compared by value</p></li><li><p>Object types</p><ul><li>array and function are special objects;</li></ul><p>They are mutable and are compared by reference</p></li></ul><h3 id="number">number</h3><p>Array indexing and the bitwise operators are 32-bit integers, All other numbers are represented as 64-bit binary floating-point values(IEEE-754), it can exactly represent <code>1/2</code>, <code>1/8</code>, ..., <code>1/2^n</code>, but can't exactly represent <code>1/10</code>, <code>1/100</code>, they are decimal fractions.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">x=<span class="number">.3</span> <span class="number">-.2</span>  <span class="comment">// =&gt; 0.09999999999999998</span></span><br><span class="line">y=<span class="number">.2</span> <span class="number">-.1</span>  <span class="comment">// =&gt; 0.1</span></span><br><span class="line">x==y      <span class="comment">// =&gt; false</span></span><br><span class="line"></span><br><span class="line"><span class="number">100</span>          <span class="comment">// base-10 integer</span></span><br><span class="line"><span class="number">0xff</span> / <span class="number">0Xff</span>  <span class="comment">// hexadecimal integer</span></span><br></pre></td></tr></table></figure><p>Arithmetic operators:</p><ul><li><code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>%</code>, <code>**</code>, <code>++</code>, <code>--</code></li><li>Math object: <code>Math.log(100)</code></li></ul><p>Doesn't raise overflow, underflow, division by zero errors, but the arithmetic results are special values:</p><ul><li>overflow: Infinity / -Infinity</li><li>underflow: 0 / -0</li><li>division by zero: Infinity / -Infinity, but 0 / 0 is NaN(not a number)</li></ul><p><code>Infinity</code>, <code>NaN</code> are global variables. <code>Infinity</code> represents infinity. <code>NaN</code> represents not-a-number.</p><p>To check <code>x</code> is <code>NaN</code>: <code>x != x</code>, it is true if only if <code>x</code> is <code>NaN</code>, don't use <code>x == NaN</code>.</p><h3 id="string">string</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">"abcde"</span></span><br><span class="line"><span class="string">'abcde'</span></span><br><span class="line"><span class="string">"ab'cd'e"</span></span><br><span class="line"><span class="string">'ab"cd"e'</span></span><br><span class="line"><span class="string">'can\'t'</span></span><br><span class="line"></span><br><span class="line"><span class="string">'one \</span></span><br><span class="line"><span class="string">very \</span></span><br><span class="line"><span class="string">long \</span></span><br><span class="line"><span class="string">line'</span></span><br></pre></td></tr></table></figure><p>Enclose a string with single or double quotes (<code>'</code> or <code>&quot;</code>). When combining JavaScript and HTML, one style of quotes for JavaScript, the other for HTML.</p><p>A string likes a read-only array, could use index to access.</p><h4 id="Template-Strings">Template Strings</h4><p>A template string is enclosed by the backtick (` `). The dollar sign and curly braces (<code>${expression}</code>) indicate a placeholder.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> name = <span class="string">'JavaScript'</span>;</span><br><span class="line"><span class="keyword">const</span> msg = <span class="string">`Hi, <span class="subst">$&#123;name&#125;</span>`</span>;  <span class="comment">// a template string</span></span><br><span class="line"><span class="built_in">console</span>.log(msg);           <span class="comment">// =&gt; Hi, JavaScript</span></span><br></pre></td></tr></table></figure><p>Tagged templates</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">tag</span>(<span class="params">strings, place</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// =&gt; 2 Hey, where are you?\n &lt;empty string&gt;</span></span><br><span class="line">    <span class="built_in">console</span>.log(strings.length, strings.raw[<span class="number">0</span>], strings.raw[<span class="number">1</span>]);</span><br><span class="line">    <span class="comment">/* </span></span><br><span class="line"><span class="comment">     * 2 Hey, where are you?</span></span><br><span class="line"><span class="comment">     * &lt;empty string&gt;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="built_in">console</span>.log(strings.length, strings[<span class="number">0</span>], strings[<span class="number">1</span>]);</span><br><span class="line">    <span class="comment">/* </span></span><br><span class="line"><span class="comment">     * Hey, where are you?</span></span><br><span class="line"><span class="comment">     * moon</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`<span class="subst">$&#123;strings[<span class="number">0</span>]&#125;</span><span class="subst">$&#123;place&#125;</span>`</span>);</span><br><span class="line">&#125;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">let</span> place = <span class="string">'moon'</span>;</span><br><span class="line">tag<span class="string">`Hey, where are you?\n<span class="subst">$&#123;place&#125;</span>`</span>;</span><br></pre></td></tr></table></figure><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals</p><h3 id="boolean">boolean</h3><p>Any values can be converted to a boolean value: <code>undefined</code>, <code>null</code>, <code>0</code>, <code>-0</code>, <code>NaN</code>, <code>&quot;&quot;</code> are converted to false, others are true.</p><h3 id="null">null</h3><p><code>null</code> is an object, indicates &quot;no value&quot;.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span>(<span class="literal">null</span>)  <span class="comment">// =&gt; "object"</span></span><br></pre></td></tr></table></figure><h3 id="undefined">undefined</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span>(<span class="literal">undefined</span>)  <span class="comment">// =&gt; "undefined"</span></span><br><span class="line"></span><br><span class="line"><span class="literal">null</span> == <span class="literal">undefined</span>  <span class="comment">// =&gt; true</span></span><br></pre></td></tr></table></figure><p>Get an <code>undefined</code> value when access an uninitialized variable, a non-existed object property or array element, a function argument that you don't supply a value, the return value of a function that have not return value.</p><p><code>undefined</code> is a error-like absence of value, if you want a variable has not any value, assign <code>null</code> to it.</p><h3 id="Wrapper-objects">Wrapper objects</h3><p>Numbers, strings, booleans are not objects, but they have properties and methods, because of JavaScript converts them to an object when refer the value of a property or invoke a method, then discard the object.</p><p><code>String()</code>, <code>Number()</code>, <code>Boolean()</code> return wrapper objects.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="keyword">new</span> <span class="built_in">Number</span>(<span class="number">123</span>)</span><br><span class="line">x == <span class="number">123</span>   <span class="comment">// =&gt; true, only compare value</span></span><br><span class="line">x === <span class="number">123</span>  <span class="comment">// =&gt; false, compare value and type</span></span><br></pre></td></tr></table></figure><h2 id="Variables">Variables</h2><h3 id="Variable-Declaration">Variable Declaration</h3><ul><li>var</li></ul><p>Declares a local or global variable, optionally initializing it to a value.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> i</span><br><span class="line">i = <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> j, k</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> m = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> n = <span class="string">'a'</span>, p = <span class="string">'1'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> ( <span class="keyword">var</span> x = <span class="number">0</span>; x &lt; <span class="number">100</span>; x++) &#123;&#125;;</span><br><span class="line"></span><br><span class="line">y = <span class="number">1</span>  <span class="comment">// automatically create a variable y</span></span><br></pre></td></tr></table></figure><ul><li>let</li></ul><p>Declares a block-scoped variable, optionally initializing it to a value.</p><ul><li>const</li></ul><p>Declares a block-scoped constant, the value of a constant can't be changed through reassignment.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> name1 = value1 [, name2 = value2 [, ... [, nameN = valueN]]];</span><br></pre></td></tr></table></figure><p><code>nameN</code> can be any legal identifier;</p><p><code>valueN</code> can be any legal expression, including a function expression;</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> outsideConstant = <span class="number">1</span>; <span class="comment">// outside functions or blocks</span></span><br><span class="line"><span class="comment">// not a property of the global object, unlike the var global variables</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">this</span>.outsideConstant);  <span class="comment">// =&gt; undefined</span></span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* =&gt; ReferenceError: can't access lexical declaration `i' before initialization</span></span><br><span class="line"><span class="comment">     * unlike the var variables, they don't raise a ReferenceError, they are undefined</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// console.log(i);</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* =&gt; SyntaxError: missing = in const declaration</span></span><br><span class="line"><span class="comment">     * constant variables must be initialized</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// const i;</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">const</span> i = <span class="number">1</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* =&gt; SyntaxError: redeclaration of const i</span></span><br><span class="line"><span class="comment">     * can't be redeclared, unlike the var variables</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// const i = '1';</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* =&gt; TypeError: invalid assignment to const `i'</span></span><br><span class="line"><span class="comment">     * can't be reassigned to a new value</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// i = 2;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// obj has a read-only reference to &#123;x: 1, y: 2&#125;</span></span><br><span class="line">    <span class="keyword">const</span> obj = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;;</span><br><span class="line">    <span class="comment">// but can change the properties</span></span><br><span class="line">    obj.x = <span class="number">2</span>;</span><br><span class="line">    obj;  <span class="comment">// =&gt; Object &#123; x: 2, y: 2 &#125;</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">const</span> arr = [];</span><br><span class="line">    arr[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">    arr[<span class="number">1</span>] = <span class="number">2</span>;</span><br><span class="line">    arr; <span class="comment">// =&gt; Array [ 1, 2 ]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="How-to-Choose">How to Choose</h4><p><code>let</code>/<code>const</code> vs <code>var</code>:</p><ul><li>Can't be redeclared</li><li>Declared variables outside functions or blocks aren't a property of the global object</li><li>Can't be used before declaring it</li></ul><p><code>const</code> vs <code>let</code>:</p><ul><li>Must be initialized</li><li>Can't be reassigned</li></ul><p>If the value of a variable won't be changed, use <code>const</code>, otherwise use <code>let</code> whenever possible. The post use many variables declared by <code>var</code> before I think and know the difference.</p><h3 id="Variables-are-Untyped">Variables are Untyped:</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="string">'a string'</span>;</span><br><span class="line">x = <span class="number">1</span>;  <span class="comment">// now becomes a number</span></span><br></pre></td></tr></table></figure><h3 id="Variable-Scope">Variable Scope</h3><p>scope:</p><ul><li>local</li><li>global</li><li>block</li></ul><p>Actually there is a globab object, we can refer to it outside functions using <code>this</code> keyword, we define a property of the global object when declare a global variable using <code>var</code>.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> myGlobalVar = <span class="number">12345</span>;</span><br><span class="line"><span class="keyword">this</span>.myGlobalVar;    <span class="comment">// =&gt; 12345</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// JS's global variables</span></span><br><span class="line"><span class="keyword">this</span>.NaN;            <span class="comment">// =&gt; NaN</span></span><br><span class="line"><span class="keyword">this</span>.undefined       <span class="comment">// =&gt; undefined</span></span><br></pre></td></tr></table></figure><p>The following sample code uses <code>var</code> to declare variables, it is more complicated.</p><p>Function parameters are local variables, like <code>k</code>;</p><p>Local variables that are within function body must always use the <code>var</code> or <code>let</code> or <code>const</code> statement to declare, don't ignore them, otherwise the variable is global, like <code>z</code>;</p><p><code>x</code>, <code>y</code>(outside the function testScope), <code>z</code> are global variables;</p><p>Could declare local variables anywhere within a function, all of them have a same scope, like <code>localVar</code>, <code>y</code>(declared in the function <code>testScope</code>);</p><p>All variables declared within a function are visible everywhere in the function, even places before they are declared. like <code>y</code>:</p><ul><li>line 15, at first glance, should be <code>y: global variable: y</code>, but the local <code>y</code> already hide the global one before it is declared, and hide it throughout the function;</li><li>the code won't go to line 20, but the declaration of <code>y</code> is still available;</li></ul><p>Declaring all variables at the top of the function is obvious to reflect their scope;</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// defined global variables</span></span><br><span class="line"><span class="keyword">var</span> x = <span class="string">'global variable: x'</span>;</span><br><span class="line"><span class="keyword">var</span> y = <span class="string">'global variable: y'</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testScope</span>(<span class="params"> k <span class="regexp">/* a local variable */</span> </span>) </span>&#123;</span><br><span class="line">    <span class="comment">// x is the above x, is global, not local</span></span><br><span class="line">    x = <span class="string">'changed the global variable: x'</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// automatically create a global variable z</span></span><br><span class="line">    z = <span class="string">'global variable: z'</span>;</span><br><span class="line">        </span><br><span class="line">    <span class="comment">// defined a local variable</span></span><br><span class="line">    <span class="keyword">var</span> localVar = <span class="string">'local variable'</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'y:'</span>, y);       <span class="comment">// =&gt; y: undefined</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>(k)&#123;</span><br><span class="line">        <span class="comment">// defined a local variable, </span></span><br><span class="line">        <span class="comment">// and hide the global one that has the same name</span></span><br><span class="line">        <span class="keyword">var</span> y = <span class="string">'local variable: y'</span>;</span><br><span class="line">        <span class="comment">// OR</span></span><br><span class="line">        <span class="comment">// var y;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">testScope();</span><br><span class="line"><span class="built_in">console</span>.log(x);    <span class="comment">// =&gt; changed the global variable: x</span></span><br><span class="line"><span class="built_in">console</span>.log(y);    <span class="comment">// =&gt; global variable: y</span></span><br></pre></td></tr></table></figure><h2 id="Objects">Objects</h2><h3 id="Create-new-object">Create new object</h3><ul><li>Using object initializers</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// empty object, no properties</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;</span><br><span class="line"><span class="comment">// could add properties later</span></span><br><span class="line">obj.x = <span class="number">1</span>;</span><br><span class="line">obj.y = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> usefulObj = &#123;</span><br><span class="line">    name: <span class="string">'foo'</span>,</span><br><span class="line">    anotherObj: &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;,</span><br><span class="line">    foo: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; </span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'I am foo'</span>);</span><br><span class="line">    &#125;,</span><br><span class="line">    bar() &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'The function bar is a ES6 shorthand method name'</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">let</span> x = <span class="number">1</span>, y = <span class="string">'a'</span>, z = <span class="string">"zz"</span>;</span><br><span class="line">    <span class="comment">/* x, y are ES6 shorthand property names</span></span><br><span class="line"><span class="comment">     * zz is a ES6 computed property name. the value of the expression in brackets [] </span></span><br><span class="line"><span class="comment">     * is a property name</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">let</span> obj = &#123;x, y, [z]: <span class="number">2</span>&#125;;</span><br><span class="line">    <span class="built_in">console</span>.log(obj);  <span class="comment">// =&gt; Object &#123; x: 1, y: "a", zz: 2 &#125;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// use the spread operator to clone properties</span></span><br><span class="line">    <span class="keyword">let</span> clonedObj = &#123; ...obj &#125;;</span><br><span class="line">    <span class="built_in">console</span>.log(clonedObj);  <span class="comment">// =&gt; Object &#123; x: 1, y: "a", zz: 2 &#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>Using a constructor function</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> <span class="built_in">Object</span>();  <span class="comment">// empty object, call the built-in constructor, same as &#123;&#125;</span></span><br><span class="line"><span class="comment">// could add properties later</span></span><br><span class="line">obj.x = <span class="number">1</span>;</span><br><span class="line">obj.y = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> anotherObj = <span class="keyword">new</span> <span class="built_in">Object</span>(&#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> dateObj = <span class="keyword">new</span> <span class="built_in">Date</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. write a constructor function</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Car</span>(<span class="params">model</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.model = model;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. call the new operator to create an instance</span></span><br><span class="line"><span class="keyword">var</span> myCar = <span class="keyword">new</span> Car(<span class="string">'Tesla'</span>);</span><br></pre></td></tr></table></figure><ul><li>Using the <code>Object.create</code> method</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = <span class="built_in">Object</span>.create(<span class="built_in">Object</span>.prototype);  <span class="comment">// empty object</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> foo = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;</span><br><span class="line"><span class="comment">// foo is the prototype of bar</span></span><br><span class="line"><span class="keyword">var</span> bar = <span class="built_in">Object</span>.create(foo);</span><br><span class="line">bar.x;  <span class="comment">// =&gt; 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// no prototype, empty object, but don't inherit anything, even basic methods, like toString()</span></span><br><span class="line"><span class="keyword">var</span> nullObj = <span class="built_in">Object</span>.create(<span class="literal">null</span>)</span><br></pre></td></tr></table></figure><p>The difference between the <code>new</code> operator and <code>Object.create()</code></p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> foo(); <span class="comment">// call the constructor</span></span><br><span class="line"><span class="built_in">console</span>.log(obj.x);  <span class="comment">// =&gt; 1</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> anotherObj = <span class="built_in">Object</span>.create(foo.prototype);</span><br><span class="line"><span class="built_in">console</span>.log(anotherObj.x);  <span class="comment">// =&gt; undefined</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> otherObj = <span class="built_in">Object</span>.create(foo);</span><br><span class="line"><span class="built_in">console</span>.log(otherObj.x);    <span class="comment">// =&gt; undefined</span></span><br></pre></td></tr></table></figure><h3 id="Accessing-Properties">Accessing Properties</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;   <span class="comment">// empty object</span></span><br><span class="line"><span class="keyword">var</span> obj2 = <span class="keyword">new</span> <span class="built_in">Object</span>(); <span class="comment">// same as obj</span></span><br><span class="line"><span class="keyword">var</span> obj3 = <span class="keyword">new</span> <span class="built_in">Object</span>;   <span class="comment">// same as obj</span></span><br><span class="line"><span class="keyword">var</span> obj4 = &#123;<span class="attr">a</span>: <span class="number">1</span>, <span class="attr">b</span>: <span class="number">2</span>&#125;;</span><br><span class="line">obj.a = <span class="number">1</span>;</span><br><span class="line">obj.b = <span class="number">2</span>;      <span class="comment">// now obj has the same properties as obj4</span></span><br><span class="line"><span class="comment">// nested, simply think a simple object is a dictionary</span></span><br><span class="line"><span class="keyword">var</span> obj5 = &#123;<span class="attr">a</span>: &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;, <span class="string">'b'</span>: <span class="number">2</span>, <span class="number">3</span>: <span class="string">'c'</span>&#125;; </span><br><span class="line">obj5[a];        <span class="comment">// ReferenceError: a is not defined</span></span><br><span class="line">obj5[<span class="string">'a'</span>];      <span class="comment">// =&gt; Object &#123; x: 1, y: 2 &#125;</span></span><br><span class="line">obj5.b;         <span class="comment">// =&gt; 2</span></span><br><span class="line">obj5[<span class="number">3</span>];        <span class="comment">// =&gt; 'c', implicitly convert 3 to '3'</span></span><br><span class="line">obj5[<span class="string">'3'</span>];      <span class="comment">// =&gt; 'c'</span></span><br><span class="line">obj5<span class="number">.3</span>;         <span class="comment">// SyntaxError: unexpected token: numeric literal</span></span><br></pre></td></tr></table></figure><ul><li>use identifier</li></ul><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">expression.identifier</span><br></pre></td></tr></table></figure><p>The property name must be a legal identifier, otherwise use the property name to access, like line 13, 14.</p><p>Identifiers must be typed literally, if the property name is determined at runtime, use name to access it;</p><ul><li>use name</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">expression [ expression ]</span><br></pre></td></tr></table></figure><p>Property names are strings, including the empty string. The value of the second expression is converted to a string as the property name, like line 12, 13.</p><p>Assigning an object to a variable simply assigns the reference to it.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> xo = &#123;<span class="attr">a</span>:<span class="number">1</span>&#125;</span><br><span class="line"><span class="keyword">var</span> yo = &#123;<span class="attr">a</span>:<span class="number">1</span>&#125;</span><br><span class="line"><span class="keyword">var</span> zo = yo</span><br><span class="line">xo == yo  <span class="comment">// =&gt; false</span></span><br><span class="line">zo == yo  <span class="comment">// =&gt; true</span></span><br></pre></td></tr></table></figure><h3 id="Prototype-and-Inheritance">Prototype and Inheritance</h3><p>All constructor functions have a <code>prototype</code> property, the property is a template object for storing properties and methods that is used to build the <code>__proto__</code> property of a new object when creating the object with the <code>new</code> operator, making the new object is an instance of the current object. Other normal objects haven't it unless setting it manually, but this make no sense, normal objects haven't a constructor function to new an instance.</p><p>Any object has the <code>__proto__</code> property, or the object returned by <code>Object.getPrototypeOf()</code>, it is a part of the prototype chain, used to look up properties and methods.</p><p>A fairly common pattern for more object definitions is to define the properties inside the constructor, and the methods on the prototype. if defining a method in the constructor, it is a closure, the methods would get reassigned for every object creation, might have performance issues, more details <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures#Performance_considerations" target="_blank" rel="noopener">here</a>.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">1</span>;  <span class="comment">// constructor functions have statements like this</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'foo constructor'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.y = <span class="number">2</span>;</span><br><span class="line">    <span class="comment">// don't inherit x</span></span><br><span class="line">    <span class="comment">// foo.call(this);</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'bar constructor'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">AnotherBar</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.z = <span class="number">3</span>;</span><br><span class="line">    foo.call(<span class="keyword">this</span>);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'AnotherBar constructor'</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(foo) === <span class="built_in">Function</span>.prototype);  <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(foo) === foo.__proto__);       <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">console</span>.log(foo <span class="keyword">instanceof</span> <span class="built_in">Function</span>);  <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// any object has the __proto__ property, including the prototype property</span></span><br><span class="line"><span class="built_in">console</span>.log(foo.prototype.__proto__ === <span class="built_in">Object</span>.prototype);       <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="comment">// the prototype property already exists, its constructor points to the function foo</span></span><br><span class="line"><span class="built_in">console</span>.log(foo.prototype.constructor === foo)                   <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// foo.prototype is also an object and is not equal to the foo object</span></span><br><span class="line">bar.prototype = <span class="built_in">Object</span>.create(foo.prototype);</span><br><span class="line"><span class="built_in">console</span>.log(bar.__proto__ === <span class="built_in">Function</span>.prototype);  <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">console</span>.log(bar.__proto__ === bar.prototype);       <span class="comment">// =&gt; false</span></span><br><span class="line"><span class="comment">// bar.prototype.constructor();  // =&gt; "foo constructor", not bar(), need to reset</span></span><br><span class="line">bar.prototype.constructor = bar;</span><br><span class="line"></span><br><span class="line">AnotherBar.prototype = <span class="built_in">Object</span>.create(foo.prototype);</span><br><span class="line">AnotherBar.prototype.constructor = AnotherBar;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(bar.prototype === AnotherBar.prototype);  <span class="comment">// =&gt; false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; bar constructor</span></span><br><span class="line"><span class="keyword">var</span> obj = <span class="keyword">new</span> bar;</span><br><span class="line"><span class="comment">/* JavaScript actually does the following</span></span><br><span class="line"><span class="comment"> * var obj = new Object();</span></span><br><span class="line"><span class="comment"> * obj.__proto__ = bar.prototype;</span></span><br><span class="line"><span class="comment"> * bar.call(obj);</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; foo constructor</span></span><br><span class="line"><span class="comment">//    AnotherBar constructor </span></span><br><span class="line"><span class="keyword">var</span> anotherObj = <span class="keyword">new</span> AnotherBar;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; true undefined undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(obj.__proto__ === bar.prototype, obj.x, obj.__proto__.x); </span><br><span class="line"><span class="comment">// =&gt; true 1 undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(anotherObj.__proto__ === AnotherBar.prototype, anotherObj.x, anotherObj.__proto__.x);</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> foo = &#123;</span><br><span class="line">    x: <span class="number">1</span>, </span><br><span class="line">    y: <span class="number">2</span>,</span><br><span class="line">    <span class="keyword">get</span> z() &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>._z;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="keyword">set</span> z(val) &#123;</span><br><span class="line">        <span class="keyword">this</span>._z = val;</span><br><span class="line">        <span class="keyword">this</span>.m = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">var</span> bar = <span class="built_in">Object</span>.create(foo);</span><br><span class="line"></span><br><span class="line">bar.x = <span class="number">2</span>; <span class="comment">// create a new bar.x, not to change foo.x</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'bar.x ='</span>, bar.x);  <span class="comment">// =&gt; bar.x = 2</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'foo.x ='</span>, foo.x);  <span class="comment">// =&gt; foo.x = 1, no change</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'bar.z ='</span>, bar.z);  <span class="comment">// =&gt; bar.z = undefined</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'foo.z ='</span>, foo.z);  <span class="comment">// =&gt; foo.z = undefined</span></span><br><span class="line"><span class="comment">// z has a setter, don't create a new bar.z, call foo.z(val)</span></span><br><span class="line">bar.z = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'bar.z ='</span>, bar.z);  <span class="comment">// =&gt; bar.z = 1</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'foo.z ='</span>, foo.z);  <span class="comment">// =&gt; foo.z = undefined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// m is created in foo.z(val), but it belongs to bar, not foo</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'bar.m ='</span>, bar.m);  <span class="comment">// =&gt; bar.m = 1</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'foo.m ='</span>, foo.m);  <span class="comment">// =&gt; foo.m = undefined</span></span><br><span class="line"></span><br><span class="line">bar.prototype = foo;</span><br><span class="line">bar.prototype.sharedProp = <span class="string">'shared'</span>;</span><br><span class="line"><span class="keyword">var</span> anotherBar = <span class="built_in">Object</span>.create(foo);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'anotherBar.sharedProp ='</span>, anotherBar.sharedProp);  <span class="comment">// =&gt; anotherBar.sharedProp = shared</span></span><br><span class="line"></span><br><span class="line">foo.newProp = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'anotherBar.newProp ='</span>, anotherBar.newProp);  <span class="comment">// =&gt; anotherBar.newProp = 1</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(bar.prototype === foo);                <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">console</span>.log(bar.prototype === bar.__proto__);      <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">console</span>.log(anotherBar.prototype === <span class="literal">undefined</span>);   <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">console</span>.log(anotherBar.__proto__=== foo);          <span class="comment">// =&gt; true</span></span><br></pre></td></tr></table></figure><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/create<br>https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Objects/Object_prototypes<br>https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Objects/Inheritance<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Inheritance_and_the_prototype_chain<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Details_of_the_Object_Model</p><p>About multiple inheritance here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Details_of_the_Object_Model#No_multiple_inheritance<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/create#Classical_inheritance_with_Object.create</p><h3 id="Object-Attributes">Object Attributes</h3><p>Each object has three attributes:</p><ul><li><code>prototype</code>, a reference to the inherited object</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj=&#123;&#125;;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.prototype.isPrototypeOf(obj));  <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> anotherObj = <span class="built_in">Object</span>.create(obj);</span><br><span class="line"><span class="built_in">console</span>.log(obj.isPrototypeOf(anotherObj));        <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">CustomObject</span>(<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line">CustomObject.prototype = obj;</span><br><span class="line"><span class="keyword">var</span> myObj = <span class="keyword">new</span> CustomObject();</span><br><span class="line"><span class="built_in">console</span>.log(obj.isPrototypeOf(myObj));             <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> arr = <span class="keyword">new</span> <span class="built_in">Array</span>();</span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.getPrototypeOf(arr) === <span class="built_in">Array</span>.prototype);  <span class="comment">// =&gt; true</span></span><br></pre></td></tr></table></figure><ul><li>class, a string that categorizes the type of the object</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// no way to set the attribute</span></span><br><span class="line"><span class="comment">// the default toString() is the only way to query it</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.toString.call(<span class="number">1</span>);   <span class="comment">// =&gt; "[object Number]", the second word is the class name</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.toString.call([]);  <span class="comment">// =&gt; "[object Array]"</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.toString.call(<span class="string">''</span>);  <span class="comment">// =&gt; "[object String]"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">CustomConstructor</span>(<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.prototype.toString.call(<span class="keyword">new</span> CustomConstructor());  <span class="comment">// =&gt; "[object Object]"</span></span><br></pre></td></tr></table></figure><ul><li>extensible, whether new properties can be added</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.isExtensible(obj);  <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.preventExtensions(obj);</span><br><span class="line"><span class="built_in">Object</span>.isExtensible(obj);  <span class="comment">// =&gt; false, and can't make it extensible again</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// preventExtensions() only affects the object itself</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.myVar = <span class="string">'inherited'</span>;</span><br><span class="line">obj.myVar;  <span class="comment">// =&gt; "inherited"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// normally be used with the configurable and writable property attributes to</span></span><br><span class="line"><span class="comment">// lock down an object</span></span><br><span class="line"><span class="keyword">var</span> anotherObj = &#123;</span><br><span class="line">    x: <span class="number">1</span>,</span><br><span class="line">    <span class="keyword">get</span> y() &#123; <span class="keyword">return</span> <span class="keyword">this</span>._y &#125;,</span><br><span class="line">    <span class="keyword">set</span> y(val) &#123; <span class="keyword">this</span>._y = val &#125;,</span><br><span class="line">    <span class="keyword">get</span> z() &#123; <span class="keyword">return</span> <span class="keyword">this</span>._z &#125;,</span><br><span class="line">    <span class="keyword">set</span> z(val) &#123; <span class="keyword">this</span>._z = val &#125;,</span><br><span class="line">    internalObj: &#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line">anotherObj.y = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Object &#123; value: 1, writable: true, enumerable: true, configurable: true &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'x'</span>);</span><br><span class="line"><span class="comment">// Object &#123; get: y(), set: y(), enumerable: true, configurable: true &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'y'</span>);</span><br><span class="line"><span class="comment">// Object &#123; get: z(), set: z(), enumerable: true, configurable: true &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'z'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// make the object non-extensible, non-configurable</span></span><br><span class="line"><span class="built_in">Object</span>.seal(anotherObj);</span><br><span class="line"><span class="built_in">Object</span>.isSealed(anotherObj);  <span class="comment">// =&gt; true, and can't unseal it</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Object &#123; value: 1, writable: true, enumerable: true, configurable: false &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'x'</span>);</span><br><span class="line"><span class="comment">// Object &#123; get: y(), set: y(), enumerable: true, configurable: false &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'y'</span>);</span><br><span class="line"><span class="comment">// Object &#123; get: z(), set: z(), enumerable: true, configurable: false &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'z'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// non-extensible</span></span><br><span class="line">anotherObj.newProp = <span class="number">3</span>;</span><br><span class="line">anotherObj.newProp;  <span class="comment">// =&gt; undefined</span></span><br><span class="line">anotherObj.z = <span class="number">3</span>;    <span class="comment">// z was not been assigned before, doesn't exist</span></span><br><span class="line">anotherObj.z;        <span class="comment">// =&gt; undefined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// writable, still can set</span></span><br><span class="line">anotherObj.x = <span class="number">0</span>;</span><br><span class="line">anotherObj.x;  <span class="comment">// =&gt; 0</span></span><br><span class="line">anotherObj.y = <span class="number">0</span>;</span><br><span class="line">anotherObj.y;  <span class="comment">// =&gt; 0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// make the object non-extensible, non-configurable, non-writable</span></span><br><span class="line"><span class="built_in">Object</span>.freeze(anotherObj);</span><br><span class="line"><span class="built_in">Object</span>.isFrozen(anotherObj);  <span class="comment">// =&gt; true, and can't unfreeze it</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Object &#123; value: 0, writable: false, enumerable: true, configurable: false &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'x'</span>);</span><br><span class="line"><span class="comment">// =&gt; Object &#123; get: y(), set: y(), enumerable: true, configurable: false &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'y'</span>);</span><br><span class="line"><span class="comment">// =&gt; Object &#123; get: z(), set: z(), enumerable: true, configurable: false &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(anotherObj, <span class="string">'z'</span>);</span><br><span class="line"></span><br><span class="line">anotherObj.x = <span class="number">1</span>;</span><br><span class="line">anotherObj.x;  <span class="comment">// =&gt; 0, no change</span></span><br><span class="line">anotherObj.y = <span class="number">2</span>;</span><br><span class="line">anotherObj.y;  <span class="comment">// =&gt; 0, no change</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// only affect the object itself</span></span><br><span class="line">anotherObj.internalObj.x = <span class="number">1</span>;</span><br><span class="line">anotherObj.internalObj.x;  <span class="comment">// =&gt; 1</span></span><br><span class="line"><span class="built_in">Object</span>.prototype.myObj = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;;</span><br><span class="line">anotherObj.myObj.x;        <span class="comment">// =&gt; 1</span></span><br></pre></td></tr></table></figure><h3 id="Property-Attributes">Property Attributes</h3><p>Each property has three attributes:</p><ul><li><code>writable</code>. Whether the value can be set;</li><li><code>enumerable</code>. Whether the name is returned by <code>for...in</code> loop. Properties created by normal JavaScript code are enumerable unless you change it explicitly. More details <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Enumerability_and_ownership_of_properties" target="_blank" rel="noopener">here</a>;</li><li><code>configurable</code>. Whether can be deleted or its attributes can be altered;</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123; <span class="attr">x</span>: <span class="number">1</span> &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// An object called property descriptor represents the four attributes</span></span><br><span class="line"><span class="comment">// =&gt; Object &#123; value: 1, writable: true, enumerable: true, configurable: true &#125;</span></span><br><span class="line"><span class="built_in">Object</span>.getOwnPropertyDescriptor(obj, <span class="string">'x'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// make 'x' non-writable</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">"x"</span>, &#123; <span class="attr">writable</span>: <span class="literal">false</span> &#125;);</span><br><span class="line">obj.x = <span class="number">2</span>;                      <span class="comment">// do nothing</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.x ='</span>, obj.x);  <span class="comment">// =&gt; obj.x = 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// but 'x' is still configurable</span></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">"x"</span>, &#123; <span class="attr">value</span>: <span class="number">2</span> &#125;);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.x ='</span>, obj.x);  <span class="comment">// =&gt; obj.x = 2</span></span><br></pre></td></tr></table></figure><h3 id="Testing-Properties">Testing Properties</h3><p>Check whether an object has a property with a given name:</p><ul><li>Directly query the property</li><li><code>in</code> operator</li><li><code>hasOwnProperty()</code>, returns true if the property belongs to the object itself, not the prototype objects</li><li><code>propertyIsEnumerable()</code>, returns true only if <code>hasOwnProperty()</code> returns true and the <code>enumerable</code> attribute is true</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">const</span> obj = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;;</span><br><span class="line">    obj.x === <span class="literal">undefined</span>; <span class="comment">// =&gt; false, directly query, exists</span></span><br><span class="line">    </span><br><span class="line">    obj.z === <span class="literal">undefined</span>; <span class="comment">// =&gt; true, doesn't exist</span></span><br><span class="line">    <span class="string">'z'</span> <span class="keyword">in</span> obj;  <span class="comment">// =&gt; false, doesn't exist</span></span><br><span class="line">    </span><br><span class="line">    obj.y = <span class="literal">undefined</span>;</span><br><span class="line">    obj.y === <span class="literal">undefined</span>; <span class="comment">// =&gt; true, but exists</span></span><br><span class="line">    <span class="comment">// should use in operator to check, not depending on the value "undefined"</span></span><br><span class="line">    <span class="string">'y'</span> <span class="keyword">in</span> obj;  <span class="comment">// =&gt; true, exists</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Enumerating-Properties">Enumerating Properties</h3><ul><li><code>for...in</code>, iterates over all enumerable properties' name, including inherited</li><li><code>Object.getOwnPropertyNames()</code>, return all own property names, including enumerable and non-enumerable</li><li><code>Object.keys()</code>, returns all own enumerable property names</li></ul><h4 id="Get-Non-Enumerable-Properties-Only">Get Non-Enumerable Properties Only</h4><p>Use the <code>Array.prototype.filter()</code> function to remove the result of <code>Object.keys()</code> from <code>Object.getOwnPropertyNames()</code>'s.</p><h3 id="Getters-Setters">Getters/Setters</h3><p>A getter is a method that gets the value of a specific property.</p><p>A setter is a method that sets the value of a specific property.</p><ul><li>defined using object initializers</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    <span class="keyword">get</span> x() &#123; <span class="keyword">return</span> <span class="number">1</span> &#125;,</span><br><span class="line">    <span class="keyword">set</span> y(val) &#123; <span class="keyword">this</span>._y = val &#125;,</span><br><span class="line">    <span class="keyword">get</span> z() &#123; <span class="keyword">return</span> <span class="keyword">this</span>._z &#125;,</span><br><span class="line">    <span class="keyword">set</span> z(val) &#123; <span class="keyword">this</span>._z = val &#125;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// no setter, read-only</span></span><br><span class="line">obj.x = <span class="number">0</span>;                      <span class="comment">// do nothing</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.x ='</span>, obj.x);  <span class="comment">// =&gt; obj.x = 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// no getter, write-only</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.y ='</span>, obj.y);  <span class="comment">// =&gt; obj.y = undefined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// read/write</span></span><br><span class="line">obj.z = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.z ='</span>, obj.z);  <span class="comment">// =&gt; obj.z = 0</span></span><br></pre></td></tr></table></figure><ul><li>defined using <code>Object.defineProperty()</code> or <code>Object.defineProperties()</code></li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.defineProperty(obj, <span class="string">'x'</span>, &#123;</span><br><span class="line">    <span class="keyword">get</span>: function() &#123; <span class="keyword">return</span> <span class="number">1</span> &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>.defineProperties(obj, &#123;</span><br><span class="line">    <span class="string">'y'</span>: &#123;</span><br><span class="line">        <span class="keyword">set</span>: function(val) &#123; <span class="keyword">this</span>._y = val &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">'z'</span>: &#123;</span><br><span class="line">        <span class="keyword">get</span>: function() &#123; <span class="keyword">return</span> <span class="keyword">this</span>._z &#125;,</span><br><span class="line">        <span class="keyword">set</span>: function(val) &#123; <span class="keyword">this</span>._z = val &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// no setter, read-only</span></span><br><span class="line">obj.x = <span class="number">0</span>;                      <span class="comment">// do nothing</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.x ='</span>, obj.x);  <span class="comment">// =&gt; obj.x = 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// no getter, write-only</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.y ='</span>, obj.y);  <span class="comment">// =&gt; obj.y = undefined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// read/write</span></span><br><span class="line">obj.z = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'obj.z ='</span>, obj.z);  <span class="comment">// =&gt; obj.z = 0</span></span><br></pre></td></tr></table></figure><h2 id="Arrays">Arrays</h2><ul><li>Untyped: an element may be of any type</li><li>Dynamic: no need to declare a fixed size, grow or shrink as needed</li><li>Sparse: no need to have contiguous indexes, may have gaps, length is larger than the number of indexes</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [];            <span class="comment">// empty arrary</span></span><br><span class="line"><span class="keyword">var</span> arr2 = [<span class="number">1</span>, <span class="number">1</span> + <span class="number">1</span>, <span class="string">'3'</span>, &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;, [<span class="string">'a'</span>, <span class="string">'b'</span>]];</span><br><span class="line"><span class="keyword">var</span> arr3 = [<span class="number">1</span>,  , <span class="number">3</span>];    <span class="comment">// the second element is undefined, sparse array</span></span><br><span class="line"><span class="keyword">var</span> arr4 = [<span class="number">1</span>,  , <span class="number">3</span>, ];  <span class="comment">// same as arr3</span></span><br><span class="line"></span><br><span class="line">arr2[<span class="number">0</span>];                 <span class="comment">// =&gt; 1, use index 0 to access, JS converts the number 0 to a string "0"</span></span><br><span class="line">arr2[<span class="string">'0'</span>];               <span class="comment">// =&gt; 1, use the property name '0' to access, an array is an object</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> arr5 = <span class="keyword">new</span> <span class="built_in">Array</span>();  <span class="comment">// same as []</span></span><br><span class="line"><span class="keyword">var</span> arr6 = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">5</span>); <span class="comment">// create 5 empty slots, sparse array</span></span><br><span class="line"><span class="comment">// =&gt; Array(5) [ undefined, undefined, undefined, undefined, undefined ]</span></span><br><span class="line">arr6;</span><br><span class="line"></span><br><span class="line"><span class="comment">// all indexes are property names, but only property names that </span></span><br><span class="line"><span class="comment">// are non-negative intergers are indexes</span></span><br><span class="line">arr6[<span class="string">'6'</span>] = <span class="number">6</span>;  <span class="comment">// same as arr6[6]</span></span><br><span class="line"><span class="comment">// if give a string that can be converted to a big number, will create large number of </span></span><br><span class="line"><span class="comment">// empty slots</span></span><br><span class="line"><span class="comment">// =&gt; Array(7) [ undefined, undefined, undefined, undefined, undefined, undefined, 6 ]</span></span><br><span class="line">arr6;</span><br><span class="line">arr6.length;    <span class="comment">// =&gt; 7, grew the array automatically</span></span><br><span class="line"></span><br><span class="line">arr6[<span class="number">6.0</span>];      <span class="comment">// =&gt; 6, floating-point, same as arr6[6]</span></span><br><span class="line"></span><br><span class="line">arr6[<span class="number">-1</span>] = <span class="number">-1</span>;  <span class="comment">// convert -1 to '-1', is property name</span></span><br><span class="line"><span class="comment">// =&gt; Array(7) [ undefined, undefined, undefined, undefined, undefined, undefined, 6 ]</span></span><br><span class="line">arr6;</span><br><span class="line">arr6.length;    <span class="comment">// =&gt; 7</span></span><br><span class="line">arr6[<span class="string">'-1'</span>];     <span class="comment">// =&gt; -1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Array [ "0", "2" ]</span></span><br><span class="line"><span class="built_in">Object</span>.keys(arr3);</span><br><span class="line">arr3.length;    <span class="comment">// =&gt; 3, sparse array, the length isn't equal to the number of indexes</span></span><br><span class="line"><span class="number">1</span> <span class="keyword">in</span> arr3;      <span class="comment">// =&gt; false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Array [ "6" ]</span></span><br><span class="line"><span class="built_in">Object</span>.keys(arr6);</span><br><span class="line"><span class="number">0</span> <span class="keyword">in</span> arr6;      <span class="comment">// =&gt; false</span></span><br><span class="line">arr6[<span class="number">0</span>];        <span class="comment">// =&gt; undefined, empty slots' value are undefined, but don't exist</span></span><br><span class="line">arr6[<span class="number">0</span>] = <span class="literal">undefined</span>;</span><br><span class="line"><span class="number">0</span> <span class="keyword">in</span> arr6;      <span class="comment">// =&gt; true, don't use 'undefined' to determine whether an element exists</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Array(3) [ 1, undefined, 3 ]</span></span><br><span class="line">arr3;</span><br><span class="line">arr3.length = <span class="number">5</span>;  <span class="comment">// growing the array, to add new empty slots</span></span><br><span class="line"><span class="comment">// =&gt; Array(5) [ 1, undefined, 3, undefined, undefined ]</span></span><br><span class="line">arr3;</span><br><span class="line">arr3.length = <span class="number">4</span>;  <span class="comment">// shortening the array</span></span><br><span class="line"><span class="comment">// =&gt; Array(4) [ 1, undefined, 3, undefined ]</span></span><br><span class="line">arr3;</span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> arr2[<span class="number">0</span>];</span><br><span class="line"><span class="comment">// =&gt; Array(5) [ undefined, 2, "3", &#123;…&#125;, (2) […] ]</span></span><br><span class="line">arr2;  <span class="comment">// don't change the length, arr2 is a sparse array now</span></span><br><span class="line">arr2.shift();</span><br><span class="line"><span class="comment">// =&gt; Array(4) [ 2, "3", &#123;…&#125;, (2) […] ]</span></span><br><span class="line">arr2;  <span class="comment">// changed the length, arr2 becomes dense again</span></span><br></pre></td></tr></table></figure><h3 id="Iterating-Arrays">Iterating Arrays</h3><ul><li><code>for</code></li><li><code>Array.prototype.forEach()</code></li><li><code>for...of</code></li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>,  , <span class="string">'3'</span>, , <span class="number">5</span>];</span><br><span class="line"></span><br><span class="line">arr.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">val, idx</span>) </span>&#123;</span><br><span class="line">    <span class="comment">/* =&gt;</span></span><br><span class="line"><span class="comment">    index: 0 value: 1</span></span><br><span class="line"><span class="comment">    index: 2 value: 3</span></span><br><span class="line"><span class="comment">    index: 4 value: 5</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'index:'</span>,idx, <span class="string">'value:'</span>,val);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (! (i <span class="keyword">in</span> arr)) <span class="keyword">continue</span>;  <span class="comment">// skip non-existence elements</span></span><br><span class="line">    <span class="comment">// =&gt; , same as forEach</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'index:'</span>,i, <span class="string">'value:'</span>, arr[i]);</span><br><span class="line">&#125;</span><br><span class="line">     </span><br><span class="line"><span class="comment">// can't distinguish an element is non-existence or its value is undefined </span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">const</span> val <span class="keyword">of</span> arr) &#123;</span><br><span class="line">    <span class="comment">/* =&gt;</span></span><br><span class="line"><span class="comment">    value: 1</span></span><br><span class="line"><span class="comment">    value: undefined</span></span><br><span class="line"><span class="comment">    value: 3</span></span><br><span class="line"><span class="comment">    value: undefined</span></span><br><span class="line"><span class="comment">    value: 5</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'value:'</span>, val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Functions">Functions</h2><p>Function definitions can be nested within other functions, can access any variables that are in scope where they are defined, so JS functions are closures. Functions are first-class objects in JS.</p><h3 id="Defining-Functions">Defining Functions</h3><h4 id="Function-Description">Function Description</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"> <span class="regexp">/* zero or more parameters, comma-separated */</span> </span>) </span>&#123; <span class="comment">/* some ource code */</span> &#125;</span><br></pre></td></tr></table></figure><h4 id="Function-Expression">Function Expression</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Function expressions are not visible everywhere with the current scope, </span></span><br><span class="line"><span class="comment">// don't like variable and function description</span></span><br><span class="line">fnVar();       <span class="comment">// =&gt; TypeError: fnVar is not a function</span></span><br><span class="line">namedFnVar();  <span class="comment">// =&gt; TypeError: namedFnVar is not a function</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. anonymous</span></span><br><span class="line"><span class="keyword">var</span> fnVar = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123; <span class="built_in">console</span>.log(<span class="string">'An anonymous function expression'</span>) &#125;;</span><br><span class="line">fnVar();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. named</span></span><br><span class="line"><span class="comment">// easily find the origin of an error from the call stack</span></span><br><span class="line"><span class="keyword">var</span> namedFnVar = <span class="function"><span class="keyword">function</span> <span class="title">namedFn</span>(<span class="params"></span>)</span>&#123; <span class="built_in">console</span>.log(<span class="string">'A named function expression'</span>) &#125;;</span><br><span class="line">namedFnVar();</span><br><span class="line"></span><br><span class="line">namedFn(); <span class="comment">// =&gt; ReferenceError: namedFn is not defined</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. one-time</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params">msg</span>) </span>&#123; </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'A one-time function expression, msg:'</span>, msg) </span><br><span class="line">&#125;)(<span class="string">'Oh'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4. arrow</span></span><br><span class="line"><span class="keyword">var</span> arrowFn = <span class="function"><span class="params">()</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'An arrow function expression'</span>);</span><br><span class="line">arrowFn();</span><br></pre></td></tr></table></figure><h4 id="Function-Constructor">Function Constructor</h4><ul><li>Create functions dynamically;</li><li>Execute in the global scope only, can't access local variables;</li><li>The function body is string, can't optimize, it parses the function body each time it is called;</li><li>Similar security and performance issues to eval</li></ul><p>It is not recommended to use normally.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> consFn = <span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">'msg'</span> <span class="comment">/* argument names must be strings and valid identifiers */</span>, </span><br><span class="line">    <span class="string">'console.log("A function created by a function constructor, msg:", msg)'</span>);</span><br><span class="line">consFn(<span class="string">'Oh'</span>);</span><br></pre></td></tr></table></figure><h3 id="Function-Scope">Function Scope</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">fn();        <span class="comment">// =&gt; hi</span></span><br><span class="line">fnVar();     <span class="comment">// =&gt; TypeError: fnVar is not a function</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>)</span>&#123;<span class="built_in">console</span>.log(<span class="string">'hi'</span>)&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fnVar = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;<span class="built_in">console</span>.log(<span class="string">'hello'</span>)&#125;;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">var</span> anotherFnVar = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;<span class="built_in">console</span>.log(<span class="string">'hey'</span>)&#125;;</span><br><span class="line">anotherFnVar();  <span class="comment">// =&gt; hey</span></span><br></pre></td></tr></table></figure><h3 id="Binding-Functions">Binding Functions</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>.<span class="title">bind</span>(<span class="params">thisArg[, arg1[, arg2[, ...]]]</span>)</span></span><br></pre></td></tr></table></figure><p>Bind a function to an object and return a new function with the specified this value and initial arguments.</p><p>It is not recommended to use <code>new</code> operator to construct new instances of bound functions.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> globalObject = <span class="keyword">this</span>;</span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    x: <span class="number">1</span>,</span><br><span class="line">    getX: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(<span class="keyword">this</span>.x) &#125;,</span><br><span class="line">    add: <span class="function"><span class="keyword">function</span>(<span class="params">x, y</span>) </span>&#123; <span class="built_in">console</span>.log(x + y, <span class="keyword">this</span> === globalObject) &#125;</span><br><span class="line">&#125;;   </span><br><span class="line"></span><br><span class="line">obj.getX();   <span class="comment">// =&gt; 1</span></span><br><span class="line"><span class="keyword">var</span> savedGetX = obj.getX;</span><br><span class="line">savedGetX();  <span class="comment">// =&gt; undefined, called as a function, not a method, "this" is the global object</span></span><br><span class="line"><span class="keyword">var</span> boundGetX = savedGetX.bind(obj);</span><br><span class="line">boundGetX();  <span class="comment">// =&gt; 1, "this" is obj</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// with initial arguments</span></span><br><span class="line"><span class="keyword">var</span> boundAdd = obj.add.bind(<span class="literal">null</span>, <span class="number">10</span>);  <span class="comment">// "this" is null, use the "this" of current scope</span></span><br><span class="line">boundAdd(<span class="number">1</span>);     <span class="comment">// =&gt; 11 true</span></span><br><span class="line">boundAdd(<span class="number">1</span>, <span class="number">2</span>);  <span class="comment">// =&gt; 11 true, 2 is ignored </span></span><br><span class="line"></span><br><span class="line"><span class="comment">// create a shortcut to a function, and could specify a special this value</span></span><br><span class="line"><span class="keyword">var</span> add = <span class="built_in">Function</span>.prototype.call.bind(obj.add);</span><br><span class="line">add(<span class="literal">null</span>, <span class="number">1</span>, <span class="number">2</span>); <span class="comment">// =&gt; 3 true, this "this" is not specical</span></span><br></pre></td></tr></table></figure><h3 id="Determining-Whether-a-Function-Exists">Determining Whether a Function Exists</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">'function'</span> === <span class="keyword">typeof</span> <span class="keyword">this</span>.fn</span><br></pre></td></tr></table></figure><h3 id="Nested-Functions">Nested Functions</h3><p>Function description can appear in global code or within other functions. It behave strangely within block code(<code>if</code>, <code>for</code>, ...) in non-strict mode, can't ensure it would be defined, but in strict mode, it is defined and its scope is the block.</p><p>Function expression and function constructor haven't so restriction.</p><p>Nested functions don't inherit the <code>this</code> value from its caller, if it is invoked as a method, <code>this</code> is the object, if as a function, <code>this</code> is the global object or undefined (strict mode)</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> globalObject = <span class="keyword">this</span>;</span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    fn: <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">var</span> savedThis = <span class="keyword">this</span>;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="keyword">this</span> === obj);  <span class="comment">// =&gt; true</span></span><br><span class="line">        (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; </span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span> === globalObject, savedThis === obj)  <span class="comment">// =&gt; true true</span></span><br><span class="line">        &#125;)();</span><br><span class="line">        </span><br><span class="line">        fn.nestedFn = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="comment">// =&gt; true false true</span></span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span> === fn, <span class="keyword">this</span> === globalObject, savedThis === obj)</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">            <span class="comment">// =&gt; true false true</span></span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span> === anotherObj, <span class="keyword">this</span> === globalObject, savedThis === obj)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> anotherObj = &#123;&#125;;</span><br><span class="line">anotherObj.fn = obj.fn();</span><br><span class="line">fn.nestedFn();</span><br><span class="line">anotherObj.fn();</span><br></pre></td></tr></table></figure><h3 id="Invoking-Functions-Indirectly">Invoking Functions Indirectly</h3><ul><li><code>Function.prototype.call()</code></li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>.<span class="title">call</span>(<span class="params">thisArg, arg1, arg2, ...</span>)</span></span><br></pre></td></tr></table></figure><p>All arguments are optional.<br>In non-strict mode, <code>thisArg</code> is <code>null</code> or <code>undefined</code>, which will be replaced with the global object, primitive values will be converted to wrapper objects.</p><p>Using <code>call()</code> to chain constructors for an object, like C++'s subclass call superclass's constructor<br>during initializing the class.</p><p>Using <code>call()</code> to invoke an anonymous function for multiple objects.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/call</p><ul><li><code>Function.prototype.apply()</code></li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>.<span class="title">apply</span>(<span class="params">thisArg, [argsArray]</span>)</span></span><br></pre></td></tr></table></figure><p><code>thisArg</code> is same as <code>call()</code>.</p><p><code>argsArray</code> is optional, could be <code>null</code> or <code>undefined</code> if the function hasn't arguments.</p><p>Using <code>apply()</code> to chain constructors for an object.</p><p>Using <code>apply()</code> and built-in functions cleverly.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> array = [<span class="string">'a'</span>, <span class="string">'b'</span>];</span><br><span class="line"><span class="keyword">var</span> anotherArray = [<span class="string">'c'</span>, <span class="string">'d'</span>]</span><br><span class="line"><span class="keyword">var</span> elements = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line"><span class="comment">// Array.prototype.push(element1[, ...[, elementN]])</span></span><br><span class="line">anotherArray.push(elements);</span><br><span class="line">array.push.apply(array, elements);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(array);         <span class="comment">// =&gt; ["a", "b", 0, 1, 2]</span></span><br><span class="line"><span class="built_in">console</span>.log(anotherArray);  <span class="comment">// =&gt; ["a", "b", [0, 1, 2]]</span></span><br></pre></td></tr></table></figure><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/apply</p><p>The difference of the two methods is only that one accepts an argument list, one accepts an argument array or array-like object.</p><p>Both methods accept an explicit this value, so could invoke any function as a method of any object, writing a method once and then inherit it in any object.</p><h3 id="Closures">Closures</h3><p>closure = function + (its) variable scope.</p><p>The variable scope includes local(own) scope, outer funtion's scope, global scope.</p><p>The variable scope is determined when the function is defined, not runtime, the function could be invoked in another scope.</p><p>A closure gives you access to an outer function’s scope from an inner function.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fn = (<span class="function"><span class="keyword">function</span> <span class="title">anonFn</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> counter = <span class="number">0</span>;</span><br><span class="line">    ++counter;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">anotherAnonFn</span>(<span class="params"></span>) </span>&#123; <span class="built_in">console</span>.log(counter) &#125;;</span><br><span class="line">&#125;)();</span><br><span class="line"></span><br><span class="line"><span class="comment">/* The counter variable is in the scope when defining anotherAnonFn(), but its value </span></span><br><span class="line"><span class="comment"> * is determined when invoking fn(), so don't create closures in a loop, they share the final value of </span></span><br><span class="line"><span class="comment"> * a variable</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">fn();  <span class="comment">// =&gt; 1</span></span><br></pre></td></tr></table></figure><p>It is common using closures to create private environments which include private methods and variables, no other methods can change these private variables.</p><p>JS doesn't like C++, uses function call stack: local variables would dispear when the function returned. Have an external reference to <code>anotherAnonFn()</code> when <code>anonFn()</code> returned, the object <code>anonFn</code> won't be garbage collected.</p><p>It is unwise to unnecessarily create functions within other functions if closures are not needed for a particular task, because of performance, more details <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures#Performance_considerations" target="_blank" rel="noopener">here</a>.</p><p>A closure has its own <code>this</code> and <code>arguemnts</code>, if need outer function's, save them to variables.</p><h3 id="Funtion-Parameters">Funtion Parameters</h3><ul><li>rest parameters</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// moreArgs is always an array</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">...moreArgs</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// =&gt; true [ 1, 2, 3 ]</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">Array</span>.isArray(moreArgs), moreArgs);</span><br><span class="line">&#125;</span><br><span class="line">fn(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure><ul><li>default parameters</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">a, b = <span class="number">1</span></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(a, b);  <span class="comment">// =&gt; undefined 1</span></span><br><span class="line">&#125;</span><br><span class="line">fn();</span><br></pre></td></tr></table></figure><ul><li>destructured parameters</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// destructuring rest parameters</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">...[a, b, c]</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(a, b, c);  <span class="comment">// =&gt; 1 2 3</span></span><br><span class="line">&#125;</span><br><span class="line">fn(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure><h3 id="The-arguments-Object">The arguments Object</h3><p>Could refer to a function's arguments within the function by using the <code>arguments</code> object, arrow functions haven't it.</p><p><code>arguments</code> is an array-like object, has a <code>length</code> property. if want a true argument array, use rest parameters.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="comment">// =&gt; 3 Arguments &#123; 0: 1, 1: 2, 2: 3, … &#125;</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length, <span class="built_in">arguments</span>);</span><br><span class="line">&#125;</span><br><span class="line">fn(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">anotherFn</span>(<span class="params">a</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(a);   <span class="comment">// =&gt; 1</span></span><br><span class="line">    <span class="built_in">arguments</span>[<span class="number">0</span>] = <span class="number">2</span>; <span class="comment">// sync with a</span></span><br><span class="line">    <span class="built_in">console</span>.log(a);   <span class="comment">// =&gt; 2</span></span><br><span class="line">&#125;</span><br><span class="line">anotherFn(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// In non-strict mode, function contains rest, default, or destructured parameters, the arguments </span></span><br><span class="line"><span class="comment">// object doesn't sync with them</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">restParaFn</span>(<span class="params">...moreArgs</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// =&gt; Arguments &#123; 0: 1, 1: 2, 2: 3, … &#125; Array(3) [ 1, 2, 3 ]</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>, moreArgs);</span><br><span class="line">    moreArgs[<span class="number">0</span>] = <span class="number">0</span>; <span class="comment">// doesn't sync with the arguments object</span></span><br><span class="line">    <span class="comment">// =&gt; Arguments &#123; 0: 1, 1: 2, 2: 3, … &#125; Array(3) [ 0, 2, 3 ]</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>, moreArgs);</span><br><span class="line">&#125;</span><br><span class="line">restParaFn(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure><h3 id="Generator-Functions">Generator Functions</h3><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Iterators_and_Generators</p><h3 id="Arrow-Functions">Arrow Functions</h3><p>Shorter functions and non-binding of this</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions</p><h2 id="Classes">Classes</h2><p>JavaScript classes, introduced in ECMAScript 2015, are primarily syntactical sugar, they are in fact <em>special functions</em>.</p><h3 id="Class-Declarations">Class Declarations</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>() &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bar</span> <span class="keyword">extends</span> <span class="title">Array</span> </span>&#123;</span><br><span class="line">    <span class="keyword">constructor</span>(args) &#123;</span><br><span class="line">        <span class="keyword">super</span>(args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> Animal = &#123;</span><br><span class="line">    speak () &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dog</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Declared public or private fields are always present. This is not necessary.</span></span><br><span class="line">    <span class="comment">// public fields</span></span><br><span class="line">    dogName = <span class="string">''</span>;</span><br><span class="line">    breed;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/* private fields, no support yet</span></span><br><span class="line"><span class="comment">     * #color = 'brown';</span></span><br><span class="line"><span class="comment">     * #height;</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// static dogName = '';  // =&gt; SyntaxError: bad method definition</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// static and prototype data properties must be defined outside the class body</span></span><br><span class="line">Dog.dangerous = <span class="literal">false</span>;</span><br><span class="line">Dog.prototype.cuteRating = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// to inherit from a non-constructible object</span></span><br><span class="line"><span class="built_in">Object</span>.setPrototypeOf(Dog.prototype, Animal);</span><br></pre></td></tr></table></figure><p>The constructor could be ignored. If the class is a base class, the default constructor is empty, like <code>Foo</code>. If the class is derived class, the default constructor calls the parent constructor, passing along any arguments that were provided, like <code>Bar</code>.</p><h3 id="Class-Expressions">Class Expressions</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// anonymous class</span></span><br><span class="line"><span class="keyword">let</span> Foo = <span class="class"><span class="keyword">class</span> </span>&#123;&#125;;</span><br><span class="line"><span class="keyword">let</span> AnotherFoo = <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">Array</span> </span>&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// named class</span></span><br><span class="line"><span class="keyword">let</span> Bar = <span class="class"><span class="keyword">class</span> <span class="title">BarClass</span> </span>&#123;&#125;;</span><br></pre></td></tr></table></figure><h3 id="Syntactical-Sugar">Syntactical Sugar</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.x = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">this</span>.y = <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line">foo.prototype.say = <span class="function"><span class="params">()</span> =&gt;</span> &#123;&#125;;</span><br><span class="line">    </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    foo.call(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line">bar.prototype = <span class="built_in">Object</span>.create(foo.prototype);</span><br><span class="line">bar.prototype.constructor = bar;</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">foo</span> </span>&#123;</span><br><span class="line">    x = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">constructor</span>() &#123;</span><br><span class="line">        <span class="keyword">this</span>.y = <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    say() &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">bar</span> <span class="keyword">extends</span> <span class="title">foo</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>They are same. The method <code>say</code> belongs to <code>foo.prototype</code>.</p><h3 id="Notes">Notes</h3><ul><li><p>Class body is executed in <strong>strict mode</strong>;</p></li><li><p>Must first declare the class then access it, including class declarations and class expressions;</p></li><li><p>static methods</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> distance(a, b) &#123;</span><br><span class="line">        <span class="comment">// calculate distance</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">static</span> nothing() &#123;</span><br><span class="line">        <span class="comment">/* "this" is legal in static methods.</span></span><br><span class="line"><span class="comment">         * typeof Point.nothing() =&gt; "function"</span></span><br><span class="line"><span class="comment">         * Point.nothing() =&gt; function Point()</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">    &#125;    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// call the static method, can't call it via the instances</span></span><br><span class="line">Point.distance(<span class="number">5</span>, <span class="number">5</span>);</span><br></pre></td></tr></table></figure><p>If assigning a static or non-static method to a variable, then call it, the value of <code>this</code> is always <code>undefined</code>.</p></li></ul><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/constructor</p><h2 id="Operators">Operators</h2><h3 id="Operators-and-Precedence">Operators and Precedence</h3><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence</p><h3 id="Bitwise-Operators">Bitwise Operators</h3><p><code>&amp;</code>, <code>|</code>, <code>^</code>, <code>~</code>, <code>&lt;&lt;</code>, <code>&gt;&gt;</code>, <code>&gt;&gt;&gt;</code></p><p>The bitwise operators expect 32-bit integer operands, and drop any fractional part and any bits beyond the 32nd.</p><p><code>NaN</code>, <code>Infinity</code>, and <code>-Infinity</code> all convert to <code>0</code>.</p><h3 id="Comparison-Operators">Comparison Operators</h3><p><code>==</code>, <code>===</code>, <code>!=</code>, <code>!==</code></p><p><code>===</code>, <code>!==</code> is the strict version of <code>==</code>, <code>!=</code>, they check the type of operands first, then compare values:</p><ul><li>different types, not equal</li><li>one or both values is <code>NaN</code>, is not equal</li><li><code>0 === -0</code> is true</li><li>strings are Unicode (UTF-16) strings, two equal strings must have a same length and same UTF-16 bit sequences</li><li>objects are compared by reference, two equal objects must refer to a same object</li></ul><p><code>==</code>, <code>!=</code> perform some type conversions, then compare, the rules:</p><ul><li>if two values have the same type, do same things like <code>===</code>, <code>!==</code>, if not, check the following rules;</li><li><code>null == undefined</code> is true</li><li>one is number, one is string, convert the string to a number, then compare</li><li>one of values is boolean, convert the boolean to a number, <code>true</code> to <code>1</code>, <code>false</code> to <code>0</code>, then compare</li><li>one of values is object, perform object-to-primitive conversions(favor number, then string), then compare</li><li>any other combinations are not equal</li></ul><p><code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code></p><p>These operators favor numbers and only perform string comparison if both operands are strings, don't like <code>+</code>(favors strings).</p><p>The rules:</p><ul><li>one of values is object, perform object-to-primitive conversions first;</li><li>both are strings, compare as strings;</li><li>one of values is not string, converts all of them to numbers, then compare</li><li><code>Infinity</code> is larger any number, <code>-Infinity</code> is the opposite</li><li>either of operand is <code>NaN</code>, the result is false</li></ul><h3 id="in-Operator">in Operator</h3><p>The left-side operand is a string, the right-side operand is an object. It is <code>true</code> if the left-side value is a property name of the object.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = [ <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span> ];</span><br><span class="line"><span class="number">0</span> <span class="keyword">in</span> x;    <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="string">'0'</span> <span class="keyword">in</span> x;  <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="number">5</span> <span class="keyword">in</span> x;    <span class="comment">// =&gt; false</span></span><br></pre></td></tr></table></figure><h3 id="instanceof-Operator">instanceof Operator</h3><p>The left-side operand is an object, the right-side operand is the constructor function to initialize the object.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = [ <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span> ];</span><br><span class="line">x <span class="keyword">instanceof</span> <span class="built_in">Array</span>;    <span class="comment">// =&gt; true</span></span><br><span class="line">x <span class="keyword">instanceof</span> <span class="built_in">Object</span>;   <span class="comment">// =&gt; true, all objects are instances of Object</span></span><br></pre></td></tr></table></figure><h3 id="Logical-Operators">Logical Operators</h3><p><code>&amp;&amp;</code>, <code>||</code>, <code>~</code></p><p><code>~</code> operator converts its operand to a boolean value first, but <code>&amp;&amp;</code> and <code>||</code> don't</p><p>If the left-side operand can determine the result, <code>&amp;&amp;</code> and <code>||</code> don't evaluate the right-side operand.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">! <span class="number">1</span>              <span class="comment">// =&gt; false</span></span><br><span class="line">! <span class="literal">null</span>           <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="string">'a'</span> &amp;&amp; <span class="number">1</span>         <span class="comment">// =&gt; 1</span></span><br><span class="line"><span class="string">'a'</span> &amp;&amp; <span class="literal">true</span>      <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="literal">null</span>.prop        <span class="comment">// =&gt; TypeError: null has no properties</span></span><br><span class="line"><span class="number">0</span> &amp;&amp; <span class="literal">null</span>.prop   <span class="comment">// =&gt; 0, no TypeError, don't evaluate null.prop</span></span><br><span class="line"><span class="number">1</span> || <span class="literal">null</span>.prop   <span class="comment">// =&gt; 1</span></span><br><span class="line"><span class="number">0</span> || <span class="literal">null</span>.prop   <span class="comment">// =&gt; TypeError: null has no properties</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> y;</span><br><span class="line"><span class="keyword">var</span> x = y || <span class="number">1</span>;   <span class="comment">// =&gt; 1, if y defined, assign it to x, otherwise, set x a default value 1</span></span><br></pre></td></tr></table></figure><h3 id="eval">eval()</h3><p>It is a global function which interpret the string as JS source code, return the value of the last expression or statement or <code>undefined</code> if no return value like in a function.</p><p>If calling <code>eval()</code> within a function, it uses the environment of the caller, can do anything like the caller within the function.</p><p>If calling <code>eval()</code> outside functions, it use the global environment.</p><p>The source code in <code>eval()</code> is unanalyzable, could assign <code>eval</code> to a variable, can't safely optimize functions that call <code>eval()</code>.</p><p>If assigning <code>eval</code> to a variable within a function, it is a indirect call to <code>eval</code>, <code>eval</code> uses the global environment, not the local environment, can't read, write, define local variables or functions.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">var</span> y = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'global x:'</span>,<span class="built_in">eval</span>(x));                       <span class="comment">// =&gt; global x: 1</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testEval</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> y = <span class="number">1</span>; <span class="comment">// hide the global one                                                                                                                                                                                                         </span></span><br><span class="line">    <span class="keyword">var</span> z = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">eval</span>(<span class="string">'z += 1; k = 1'</span>);  <span class="comment">// also create a new global variable k</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'local z, should be 2:'</span>, z);  <span class="comment">// =&gt; local z, should be 2: 2</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// indirectly call eval, use the global environment</span></span><br><span class="line">    <span class="keyword">var</span> aliasOfEval = <span class="built_in">eval</span>;</span><br><span class="line">    aliasOfEval(<span class="string">'y += 1'</span>);</span><br><span class="line">    <span class="comment">// aliasOfEval('z += 1');                     // =&gt; ReferenceError: z is not defined</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'local y, should be 1:'</span>, y);      <span class="comment">// =&gt; local y, should be 1: 1</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// eval is redefined, hide the global function eval()</span></span><br><span class="line">    <span class="comment">// =&gt; TypeError: eval is not a function, occured at line 10</span></span><br><span class="line">    <span class="comment">// var eval = 1;</span></span><br><span class="line">&#125;</span><br><span class="line">testEval();</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'global y, should be 2:'</span>, y);       <span class="comment">// =&gt; global y, should be 2: 2</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'new global var, k:'</span>, k);           <span class="comment">// =&gt; new global var, k: 1</span></span><br></pre></td></tr></table></figure><h3 id="Conditional-Operator">Conditional Operator (?:)</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">condition ? exprIfTrue : exprIfFalse</span><br></pre></td></tr></table></figure><p>The operands may be of any type, if condition is truthy, evaluate the <code>exprIfTrue</code> expression and its value is returned, otherwise is the <code>exprIfFalse</code> expression</p><p>This operator is frequently used as a shortcut for the <code>if</code> statement.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x;</span><br><span class="line"><span class="keyword">var</span> y = x ? x : <span class="string">'default'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> z;</span><br><span class="line"><span class="keyword">if</span>(x) &#123;</span><br><span class="line">    z = x;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    z = <span class="string">'default'</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="delete-Operator">delete Operator</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> object.property</span><br><span class="line"><span class="keyword">delete</span> object[<span class="string">'property'</span>]</span><br></pre></td></tr></table></figure><p>The return value is <code>true</code> or <code>false</code>.</p><p><code>delete</code> operator removes a property from an object, but:</p><ul><li>can't delete any properties declared by <code>var</code>, <code>let</code>, <code>const</code></li><li>can't delete function declaration</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">1</span>&#125;;</span><br><span class="line"><span class="keyword">delete</span> obj.x;  <span class="comment">// =&gt; true</span></span><br><span class="line">x <span class="keyword">in</span> obj;      <span class="comment">// =&gt; false</span></span><br><span class="line"><span class="keyword">delete</span> obj;    <span class="comment">// =&gt; false, declared by var</span></span><br><span class="line"></span><br><span class="line">obj2 = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">1</span>&#125;;</span><br><span class="line"><span class="keyword">delete</span> obj2;   <span class="comment">// true, obj2 is a property of the global object 'this'</span></span><br><span class="line">obj2;          <span class="comment">// =&gt; ReferenceError: obj2 is not defined</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">delete</span> arr[<span class="number">0</span>];      <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="keyword">delete</span> arr[<span class="string">'1'</span>];    <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="comment">// the size of arr is not changed, is still 5</span></span><br><span class="line">arr;                <span class="comment">// =&gt; Array(5) [ undefined, undefined, 3, 4, 5 ]</span></span><br><span class="line"><span class="number">0</span> <span class="keyword">in</span> arr;           <span class="comment">// =&gt; false</span></span><br><span class="line">arr[<span class="number">0</span>];             <span class="comment">// =&gt; undefined</span></span><br><span class="line"></span><br><span class="line">arr[<span class="number">2</span>]=<span class="literal">undefined</span>;   <span class="comment">// arr[2] is still there</span></span><br><span class="line">arr;                <span class="comment">// =&gt; Array(5) [ undefined, undefined, undefined, 4, 5 ]</span></span><br><span class="line"><span class="number">2</span> <span class="keyword">in</span> arr;           <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line"><span class="keyword">delete</span> fn;       <span class="comment">// =&gt; false</span></span><br><span class="line"><span class="keyword">delete</span> <span class="keyword">this</span>.fn;  <span class="comment">// =&gt; false</span></span><br></pre></td></tr></table></figure><h3 id="typeof-Operator">typeof Operator</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span> operand</span><br><span class="line"><span class="keyword">typeof</span>(operand)</span><br></pre></td></tr></table></figure><p>The return value is a string.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typeof</span> <span class="literal">NaN</span>          <span class="comment">// =&gt; "number"</span></span><br><span class="line"><span class="keyword">typeof</span> <span class="built_in">Number</span>(<span class="string">'1'</span>)  <span class="comment">// =&gt; "number"</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// All constructor functions, with the exception of the Function constructor, </span></span><br><span class="line"><span class="comment">// will always be typeof 'object'</span></span><br><span class="line"><span class="keyword">typeof</span>(<span class="keyword">new</span> <span class="built_in">Number</span>(<span class="string">'1'</span>))  <span class="comment">// =&gt; "object"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> fn = <span class="keyword">new</span> <span class="built_in">Function</span>(<span class="string">'x'</span>, <span class="string">'y'</span>, <span class="string">'var z = x + y; return z'</span>);</span><br><span class="line"><span class="keyword">typeof</span> fn;     <span class="comment">// =&gt; "function"</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typeof</span> x;      <span class="comment">// =&gt; "undefined"</span></span><br><span class="line"><span class="keyword">var</span> x;</span><br><span class="line"></span><br><span class="line"><span class="comment">// BUT</span></span><br><span class="line"><span class="comment">// =&gt; ReferenceError: can't access lexical declaration `letVar' before initialization</span></span><br><span class="line"><span class="keyword">typeof</span> letVar;</span><br><span class="line"><span class="keyword">let</span> letVar;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; ReferenceError: can't access lexical declaration `constVar' before initialization</span></span><br><span class="line"><span class="keyword">typeof</span> constVar;</span><br><span class="line"><span class="keyword">const</span> constVar = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; ReferenceError: can't access lexical declaration `customClass' before initialization</span></span><br><span class="line"><span class="keyword">typeof</span> customClass;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">customClass</span></span>&#123;&#125;;</span><br></pre></td></tr></table></figure><h3 id="Spread-Operator">Spread Operator</h3><p>Spread operator expands an iterable.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fn</span>(<span class="params">x, y, z</span>) </span>&#123; <span class="built_in">console</span>.log(x) &#125;;</span><br><span class="line">fn(...[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);  <span class="comment">// =&gt; 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Sat Feb 01 2020 00:00:00 GMT+0000 (Coordinated Universal Time)</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="keyword">new</span> <span class="built_in">Date</span>(...[<span class="number">2020</span>, <span class="number">1</span>, <span class="number">1</span>]).toString());</span><br><span class="line"></span><br><span class="line"><span class="comment">// concatenate arrays</span></span><br><span class="line"><span class="keyword">const</span> arr = [...[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>], ...[<span class="string">'a'</span>, <span class="string">'b'</span>]];</span><br><span class="line"><span class="built_in">console</span>.log(arr);  <span class="comment">// =&gt; Array(5) [ 1, 2, 3, "a", "b" ]</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">// Object Shallow-cloning (excluding prototype)     </span></span><br><span class="line"><span class="keyword">const</span> obj = &#123;<span class="attr">x</span>: <span class="number">1</span>,</span><br><span class="line">    <span class="keyword">get</span> y() &#123; <span class="keyword">return</span> <span class="keyword">this</span>._y&#125;,</span><br><span class="line">    <span class="keyword">set</span> y(val) &#123; <span class="keyword">this</span>._y = val&#125;,</span><br><span class="line">    <span class="keyword">get</span> z() &#123; <span class="keyword">return</span> <span class="number">3</span>; &#125;,</span><br><span class="line">    <span class="keyword">set</span> z(val) &#123; <span class="built_in">console</span>.log(<span class="string">'do nothing for z'</span>); &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> clonedObj = &#123;</span><br><span class="line">    <span class="keyword">get</span> z() &#123; <span class="built_in">console</span>.log(<span class="string">'z is undefined'</span>)&#125;,</span><br><span class="line">    <span class="keyword">set</span> z(val) &#123;<span class="built_in">console</span>.log(<span class="string">'set z with value:'</span>, val)&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Object &#123; x: 1, y: Getter &amp; Setter, z: Getter &amp; Setter &#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(obj);</span><br><span class="line"><span class="comment">// =&gt; Object &#123; x: 1, y: undefined, z: 3 &#125;</span></span><br><span class="line"><span class="built_in">console</span>.log(&#123;...obj&#125;);</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * =&gt; set z with value: 3</span></span><br><span class="line"><span class="comment"> * =&gt; Object &#123; z: Getter &amp; Setter, x: 1, y: undefined &#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="built_in">Object</span>.assign(clonedObj, obj));</span><br></pre></td></tr></table></figure><p><code>Object.assign</code> calls the getter of the source object and the setter of the target object, look at its name <strong>assign</strong>. The spread operator only calls the getter.</p><p>If <code>Object.assign</code> copies property definitions into prototypes, look more details from <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/assign" target="_blank" rel="noopener">here</a>.</p><h2 id="Expressions">Expressions</h2><h3 id="Expression-Evaluation-Order">Expression Evaluation Order</h3><p>Always left- to-right order</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> z = x + y; <span class="comment">// the evaluation order: z, x, y</span></span><br></pre></td></tr></table></figure><h3 id="Destructuring-Assignment">Destructuring Assignment</h3><h4 id="Array-Destructuring">Array Destructuring</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> arr = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">const</span> [one=<span class="string">'1'</span>, two, , ...others] = arr;  <span class="comment">// "one" has a default value</span></span><br><span class="line"><span class="comment">// 3 is ignored, the remaining part of the array is assigned to "others"</span></span><br><span class="line"><span class="built_in">console</span>.log(one, two, others);  <span class="comment">// =&gt; 1 2 Array [ 4, 5 ]</span></span><br></pre></td></tr></table></figure><h4 id="Object-Destructuring">Object Destructuring</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obj = &#123;<span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>&#125;;</span><br><span class="line"><span class="keyword">let</span> &#123;foo, bar&#125; = obj;</span><br><span class="line"><span class="keyword">let</span> &#123;x, y&#125; = obj;</span><br><span class="line"><span class="built_in">console</span>.log(foo, bar, x, y);  <span class="comment">// =&gt; undefined undefined 1 2</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// SyntaxError: redeclaration of let foo</span></span><br><span class="line"><span class="comment">// const &#123;x: foo, y: bar&#125; = obj;    </span></span><br><span class="line"><span class="keyword">const</span> &#123;<span class="attr">x</span>: foo2, <span class="attr">y</span>: bar2&#125; = obj;</span><br><span class="line"><span class="built_in">console</span>.log(foo2, bar2, foo2 <span class="keyword">in</span> <span class="keyword">this</span>);  <span class="comment">// =&gt; 1 2 false</span></span><br><span class="line">    </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// SyntaxError: expected expression, got '='</span></span><br><span class="line">    <span class="comment">// &#123;x, y&#125; = &#123;x: 1, y: 2&#125;;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// the parentheses is needed when destructuring a literal object </span></span><br><span class="line">    <span class="comment">// without a declaration</span></span><br><span class="line">    (&#123;x, y&#125; = &#123;<span class="attr">x</span>: <span class="number">10</span>, <span class="attr">y</span>: <span class="number">20</span>&#125;);</span><br><span class="line">    <span class="built_in">console</span>.log(x, y);  <span class="comment">// =&gt; 10 20</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(x, y);  <span class="comment">// =&gt; 10 20</span></span><br></pre></td></tr></table></figure><h2 id="Type-Conversions">Type Conversions</h2><p>The primitive-to-primitive and primitive-to-object conversions are straightforward.</p><p>The object-to-primitive conversion is more complicated.</p><h3 id="Implicit-Conversions">Implicit Conversions</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">"0"</span> == <span class="number">0</span>  <span class="comment">// =&gt; true</span></span><br></pre></td></tr></table></figure><p>Convert <code>&quot;0&quot;</code> to <code>0</code> before comparing.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span> + <span class="string">""</span> =&gt; <span class="string">"0"</span></span><br><span class="line"><span class="number">1</span> + <span class="string">"0"</span> =&gt; <span class="string">"10"</span></span><br><span class="line"><span class="string">"0"</span> + <span class="number">1</span> =&gt; <span class="string">"01"</span></span><br></pre></td></tr></table></figure><p>If one operand of the <code>+</code> operator is a string, the result is a string.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> - <span class="string">"1"</span>        <span class="comment">// =&gt; 0</span></span><br><span class="line"><span class="number">1</span> - <span class="string">"1"</span> == <span class="number">0</span>   <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="number">1</span> - <span class="string">"a"</span>        <span class="comment">// =&gt; NaN</span></span><br></pre></td></tr></table></figure><p>These are arithmetic operations.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">+<span class="string">"0"</span>  <span class="comment">// =&gt; 0</span></span><br></pre></td></tr></table></figure><p><code>+</code> operator converts its operand to a number.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">-<span class="string">"1"</span>       <span class="comment">// =&gt; -1</span></span><br><span class="line"><span class="keyword">var</span> x=<span class="string">"2"</span></span><br><span class="line">-x + <span class="number">1</span>     <span class="comment">// =&gt; -1</span></span><br></pre></td></tr></table></figure><p>These are arithmetic operations.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">!<span class="number">2</span> =&gt; <span class="literal">false</span></span><br><span class="line">!<span class="string">"0"</span> =&gt; <span class="literal">true</span></span><br></pre></td></tr></table></figure><p><code>!</code> operator converts its operand to a boolean.</p><h3 id="Explicit-Conversions">Explicit Conversions</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// other global functions: parseInt(), parseFloat()</span></span><br><span class="line"><span class="built_in">parseInt</span>(<span class="string">"123"</span>)    <span class="comment">// =&gt; 123</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> x = <span class="number">2</span>;</span><br><span class="line">x.toString() =&gt; <span class="string">"2"</span></span><br><span class="line">x.toString(<span class="number">2</span>) =&gt; <span class="string">"10"</span>, binary</span><br><span class="line">x.toString(<span class="number">16</span>) =&gt; <span class="string">"2"</span>, hexadecimal</span><br><span class="line"><span class="comment">// other methods like toString(): toFixed(), toExponential(), toPrecision()</span></span><br></pre></td></tr></table></figure><h4 id="Primitive-to-Object-Conversions">Primitive-to-Object Conversions</h4><p>The Primitive-to-object conversions are straightforward, using wrapper objects: <code>String()</code>, <code>Number()</code>, <code>Boolean()</code>.</p><p>No conversion and raise <code>TypeError</code> when using <code>null</code> and <code>undefined</code> where an object is excepted.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span>(<span class="number">123</span>)        <span class="comment">// =&gt; "123"</span></span><br><span class="line"><span class="built_in">Number</span>(<span class="string">"0"</span>) == <span class="number">0</span>   <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Object</span>(<span class="literal">null</span>)       <span class="comment">// =&gt; Object(), empty object</span></span><br><span class="line"><span class="built_in">Object</span>(<span class="literal">undefined</span>)  <span class="comment">// =&gt; Object(), empty object</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Number</span>(<span class="literal">null</span>)       <span class="comment">// =&gt; 0</span></span><br><span class="line"><span class="built_in">Number</span>(<span class="literal">undefined</span>)  <span class="comment">// =&gt; NaN</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">String</span>(<span class="literal">null</span>)       <span class="comment">// =&gt; "null"</span></span><br><span class="line"><span class="built_in">String</span>(<span class="literal">undefined</span>)  <span class="comment">// =&gt; "undefined"</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="literal">null</span>)      <span class="comment">// =&gt; false</span></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="literal">undefined</span>) <span class="comment">// =&gt; false</span></span><br></pre></td></tr></table></figure><h4 id="Object-to-Primitive-Conversions">Object-to-Primitive Conversions</h4><p>All objects convert to the boolean <code>true</code>.</p><p>All objects(don't include web browsers defined objects) inherit <code>toString()</code> and <code>valueOf()</code>, could override them in custom objects.</p><p>The <code>toString()</code> returns a string representing the object</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(&#123;&#125;).toString()      <span class="comment">// =&gt; "[object Object]", by default</span></span><br><span class="line">[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>].toString()   <span class="comment">// =&gt; "1,2,3"</span></span><br><span class="line"></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;<span class="built_in">console</span>.log(<span class="string">'hello'</span>)&#125;).toString()</span><br><span class="line">                     <span class="comment">// =&gt; "function() &#123;console.log('hello')&#125;"</span></span><br></pre></td></tr></table></figure><p>The <code>valueOf()</code> returns the primitive value of the object</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(&#123;&#125;).valueOf()        <span class="comment">// =&gt; Object &#123;  &#125;, return the object itself</span></span><br><span class="line">[<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>].valueOf()     <span class="comment">// =&gt; Array(3) [ 1, 2, 3 ], return the object itself</span></span><br><span class="line"></span><br><span class="line">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;<span class="built_in">console</span>.log(<span class="string">'hello'</span>)&#125;).valueOf()</span><br><span class="line">                      <span class="comment">// =&gt; function (), return the object itself</span></span><br><span class="line"><span class="built_in">String</span>(<span class="string">"1"</span>).valueOf() <span class="comment">// =&gt; "1", return the wrapped primitive value</span></span><br></pre></td></tr></table></figure><p>The rules:</p><ul><li><p>To convert an object to a string</p><ul><li>check <code>toString()</code> returned value, overrided <code>toString()</code> can return any value, if the value is primitive, convert it to a string(if necessary), done;</li><li>check <code>valueOf()</code>, if the value is primitive, convert it to a string(if necessary), done;</li><li>throw <code>TypeError</code></li></ul></li><li><p>To convert an object to a number</p><ul><li>similar to string, but check <code>valueOf()</code> first, then <code>toString()</code>;</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[<span class="number">1</span>].toString()   <span class="comment">// =&gt; "1"</span></span><br><span class="line">[<span class="number">1</span>].valueOf()    <span class="comment">// =&gt; Array [ 1 ], return the object itself</span></span><br><span class="line"><span class="number">3</span> - [<span class="number">1</span>]          <span class="comment">// =&gt; 2</span></span><br></pre></td></tr></table></figure></li></ul><p>The object-to-primitive conversion is an object-to-number conversion first( <code>valueOf()</code> ), then an object-to-string conversion( <code>toString()</code> ).</p><p><code>+</code>, <code>-</code>, <code>*</code>, <code>/</code>, <code>%</code>, <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&gt;</code> operators use the object-to-primitive conversion, use the returned primitive value directly, no further conversion to a number or string.</p><p>If either of operands of <code>+</code> is object, perform the object-to-primitive conversion, then if one of the operands is string, perform string concatenation, otherwise, is arithmetic. Look at the difference of line 1, line 11.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="number">1</span> + [<span class="number">1</span>]  <span class="comment">// =&gt; "11", convert object to string</span></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="literal">true</span>).toString() <span class="comment">// =&gt; "true"</span></span><br><span class="line"><span class="built_in">Boolean</span>(<span class="literal">true</span>).valueOf()  <span class="comment">// =&gt; true</span></span><br><span class="line"><span class="number">1</span> == <span class="built_in">Boolean</span>(<span class="literal">true</span>)  <span class="comment">// =&gt; true, convert object to boolean</span></span><br><span class="line"><span class="number">1</span> + <span class="built_in">Boolean</span>(<span class="literal">true</span>)   <span class="comment">// =&gt; 2, convert object to boolean, not string</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> myObject = &#123;</span><br><span class="line">  toString: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="string">"1"</span>;&#125;,</span><br><span class="line">  valueOf: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="number">3</span>;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="number">1</span> + myObject   <span class="comment">// =&gt; 4</span></span><br><span class="line"><span class="number">2</span> - myObject   <span class="comment">// =&gt; -1</span></span><br><span class="line"><span class="number">3</span> != myObject  <span class="comment">// =&gt; false</span></span><br><span class="line"><span class="number">3</span> &gt; myObject   <span class="comment">// =&gt; false</span></span><br><span class="line"><span class="number">3</span> * myObject   <span class="comment">// =&gt; 9</span></span><br><span class="line"><span class="number">3</span> / myObject   <span class="comment">// =&gt; 1</span></span><br><span class="line"><span class="number">3</span> % myObject   <span class="comment">// =&gt; 0</span></span><br></pre></td></tr></table></figure><p>Date objects are special, its <code>+</code>, <code>==</code>, <code>!=</code> use an object-to-string conversion( <code>toString()</code> ) first, not <code>valueOf()</code> first;</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> now = <span class="keyword">new</span> <span class="built_in">Date</span>();</span><br><span class="line"></span><br><span class="line">now + <span class="number">1</span>                <span class="comment">// =&gt; return a string</span></span><br><span class="line">now == now.toString()  <span class="comment">// =&gt; true</span></span><br><span class="line">now != now.toString()  <span class="comment">// =&gt; false</span></span><br></pre></td></tr></table></figure><h2 id="Control-Flow">Control Flow</h2><h3 id="Block-Statement">Block Statement</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    StatementList</span><br><span class="line">&#125; <span class="comment">// no ';'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// OR</span></span><br><span class="line"></span><br><span class="line">LabelIdentifier: &#123;</span><br><span class="line">    StatementList</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>In non-strict code, function declarations inside blocks behave strangely. Do not use them. When use <code>let</code> or <code>const</code> to declare variables within a block, the block also define a block scope, these variables are only visible within the block and hide global or local variables that have the same name.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">let</span> x = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(x);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>{}</code> included code is a whole, acts as one statement, can be used anywhere that JS except a single statement.</p><h3 id="if-else">if...else</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ( expression )</span><br><span class="line">    statement</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> ( expression )   <span class="comment">// Optional</span></span><br><span class="line">    statement</span><br><span class="line"><span class="comment">/* ... */</span>    </span><br><span class="line"><span class="keyword">else</span>                     <span class="comment">// Optional</span></span><br><span class="line">    statement</span><br></pre></td></tr></table></figure><h3 id="switch">switch</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> ( expression ) &#123;</span><br><span class="line">    <span class="keyword">case</span> value1:           <span class="comment">// case expression can be an arbitrary expression</span></span><br><span class="line">        statement</span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    <span class="keyword">case</span> valueN:</span><br><span class="line">        statement</span><br><span class="line">    <span class="keyword">default</span>:               <span class="comment">// Optional</span></span><br><span class="line">        statement</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testSwitch</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// use === operator to match x, not == operator, no type conversions</span></span><br><span class="line">    <span class="keyword">switch</span>(x) &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">'1'</span>:</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="string">'1'</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:  <span class="comment">// 'default' label can appear anywhere within the switch statement</span></span><br><span class="line">            <span class="built_in">console</span>.log(<span class="string">'default'</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="number">1</span>);</span><br><span class="line">            <span class="comment">// no break, continue executing the following labels until encounter a break or end</span></span><br><span class="line">        <span class="keyword">case</span> <span class="string">'2'</span>:</span><br><span class="line">        <span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">testSwitch(<span class="string">'1'</span>);   <span class="comment">// =&gt; "1"</span></span><br><span class="line">testSwitch(<span class="number">1</span>);     <span class="comment">// =&gt; 1 2</span></span><br><span class="line">testSwitch(<span class="number">2</span>);     <span class="comment">// =&gt; 2</span></span><br><span class="line">testSwitch(<span class="string">'2'</span>);   <span class="comment">// =&gt; 2</span></span><br><span class="line">testSwitch(<span class="number">3</span>);     <span class="comment">// =&gt; "default"</span></span><br></pre></td></tr></table></figure><h3 id="while">while</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> ( expression )</span><br><span class="line">    statement</span><br></pre></td></tr></table></figure><p>It is impossible to completely simulate the <code>for</code> loop with <code>while</code>.</p><h3 id="do-while">do...while</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span></span><br><span class="line">    statement          <span class="comment">// iterates at least once</span></span><br><span class="line"><span class="keyword">while</span> ( expression );  <span class="comment">// always be terminated with a semicolon, the while statement doesn't</span></span><br></pre></td></tr></table></figure><h3 id="for">for</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> ([initialization]; [condition]; [final-expression])</span><br><span class="line">   statement</span><br><span class="line">   </span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="for-in">for...in</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (variable <span class="keyword">in</span> object)</span><br><span class="line">    statement</span><br></pre></td></tr></table></figure><p>Iterates over all enumerable properties of an object, including inherited enumerable properties.</p><p>Think the object is a list of key-value pairs, <code>for...in</code> return the keys, not the values.</p><p><code>variable</code> must be an lvalue, may be an existing variable, a <code>var</code>/<code>let</code>/<code>const</code> declared new variable, or any expression that evaluates to an lvalue.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;           </span><br><span class="line">    <span class="keyword">let</span> arr = [], i=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(arr[i++] <span class="keyword">in</span> <span class="string">'123'</span>);  <span class="comment">// arr is an lvalue</span></span><br><span class="line">    arr;  <span class="comment">// =&gt; Array(3) [ "0", "1", "2" ]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>object</code> can be any expression that evaluates to an object, do type conversions, convert the primitive value to its wrapper object.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="keyword">const</span> v <span class="keyword">in</span> <span class="string">'123'</span>)</span><br><span class="line">    <span class="built_in">console</span>.log(v);      <span class="comment">// =&gt; "0" "1" "2", convert '123' to String('123')</span></span><br></pre></td></tr></table></figure><p>Only enumerate enumerable properties of an object, not all properties. All<br>properties and methods defined by our code are enumerable.</p><p>Iterate over the properties in an arbitrary order, don't depend on the order you see,<br>especially to use with an array and may be return non-integer property names(not recommended for array)</p><p>It is best not to add, modify, or remove properties from the object during iteration,<br>no guarantee whether to enumerate the changed properties.</p><p>Most of the time, use <code>for...in</code> to debug.</p><h3 id="for-of">for...of</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (variable <span class="keyword">of</span> iterable) &#123;</span><br><span class="line">    statement</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Iterating over iterable objects.</p><p>Similar to <code>for...in</code>, the difference between them:</p><ul><li><code>for...in</code> iterates over the <strong>enumerable</strong> properties of an object, in an arbitrary order;</li><li><code>for...of</code> iterates over values(not keys or properties) of an <strong>iterable</strong> object. an object that implements the <code>@@iterator</code> method is an iterable object, such as <code>String</code>, <code>Array</code>.</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="built_in">Object</span>.prototype.objCustom = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line">    <span class="built_in">Array</span>.prototype.arrCustom = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> iterable = [<span class="number">3</span>, <span class="number">5</span>, <span class="number">7</span>];</span><br><span class="line">    iterable.foo = <span class="string">'hello'</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> i <span class="keyword">in</span> iterable) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(i); <span class="comment">// =&gt; "0" "1" "2" "foo" "arrCustom" "objCustom"</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> i <span class="keyword">in</span> iterable) &#123;</span><br><span class="line">        <span class="keyword">if</span> (iterable.hasOwnProperty(i)) &#123;</span><br><span class="line">            <span class="comment">// =&gt; "0" "1" "2" "foo"</span></span><br><span class="line">            <span class="comment">// arrCustom and objCustom are inherited properties, not own properties</span></span><br><span class="line">            <span class="built_in">console</span>.log(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> i <span class="keyword">of</span> iterable) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(i); <span class="comment">// =&gt; 3 5 7</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="label-statement">label statement</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">label :</span><br><span class="line">    statement</span><br></pre></td></tr></table></figure><p><code>label</code> must be a legal identifier;</p><p>JS has not <code>goto</code> statement like C language, can only use labels with <code>break</code> and <code>continue</code>;</p><p><code>break</code> can be used with any labeled statement and needs to be nested within the referenced label, it is able to break out of a loop, or <code>switch</code> that is not the nearest one;</p><p><code>continue</code> can be used with looping labeled statements, it is able to restart the loop that is not the nearest one;</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">hi: &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'hi'</span>);             <span class="comment">// =&gt; hi</span></span><br><span class="line">    <span class="keyword">break</span> hi;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'How are you?'</span>);   <span class="comment">// jump over the statement</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'hi'</span>);                 <span class="comment">// =&gt; hi</span></span><br><span class="line"></span><br><span class="line">top: </span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> v <span class="keyword">of</span> [<span class="string">'hi'</span>, <span class="string">'hey'</span>, <span class="string">'hello'</span>]) &#123;</span><br><span class="line">    <span class="keyword">if</span>( v == <span class="string">'hey'</span> )</span><br><span class="line">        <span class="keyword">continue</span> top;</span><br><span class="line">    <span class="built_in">console</span>.log(v);   <span class="comment">// =&gt; "hi" "hello", skip "hey"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">blockOne: &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'1'</span>);</span><br><span class="line">    <span class="keyword">break</span> blockTwo;  <span class="comment">// =&gt; SyntaxError: label not found, break is not nested within label blockTwo</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">blockTwo: &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'2'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="break">break</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">break [label];</span><br></pre></td></tr></table></figure><p>Break the current loop, <code>switch</code>, or <code>label</code> statement;</p><p>A function itself is nested within a loop, <code>switch</code>, or <code>label</code> statement, <code>break</code> cannot be used within the function body to break out of the loop, <code>switch</code>, or <code>label</code>.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">blockOne: &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'1'</span>);</span><br><span class="line">    ( <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">break</span> blockOne; <span class="comment">// =&gt; SyntaxError: label not found</span></span><br><span class="line">    &#125;)();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">labelFn: <span class="function"><span class="keyword">function</span> <span class="title">testBreakAndLabel</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'hi'</span>);</span><br><span class="line">    <span class="keyword">break</span> labelFn;       <span class="comment">// =&gt; SyntaxError: label not found</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'How are you?'</span>);</span><br><span class="line">&#125;</span><br><span class="line">testBreakAndLabel();</span><br></pre></td></tr></table></figure><h3 id="continue">continue</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">continue [label];</span><br></pre></td></tr></table></figure><p><code>continue</code> can be used only within the body of a loop, with or without <code>label</code>;</p><h3 id="return">return</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">return [expression];</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fn = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;<span class="comment">/* no return statement */</span>&#125;;</span><br><span class="line"><span class="keyword">var</span> fn2 = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;<span class="keyword">return</span>; <span class="comment">/* the return statement hasn't a value */</span>&#125;;</span><br><span class="line">fn();      <span class="comment">// =&gt; undefined</span></span><br><span class="line">fn2();     <span class="comment">// =&gt; undefined</span></span><br></pre></td></tr></table></figure><h3 id="try-catch">try...catch</h3><p>Three forms:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">try...catch</span><br><span class="line"></span><br><span class="line">try...finally</span><br><span class="line"></span><br><span class="line">try...catch...finally</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Nested try-blocks</span></span><br><span class="line"><span class="keyword">try</span> &#123;  <span class="comment">// can't miss &#123;&#125;, even only one statement </span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// throw;  // =&gt; SyntaxError: throw statement is missing an expression</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="string">'oops'</span>; <span class="comment">// can be any type of a value of the expression, e.g.: throw 1</span></span><br><span class="line">    &#125; <span class="keyword">catch</span> (e) &#123;  <span class="comment">// e is a block-scope variable</span></span><br><span class="line">        <span class="built_in">console</span>.error(<span class="string">'inner:'</span>, e);                 <span class="comment">// =&gt; "inner: oops"</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;    <span class="comment">// always executed regardless of what happens in the try block</span></span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'inner: finally'</span>);              <span class="comment">// =&gt; "inner: finally"</span></span><br><span class="line">    &#125;   </span><br><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="built_in">console</span>.error(<span class="string">'outer:'</span>, e); </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'oops'</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">        <span class="built_in">console</span>.error(<span class="string">'inner:'</span>, e.message);   <span class="comment">// =&gt; "inner: oops"</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'another oops in catch-block'</span>);</span><br><span class="line">        <span class="comment">// OR</span></span><br><span class="line">        <span class="comment">// throw e; // rethrow the exception</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(<span class="string">'inner: finally'</span>);        <span class="comment">// =&gt; "inner: finally"</span></span><br><span class="line">    &#125;   </span><br><span class="line">&#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">    <span class="built_in">console</span>.error(<span class="string">'outer:'</span>, e.message);       <span class="comment">// =&gt; outer: another oops in catch-block</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testTry</span>(<span class="params">toThrow</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">            <span class="built_in">console</span>.error(<span class="string">'OMG, oops!'</span>);</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'Oops in catch-block'</span>);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">/* If the finally-block itself causes a jump with a return, continue, break, or </span></span><br><span class="line"><span class="comment">             * throw statement, or by calling another function that throws an exception, no matter </span></span><br><span class="line"><span class="comment">             * whatever happened and performs the new jump</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="keyword">if</span>(toThrow) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'Oops in finally-blcok'</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="built_in">console</span>.log(<span class="string">'No oops, oh, yeah!'</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="string">'OK'</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">        <span class="built_in">console</span>.error(<span class="string">'What happened?'</span>, e.message);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="string">'NOT OK'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* output:</span></span><br><span class="line"><span class="comment">  OMG, oops!</span></span><br><span class="line"><span class="comment">  No oops, oh, yeah!</span></span><br><span class="line"><span class="comment">  OK</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="built_in">console</span>.log(testTry(<span class="literal">false</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">/* output:</span></span><br><span class="line"><span class="comment">  OMG, oops!</span></span><br><span class="line"><span class="comment">  What happened? Oops in finally-blcok</span></span><br><span class="line"><span class="comment">  NOT OK</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="built_in">console</span>.log(testTry(<span class="literal">true</span>));</span><br></pre></td></tr></table></figure><h3 id="debugger">debugger</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">debugger</span><br></pre></td></tr></table></figure><p>If have an available and running debugger (the debugger of DevTools), the statement acts like setting a breakpoint in the debugger, otherwise, do nothing;</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">'hi'</span>);</span><br><span class="line"><span class="keyword">debugger</span>;</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'How are you?'</span>);</span><br></pre></td></tr></table></figure><p>Run in the console of DevTools, pause at line 2;</p><p>Run using Node.js (<code>node testDebugger.js</code>), the statement do nothing;</p><h2 id="Strict-mode">Strict mode</h2><p>Use <code>'use strict'</code> to invoke the strict mode.</p><p><code>&quot;use strict&quot;</code> or <code>'use strict'</code> is a directive, not a statement, appears only at the top of a JS script or a function body before any real statements.</p><h3 id="Don-t-do-this">Don't do this:</h3><ul><li>concatenating strict code, non-strict code, the result is strict;</li><li>concatenating non-strict code, strict code, the result is non-strict;</li></ul><p>JavaScript modules are strict by default.</p><p>Code passed to <code>eval()</code> is in strict mode if calling <code>eval()</code> (must call directly) from strict code. Can also use <code>'use strict'</code> directly in <code>eval()</code>.</p><h3 id="Why-to-Use">Why to Use</h3><ul><li><p>throwing errors instead of some silent errors, fix problems easier;</p></li><li><p>making things simpler, let JS engine optimize the code better;</p></li><li><p>write more secure JavaScript code;</p></li></ul><h3 id="What-Changed">What Changed</h3><ul><li>Converting mistakes into errors</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> nonStrictFn = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'"this" should be the global object'</span>, <span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line">    </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testStrictMode</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"><span class="meta">    'use strict'</span>;</span><br><span class="line">    <span class="comment">// not a new implicit global variable, must declare the variable first, then assign</span></span><br><span class="line">    newGlobalVar = <span class="number">0</span>;  <span class="comment">// =&gt; ReferenceError: assignment to undeclared variable newGlobalVar</span></span><br><span class="line">        </span><br><span class="line">    nonStrictFn();  <span class="comment">// =&gt; "this" should be the global object</span></span><br><span class="line">    <span class="comment">// functions(not methods of objects) in strict mode haven't "this"</span></span><br><span class="line">    <span class="comment">// can use the feature to determine whether the code is in strict mode</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'In strict mode? '</span>, !<span class="keyword">this</span> ? <span class="string">'Yes'</span>: <span class="string">'No'</span>);  <span class="comment">// =&gt; In strict mode?  Yes</span></span><br><span class="line">&#125;</span><br><span class="line">testStrictMode();</span><br></pre></td></tr></table></figure><ul><li>Making <code>eval</code> and <code>arguments</code> simpler</li></ul><h3 id="eval-in-Strict-Mode">eval() in Strict Mode</h3><ul><li><p>use the environment of the caller within a function, but can't define new variables in the local or global scope;</p></li><li><p>has a private environment, defined new variables are in this scope;</p></li><li><p>can't redefine eval, it is a keyword;</p></li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="meta">"use strict"</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">var</span> y = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">testEval</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> y = <span class="number">1</span>; <span class="comment">// hide the global one</span></span><br><span class="line">    <span class="keyword">var</span> z = <span class="number">1</span>;</span><br><span class="line">  </span><br><span class="line">    <span class="built_in">eval</span>(<span class="string">'x += 1'</span>);  <span class="comment">// strict mode</span></span><br><span class="line">    <span class="built_in">eval</span>(<span class="string">'z += 1'</span>);  <span class="comment">// strict mode</span></span><br><span class="line">  </span><br><span class="line">    <span class="comment">// eval('k = 1');   // =&gt; ReferenceError: assignment to undeclared variable k</span></span><br><span class="line">  </span><br><span class="line">    <span class="comment">// strict mode, varInEvalPrivateEnv is only in the private environment of eval</span></span><br><span class="line">    <span class="built_in">eval</span>(<span class="string">'var varInEvalPrivateEnv = 1'</span>);</span><br><span class="line">    <span class="comment">// =&gt; ReferenceError: varInEvalPrivateEnv is not defined</span></span><br><span class="line">    <span class="comment">// console.log('varInEvalPrivateEnv:', varInEvalPrivateEnv);</span></span><br><span class="line">  </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'local z, should be 2:'</span>, z);  <span class="comment">// =&gt; local z, should be 2: 2</span></span><br><span class="line">  </span><br><span class="line">    <span class="comment">// indirectly call eval, use the global environment, not strict mode</span></span><br><span class="line">    <span class="keyword">var</span> aliasOfEval = <span class="built_in">eval</span>;</span><br><span class="line">    aliasOfEval(<span class="string">'y += 1; m = 1'</span>);</span><br><span class="line">  </span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'local y, should be 1:'</span>, y);  <span class="comment">// =&gt; local y, should be 1: 1</span></span><br><span class="line">  </span><br><span class="line">    <span class="comment">// eval is a JS keyword now</span></span><br><span class="line">    <span class="comment">// var eval = 1;   // =&gt; SyntaxError: 'eval' can't be defined or assigned to in strict mode code</span></span><br><span class="line">&#125;</span><br><span class="line">testEval();</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'global x, should be 2:'</span>, x);  <span class="comment">// =&gt; global x, should be 2: 2</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'global y, should be 2:'</span>, y);  <span class="comment">// =&gt; global y, should be 2: 2</span></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'global m, should be 1:'</span>, m);  <span class="comment">// =&gt; global m, should be 1: 1</span></span><br></pre></td></tr></table></figure><h3 id="arguments-in-Strict-Mode">arguments in Strict Mode</h3><ul><li>the properties of <code>arguments</code> are not aliases of the arguments of function;</li><li>can't read or set <code>arguments.callee</code>;</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">x, y</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length);      <span class="comment">// =&gt; 2</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'x: %d, y: %d'</span>, x, y);  <span class="comment">// =&gt; x: 1, y: 2</span></span><br><span class="line">    <span class="built_in">arguments</span>[<span class="number">0</span>]=<span class="string">'a'</span>;</span><br><span class="line">    y = <span class="string">'b'</span>;</span><br><span class="line">    <span class="comment">// arguments[0] is an alias of x, arguments[1] is an alias of y</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'x: %s, y: %s'</span>, x, y);  <span class="comment">// =&gt; x: a, y: b</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.callee);      <span class="comment">// =&gt; function () ...</span></span><br><span class="line">&#125;)(<span class="number">1</span>,<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">( <span class="function"><span class="keyword">function</span> (<span class="params">x, y</span>) </span>&#123;</span><br><span class="line"><span class="meta">    'use strict'</span>;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="built_in">arguments</span>.length);      <span class="comment">// =&gt; 2</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'x: %d, y: %d'</span>, x, y);  <span class="comment">// =&gt; x: 5, y: 6</span></span><br><span class="line">    <span class="built_in">arguments</span>[<span class="number">0</span>]=<span class="string">'c'</span>;</span><br><span class="line">    y = <span class="string">'d'</span>;</span><br><span class="line">    <span class="comment">// arguments[0] isn't an alias of x, arguments[1] is in the same way</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'x: %s, y: %s'</span>, x, y);  <span class="comment">// =&gt; x: 5, y: d</span></span><br><span class="line">    <span class="comment">// =&gt; TypeError: 'caller', 'callee', and 'arguments' properties may not be \</span></span><br><span class="line">    <span class="comment">//    accessed on strict mode functions</span></span><br><span class="line">    <span class="comment">// console.log(arguments.callee);</span></span><br><span class="line">&#125; )(<span class="number">5</span>,<span class="number">6</span>);</span><br></pre></td></tr></table></figure><h2 id="Asynchronous-JavaScript">Asynchronous JavaScript</h2><p>Asynchronous approachs:</p><ul><li>function callbacks</li><li>setTimeout() / setInterval() / requestAnimationFrame()</li><li>Promise</li><li>async/await</li></ul><h3 id="Promise">Promise</h3><p>A <code>Promise</code> is an object representing the eventual completion or failure of an asynchronous operation, the object could be attatched callback functions for the success and failure cases.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then</p><h3 id="Async-await">Async/await</h3><p>Syntactic sugar built on top of promises that allows you to run asynchronous operations using syntax that's more like writing synchronous callback code.</p><p><code>async</code> could be putted in front of a function declaration or object methods.</p><p><code>await</code> only works inside <code>async</code> functions.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Asynchronous<br>https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Asynchronous/Async_await<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function</p><h2 id="Meta-programming">Meta programming</h2><p>The <code>Proxy</code> and <code>Reflect</code> objects allowing you to intercept and define custom behavior for fundamental language operations (e.g. property lookup, assignment, enumeration, function invocation, etc).</p><ul><li>Proxy</li></ul><p>Intercept certain operations on an object, these operations are through the proxy first, could add some custom behaviors.</p><p>The proxy could be as a prototype object to create new objects. Operations on these new objects are also intercepted.</p><ul><li>Reflect</li></ul><p>If operations are intercepted, how to invoke original methods of an object? use <code>Reflect</code>.</p><p>In addition:</p><ul><li>let <code>in</code>, <code>delete</code> operators become function calls.</li><li>let some methods that throw exceptions return a boolean value.</li></ul><p>Save multiple prototype objects into an array property of an object, then could use <code>Proxy</code>, <code>Reflect</code> to intercept method invoking, implement a non-complicated multiple inheritance.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Meta_programming</p><h2 id="JavaScript-modules">JavaScript modules</h2><p>Use of native JavaScript modules is dependent on the import and export statements. Put different features into different JavaScript files, one file, one module.</p><p>Moduels is in <strong>strict</strong> mode.</p><p>Modules are <strong>deferred</strong> automatically, no need to use <strong>defer</strong> attribute in <code>&lt;script&gt;</code> tag.</p><p>Module features aren't available in the global scope. Therefore, you will only be able to access imported features in the script they are imported into.</p><p>Can't load modules in local HTML files(file:// URL), one is the file protocol, one is the http protocol, run into <em>CORS</em>, use a http server instead.</p><h3 id="Applying-modules">Applying modules</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">// in a HTML file</span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"module"</span> <span class="attr">src</span>=<span class="string">"main.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Need to include <code>type=&quot;module&quot;</code> in the <code>&lt;script&gt;</code> tag.</p><h3 id="Creating-a-module-object">Creating a module object</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> Module <span class="keyword">from</span> <span class="string">'./modules/module.js'</span>;</span><br></pre></td></tr></table></figure><p>This makes all exports as members of an object <code>Module</code>, creating a namespace which avoids conflicts.</p><h3 id="Classes-2">Classes</h3><p>Could import and export classes. This is another option to avoid conflicts.</p><h3 id="Aggregating-modules">Aggregating modules</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// shapes.js</span></span><br><span class="line"><span class="keyword">export</span> &#123; Square &#125; <span class="keyword">from</span> <span class="string">'./shapes/square.js'</span>;</span><br><span class="line"><span class="keyword">export</span> &#123; Triangle &#125; <span class="keyword">from</span> <span class="string">'./shapes/triangle.js'</span>;</span><br><span class="line"><span class="keyword">export</span> &#123; Circle &#125; <span class="keyword">from</span> <span class="string">'./shapes/circle.js'</span>;</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; Square, Circle, Triangle &#125; <span class="keyword">from</span> <span class="string">'./modules/shapes.js'</span>;</span><br></pre></td></tr></table></figure><p>This grabs the exports from three submodules and effectively makes them available from one module <code>shapes.js</code>.</p><h3 id="Dynamic-module-loading">Dynamic module loading</h3><p>Calling <code>import()</code> as a function, it returns a <code>Promise</code>, to dynamically load modules only when they are needed. Such as calling <code>import()</code> in a click event callback function would only load the module when clicking.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">squareBtn.addEventListener(<span class="string">'click'</span>, () =&gt; &#123;</span><br><span class="line">    <span class="keyword">import</span>(<span class="string">'./modules/square.js'</span>).then(<span class="function">(<span class="params">Module</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="comment">// access all exports through the Module object</span></span><br><span class="line">    &#125;)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h3 id="Other-Module-Solutions">Other Module Solutions</h3><h4 id="The-Revealing-Module-Pattern">The Revealing Module Pattern</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> revealingModule = (<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> privateVar = <span class="string">""</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">publicSetName</span>(<span class="params"> strName </span>) </span>&#123;</span><br><span class="line">        privateVar = strName;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">publicGetName</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> privateVar;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        setName: publicSetName,</span><br><span class="line">        getName: publicGetName</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure><p>It is a common way using closures to create private environments as modules.</p><h4 id="CommonJS-and-AMD">CommonJS and AMD</h4><p>The official JavaScript specification defines APIs that are useful for building browser-based applications. CommonJS defines a series of specifications which APIs could handle many common application needs. Module definition is a part of these specifications. AMD(Asynchronous Module Definition) was split from CommonJS, is also a specification which specifies a mechanism for defining modules. The main difference for modules between AMD and CommonJS is that AMD supports asynchronous module loading. Normally, CommonJS modules is used in the server-side, AMD modules is in the browser-side.</p><p>Node.js is one of implementations of CommonJS(in partial form). Node.js also supports native JavaScript modules.</p><p>RequireJS is one of implementations of AMD.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/import<br>https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/export<br>https://auth0.com/blog/javascript-module-systems-showdown/</p><h2 id="JavaScript-and-HTML">JavaScript and HTML</h2><h3 id="DOM-Document-Object-Model">DOM (Document Object Model)</h3><p>The DOM is a programming interface for HTML and XML, can be implemented by any programming language.</p><p>A Web page is a document, a document consists of a hierarchical tree of nodes, the DOM is an object-oriented representation of the tree which can be modified with JavaScript to change its structure, style, and content.</p><p>The <code>Document</code> object represents a document.</p><p>The <code>Node</code> object represents a node, have different kind of nodes, element node, text node, comment node, attribute node, etc.</p><p>Nodes are empty vessels, they can't represent visual content, elements are for this, the <code>Element</code> object represents an element, which inherits from the <code>Node</code> object. HTML has an enhanced <code>HTMLElement</code> object, the <code>HTMLElement</code> object has many specific implementations, such as <code>HTMLImageElement</code> represents the <code>&lt;img&gt;</code> element, <code>HTMLInputElement</code> represents the <code>&lt;input&gt;</code> element. each node is an HTML element in an HTML document.</p><p>Web API = DOM + JavaScript</p><p>HTML element list here:<br>https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API</p><p>More DOM details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Document_Object_Model</p><h3 id="windows-Object">windows Object</h3><p>We say the global object many times before, the <code>window</code> object is the global object in browsers.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">this</span> === <span class="built_in">window</span>;  <span class="comment">// =&gt; true</span></span><br></pre></td></tr></table></figure><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Window</p><h3 id="location-object">location object</h3><p>Is a property of the <code>window</code> object, represents URL info, such as protocol, host, port, href, of the current page. Changing properties of the <code>location</code> object lets the browser apply them and refresh the page.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">location === <span class="keyword">this</span>.location;  <span class="comment">// =&gt; true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; Location https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof</span></span><br><span class="line">location; </span><br><span class="line"></span><br><span class="line"><span class="comment">// jump to </span></span><br><span class="line"><span class="comment">// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/instanceof</span></span><br><span class="line">location=<span class="string">'instanceof'</span>;</span><br><span class="line"><span class="comment">// location.href='instanceof'; // do the same thing</span></span><br><span class="line"></span><br><span class="line">location = <span class="string">'https://www.bing.com'</span>; <span class="comment">// jump to www.bing.com</span></span><br><span class="line"><span class="comment">// jump to </span></span><br><span class="line"><span class="comment">// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/www.bing.com</span></span><br><span class="line"><span class="comment">// location = 'www.bing.com'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/** the following do the same thing</span></span><br><span class="line"><span class="comment"> * location.href = 'https://www.bing.com';</span></span><br><span class="line"><span class="comment"> * location.assign('https://www.bing.com');</span></span><br><span class="line"><span class="comment"> * location.replace('https://www.bing.com');</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><h3 id="history-Object">history Object</h3><p><code>history.back()</code> / <code>history.forward()</code> / <code>history.go()</code> like clicking / holding the Back / Forward button in the browser toolbar.</p><p><code>history.pushState()</code> / <code>history.replaceState()</code> change the brower history for the current tab or window.</p><p>event: <code>window.onpopstate</code>, <code>window.popstate</code>, <code>windows.onhashchange</code>, <code>window.hashchange</code> (change <code>localtion.hash</code>)</p><h3 id="navigator-Object">navigator Object</h3><p>Contains the browser and OS information, such as User-Agent, language, platform, geolocation, keyboard.</p><h3 id="screen-Object">screen Object</h3><p>Represents a screen on which the current window is being rendered. Contains width, height, orientation, color depth, etc.</p><h3 id="Selecting-HTML-Elements">Selecting HTML Elements</h3><ul><li>By id</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// only can be used by the document object</span></span><br><span class="line"><span class="comment">// any HTML element can have id attribute</span></span><br><span class="line"><span class="built_in">document</span>.getElementById();  <span class="comment">// return an Element object</span></span><br></pre></td></tr></table></figure><ul><li>By name attribue</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * only can be used by the document object </span></span><br><span class="line"><span class="comment"> * the name attribute is only valid on a handful of HTML elements, such button, input, </span></span><br><span class="line"><span class="comment"> * it is deprecated on &lt;img&gt; element. more details here:</span></span><br><span class="line"><span class="comment"> * https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="built_in">document</span>.getElementsByName();  <span class="comment">// return a live NodeList object</span></span><br></pre></td></tr></table></figure><ul><li>By tag name</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**  </span></span><br><span class="line"><span class="comment"> * return a live HTMLCollection object, the object is automatically updated when the DOM is changed, </span></span><br><span class="line"><span class="comment"> * no need to call methods which return a HTMLCollection object again to retrive changes, the NodeList</span></span><br><span class="line"><span class="comment"> * object is also. sample code here:</span></span><br><span class="line"><span class="comment"> * https://developer.mozilla.org/en-US/docs/Web/API/Element/getElementsByClassName</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * make a static snapshot using Array.prototype.slice to add or remove elements when iterating through </span></span><br><span class="line"><span class="comment"> * such type of objects</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * can replace the document object with a specified Element object, only search all descendants of the </span></span><br><span class="line"><span class="comment"> * element except itself</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * For historical reason, the document object has shortcut properties to acces some kinds of elements:</span></span><br><span class="line"><span class="comment"> * document.anchors</span></span><br><span class="line"><span class="comment"> * document.embeds</span></span><br><span class="line"><span class="comment"> * document.forms</span></span><br><span class="line"><span class="comment"> * document.images</span></span><br><span class="line"><span class="comment"> * document.links</span></span><br><span class="line"><span class="comment"> * document.plugins</span></span><br><span class="line"><span class="comment"> * document.scripts</span></span><br><span class="line"><span class="comment"> * they all are HTMLCollection objects, exposes its members directly as properties by both name and</span></span><br><span class="line"><span class="comment"> * index.</span></span><br><span class="line"><span class="comment"> *  </span></span><br><span class="line"><span class="comment"> * Other useful properties:</span></span><br><span class="line"><span class="comment"> * document.head</span></span><br><span class="line"><span class="comment"> * document.title</span></span><br><span class="line"><span class="comment"> * document.body</span></span><br><span class="line"><span class="comment"> * document.cookie</span></span><br><span class="line"><span class="comment"> * document.documentElement (the &lt;html&gt; element)</span></span><br><span class="line"><span class="comment"> * document.URL</span></span><br><span class="line"><span class="comment"> * document.referrer</span></span><br><span class="line"><span class="comment"> * document.lastModified</span></span><br><span class="line"><span class="comment"> * document.location</span></span><br><span class="line"><span class="comment"> * document.domain, could reduce the same-origin restrictions</span></span><br><span class="line"><span class="comment"> */</span>    </span><br><span class="line"><span class="built_in">document</span>.getElementsByTagName();</span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.images[<span class="number">0</span>].tagName;  <span class="comment">// =&gt; "IMG", HTML's tag names are always upper-case</span></span><br><span class="line"><span class="built_in">document</span>.getElementsByTagName(<span class="string">'img'</span>); <span class="comment">// lower-case for compatibility with XHTML</span></span><br><span class="line"><span class="comment">// use a specified Element object</span></span><br><span class="line">(<span class="built_in">document</span>.getElementById(<span class="string">'content'</span>).getElementsByTagName(<span class="string">'p'</span>))[<span class="number">0</span>].innerHTML;</span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.scripts[<span class="number">0</span>].innerText</span><br><span class="line"><span class="built_in">document</span>.links[<span class="string">"skip-main"</span>]</span><br><span class="line"><span class="built_in">document</span>.links.skipsearch</span><br></pre></td></tr></table></figure><ul><li>By class attribute</li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * with class attribute name or names(separated by whitespace)</span></span><br><span class="line"><span class="comment"> * the name or names are case sensitive</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="built_in">document</span>.getElementsByClassName();</span><br></pre></td></tr></table></figure><ul><li>By CSS selecotrs</li></ul><p>This is modern approach to match an element.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * can replace the document object with a specified Element object, only match all descendants </span></span><br><span class="line"><span class="comment"> * of the element except itself. </span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * CSS pseudo-elements will never return any elements.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> * when invoked on an element, the two methods search the whole document first, then filter, </span></span><br><span class="line"><span class="comment"> * including ancestors of the element within the CSS selector is legal, but is weird, like a </span></span><br><span class="line"><span class="comment"> * mistake. sample code here:</span></span><br><span class="line"><span class="comment"> * https://developer.mozilla.org/en-US/docs/Web/API/Element/querySelectorAll</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="built_in">document</span>.querySelector();  <span class="comment">// only return the first matched element</span></span><br><span class="line"><span class="built_in">document</span>.querySelectorAll();  <span class="comment">// return a non-live NodeList object</span></span><br></pre></td></tr></table></figure><h3 id="DOM-Traversal">DOM Traversal</h3><h4 id="As-Tree-of-Nodes">As Tree of Nodes</h4><p>Use properties of the <code>Node</code> object:</p><ul><li>parentNode</li><li>childNodes</li><li>firstChild</li><li>lastChild</li><li>nextSibling</li><li>previousSibling</li></ul><h4 id="As-Tree-of-Elements">As Tree of Elements</h4><p>Use properties of the <code>Element</code> object:</p><ul><li>children</li><li>firstElementChild</li><li>lastElementChild</li><li>previousElementSibling</li><li>nextElementSibling</li></ul><p>Text and comment nodes are not elments, they are ignored.</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">...</span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">id</span>=<span class="string">"properties"</span>&gt;</span>Properties<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line">...</span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The <code>&lt;h2&gt;</code> element is in the traversal path, its text &quot;Properties&quot; won't be.</p><h3 id="Element-Attributes">Element Attributes</h3><p>The <code>Element</code> object has an <code>attributes</code> property which is a live collection including all attributes of the element. Also have a <code>getAttributeNames()</code> methods which returns all attribute names.</p><h4 id="Standard-HTML-Attributes">Standard HTML Attributes</h4><p>These attributes are properties of an element, straightforward.</p><p>HTML attributes are not case sensitive, but JavaScript isn't, the property names are lowercase. A few HTML properties are longer than one word, such as <code>http-equiv</code>, convert them to the style: <code>propNameLikeThis</code>.</p><p>If HTML attribute names are JavaScript reserved words, prefixing them with <code>html</code>, but <code>class</code> is an exception, it is <code>className</code>, not <code>htmlClass</code>.</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">...</span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">id</span>=<span class="string">"myMeta"</span> <span class="attr">http-equiv</span>=<span class="string">"X-UA-Compatible"</span> <span class="attr">content</span>=<span class="string">"IE=Edge"</span>&gt;</span></span><br><span class="line">...</span><br><span class="line"><span class="tag">&lt;<span class="name">label</span> <span class="attr">id</span>=<span class="string">"myLabel"</span> <span class="attr">for</span>=<span class="string">"main-q"</span> <span class="attr">class</span>=<span class="string">"visually-hidden"</span> <span class="attr">draggable</span>=<span class="string">"false"</span> <span class="attr">style</span>=<span class="string">""</span>&gt;</span>Search MDN<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">...</span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myMeta'</span>).httpEquiv;  <span class="comment">// =&gt; "X-UA-Compatible"</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myLabel'</span>).htmlFor;   <span class="comment">// =&gt; "main-q"</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myLabel'</span>).for;       <span class="comment">// =&gt; undefined, for is a reserved word</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myLabel'</span>).htmlClass; <span class="comment">// =&gt; undefined</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myLabel'</span>).className; <span class="comment">// =&gt; "visually-hidden"</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myLabel'</span>).draggable; <span class="comment">// =&gt; flase, convert it to boolean, number is also</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#myLabel'</span>).style;     <span class="comment">// =&gt; CSS2Properties(0), an object</span></span><br></pre></td></tr></table></figure><h4 id="Non-Standard-HTML-Attributes">Non-Standard HTML Attributes</h4><p>Use methods:</p><ul><li>getAttribute()</li><li>hasAttribute()</li><li>setAttribute()</li><li>removeAttribute()</li></ul><p><code>getAttribute()</code> returns a string, or <code>null</code>, or <code>&quot;&quot;</code>.</p><p>Use <code>hasAttribute()</code> to check for an attribute's existence, not the return value of <code>getAttribute()</code>.</p><p><code>setAttribute()</code> always converts its value argument to a string. Doesn't care about boolean attributes' actual value, they're present, they're <code>true</code>, we can just set their value to <code>&quot;&quot;</code>.</p><p><code>removeAttribute()</code> can be used to remove a standard attribute, but just empties its value in Javascript, the HTML attribute in the web page will disappear.</p><h4 id="data-Attributes">data-* Attributes</h4><p>HTML5 defines a standard way to attach additional data, any attribute whose name is lowercase and begins with the prefix <code>data-</code> is a valid custom data attribute. e.g.</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">li</span> <span class="attr">data-item</span>=<span class="string">"Technologies"</span> <span class="attr">role</span>=<span class="string">"menuitem"</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Valid custom data attribute names can only include lowercase letters, numbers, dash(<code>-</code>), underscore(<code>_</code>), dot(<code>.</code>), colon(<code>:</code>).</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">li</span> <span class="attr">data-abc:123-efg_-h32.hi</span>=<span class="string">'1'</span> <span class="attr">data-item</span>=<span class="string">"Tech"</span>&gt;</span></span><br></pre></td></tr></table></figure><p>The property name in JavaScript is <code>abc:123Efg_H32.hi</code>, remove <code>data-</code> and dashs, the letters that follow a dash are transformed into its uppercase.</p><p>The <code>dataset</code> property is a <code>DOMStringMap</code> which stores all custom data attributes of the element and<br>is live, the property itself is read-only, but can read, write, delete its members.</p><p>Each property in <code>dataset</code> is always a string.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// =&gt; DOMStringMap &#123; "abc:123Efg_H32.hi" → "1", item → "Tech" &#125;</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).dataset;</span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).dataset.item;  <span class="comment">// =&gt; "Tech"</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).dataset[<span class="string">"abc:123Efg_H32.hi"</span>];  <span class="comment">// =&gt; "1"</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).getAttribute(<span class="string">'data-abc:123-efg_-h32.hi'</span>);  <span class="comment">// =&gt; "1"</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).dataset = &#123;&#125;; <span class="comment">// do nothing, read-only</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).dataset.item = <span class="string">"Technologies"</span>;</span><br><span class="line"><span class="keyword">delete</span> <span class="built_in">document</span>.querySelector(<span class="string">'li'</span>).dataset.item;</span><br><span class="line"><span class="string">'item'</span> <span class="keyword">in</span> <span class="built_in">document</span>.querySelector(<span class="string">'meta'</span>).dataset;  <span class="comment">// =&gt; false</span></span><br></pre></td></tr></table></figure><p>Notes:</p><ul><li>Do not store content that should be visible and accessible in data attributes.</li><li>Poor read performance.</li></ul><p>More datails here:<br>https://developer.mozilla.org/en-US/docs/Learn/HTML/Howto/Use_data_attributes<br>https://developer.mozilla.org/en-US/docs/Web/API/HTMLOrForeignElement/dataset<br>https://bugzilla.mozilla.org/show_bug.cgi?id=802157</p><h3 id="Element-Content">Element Content</h3><h4 id="As-HTML">As HTML</h4><ul><li><code>innerHTML</code> property, gets or replaces the HTML code contained within the element</li><li><code>outerHTML</code> property, like <code>innerHTML</code>, but apply to the element itself and its descendants</li><li><code>insertAdjacentHTML()</code> method, insert HTML code</li></ul><p>If an element has no parent element, setting its <code>outerHTML</code> property does nothing.</p><p>The markup and formatting of the returned HTML by <code>innerHTML</code> or <code>outerHTML</code> is likely not to match the original page markup.</p><p>HTML5 doesn't execute <code>&lt;script&gt;</code> tag in <code>innerHTML</code> and <code>outerHTML</code>, but there are still other ways to execute JavaScript, don't use them to insert untrusted HTML code or plain text (use <code>textContent</code> property).</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Element/innerHTML<br>https://developer.mozilla.org/en-US/docs/Web/API/Element/outerHTML<br>https://developer.mozilla.org/en-US/docs/Web/API/Element/insertAdjacentHTML</p><h4 id="As-Plain-Text">As Plain Text</h4><ul><li><code>textContent</code> property, gets the text content of an element and its descendants, or replaces them with a single text node</li><li><code>innerText</code> property, like <code>textContent</code>, but the text is <em>rendered</em></li></ul><p><code>textContent</code> returns concatenated text which is like <code>innerHTML</code> returned HTML code, but stripping all HTML tags, so ignores <code>&lt;br&gt;</code>, a newline is gone, but preserves tabs, spaces that are within these tags. It gets the content of all elements, including <code>&lt;script&gt;</code> and <code>&lt;style&gt;</code> elements.</p><p><code>innerText</code> returns concatenated text which is like we highlight an area of a web page with the cursor and then copy it, only shows <em>human-readable</em> elements, no <code>&lt;script&gt;</code>, <code>&lt;style&gt;</code> and hidden (e.g. <code>display:none</code>) elements, these elements are not rendered.</p><p>The returned values by <code>textContent</code> and <code>innerText</code> are same if the element is not rendered, such as:</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// =&gt; true</span></span><br><span class="line"><span class="built_in">document</span>.scripts[<span class="number">0</span>].innerText===<span class="built_in">document</span>.scripts[<span class="number">0</span>].textContent</span><br></pre></td></tr></table></figure><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Node/textContent</p><h3 id="Text-In-script">Text In <code>&lt;script&gt;</code></h3><p>Could embed arbitrary text into <code>&lt;script&gt;</code>, just set an invalid MIME type, make the script non-executable.</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> noexesrc = <span class="built_in">document</span>.createElement(<span class="string">'script'</span>);</span><br><span class="line">noexesrc.setAttribute(<span class="string">'id'</span>, <span class="string">'whoknows'</span>);</span><br><span class="line">noexesrc.setAttribute(<span class="string">'type'</span>, <span class="string">'text/x-whoknows'</span>);  <span class="comment">// invalid MIME type</span></span><br><span class="line">noexesrc.text=<span class="string">'alert("Yeah")'</span>;</span><br><span class="line"><span class="built_in">document</span>.body.appendChild(noexesrc);   <span class="comment">// should be no popup dialog</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// =&gt; "alert(\"Yeah\")"</span></span><br><span class="line"><span class="built_in">document</span>.querySelector(<span class="string">'#whoknows'</span>).text;  <span class="comment">// text property is same as textContent</span></span><br></pre></td></tr></table></figure><h3 id="DOM-Tree-Manipulation">DOM Tree Manipulation</h3><h4 id="Creating-Nodes">Creating Nodes</h4><p>methods in the <code>Document</code> object:</p><ul><li>createElement()</li><li>createTextNode()</li><li>createDocumentFragment()</li><li>createAttribute()</li><li>createComment()</li><li>importNode()</li><li>adoptNode()</li></ul><p><code>createDocumentFragment()</code> returns a <code>DocumentFragment</code> object that is a mini <code>Document</code> object and has no parent. A common use is to assemble a DOM subtree within the <code>DocumentFragment</code> object, then put the subtree (not the <code>DocumentFragment</code> object itself) into current active DOM tree, this will empty the object, it likes a temporary nodes container.</p><p>methods in the <code>Node</code> object:</p><ul><li>cloneNode(), cloned node has an same id attribute, should modify it manually if it exists</li></ul><h4 id="Inserting-Nodes">Inserting Nodes</h4><p>methods in the <code>Node</code> object:</p><ul><li>appendChild()</li><li>insertBefore()</li><li>replaceChild()</li></ul><p>Invoke these methods on the parent node and pass the child node to them.</p><h4 id="Deleting-Nodes">Deleting Nodes</h4><p>methods in the <code>Node</code> object:</p><ul><li>removeChild()</li></ul><p>Invoke the method on the parent node and pass the child node to it.</p><h3 id="Element-Coordinates">Element Coordinates</h3><p>Document Coordinates: a point (x, y) of an element is relative to the top-left corner of the document.</p><p>Viewport Coordinates: a point (x, y) of an element is relative to the top-left corner of the viewport, the viewport is the portion of the browser window that actually displays document content, excluding browser menus, toolbars.</p><p>width, height of the document</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">document</span>.documentElement.offsetWidth</span><br><span class="line"><span class="built_in">document</span>.documentElement.offsetHeight</span><br></pre></td></tr></table></figure><p>width, height of the viewport</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">window</span>.innerWidth</span><br><span class="line"><span class="built_in">window</span>.innerHeight</span><br></pre></td></tr></table></figure><p>properties in the <code>Window</code> object:</p><ul><li>innerWidth</li><li>innerHeight</li></ul><p>The two properties' value include scrollbars and borders.</p><ul><li>scrollX (alias: pageXOffset, for compatibility)</li><li>scrollY (alias: pageYOffset, for compatibility)</li></ul><p>Return the number of pixels that the document is currently scrolled horizontally or vertically.</p><p>methods in the <code>Window</code> object:</p><ul><li>scroll()</li><li>scrollBy()</li><li>scrollTo()</li></ul><p>properties in the <code>Element</code> object:</p><ul><li>clientWidth</li><li>clientHeight</li></ul><p>Return inner width, height of an element, including padding but excluding borders, margins, and scrollbars (if present). scrollbars' position are between padding and borders.<br>When they are used on the root element, they are width, height of the viewport, excluding scrollbars.<br>They return <code>0</code> for elements with no CSS or inline layout boxes.</p><ul><li>scrollWidth</li><li>scrollHeight</li></ul><p>Minimum required width, height of an element to show without scroolbars, measured like <code>clientWidth</code>, <code>clientHeight</code>, but including width, height of pseudo-elements.</p><p>Don't need a horizontal scrollbar if <code>clientWidth == scrollWidth</code>, the vertical scrollbar is also</p><ul><li>scrollLeft</li><li>scrollTop</li></ul><p>Get or set the number of pixels that an element's content is scrolled horizontally or vertically.</p><p>methods in the <code>Element</code> object:</p><ul><li><p>scroll()</p></li><li><p>scrollBy()</p></li><li><p>scrollIntoView(), also could mark an anchor, then change location.hash to it</p></li><li><p>scrollTo()</p></li><li><p>getBoundingClientRect()<br>Returns the size of an element and its position relative to the viewport. The position includes padding and borders, not margins. The returned value is the union of the rectangles returned by <code>getClientRects()</code>.</p></li></ul><p>Convert the viewport coordinates to the document coordinates:</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> box = e.getBoundingClientRect();</span><br><span class="line"><span class="keyword">const</span> x = box.left + <span class="built_in">window</span>.scrollX;</span><br><span class="line"><span class="keyword">const</span> y = box.top + <span class="built_in">window</span>.scrollY;</span><br></pre></td></tr></table></figure><ul><li>getClientRects()</li></ul><p>Like <code>getBoundingClientRect()</code>, but returns a collection. Inline elements may consist of multiple rectangles, use it to query these individual rectangles.</p><ul><li>elementFromPoint()</li><li>elementsFromPoint()</li></ul><p>Return the element or elements at the specified coordinates (relative to the viewport).</p><p>properties in the <code>HTMLElement</code> object:</p><ul><li>offsetWidth</li><li>offsetHeight</li></ul><p>Return width, height of an element, include padding, borders, scrollbars, not margins. They are opposite to <code>clientWidth</code> and <code>clientHeight</code>.</p><p><code>scrollWidth &gt; offsetWidth</code> if an content overflow is happening.</p><h3 id="Selected-Text">Selected Text</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// https://developer.mozilla.org/en-US/search?q=querySelector</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// the two methods are identical, but they don't work on &lt;textarea&gt; and &lt;input&gt;</span></span><br><span class="line"><span class="comment">// =&gt; "1,468 documents found for \"querySelector\""</span></span><br><span class="line"><span class="built_in">window</span>.getSelection().toString();</span><br><span class="line"><span class="comment">// document.getSelection().toString();  </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> searchInput = <span class="built_in">document</span>.querySelector(<span class="string">'#main-q'</span>);  <span class="comment">// #main-q is the search box</span></span><br><span class="line"><span class="comment">// =&gt; "Selector"</span></span><br><span class="line">searchInput.value.substring(searchInput.selectionStart, searchInput.selectionEnd);</span><br></pre></td></tr></table></figure><h3 id="Editable-Element">Editable Element</h3><p>Any HTML element can be editable.</p><p><code>document.designMode</code> makes the entire document editable, there is not a corresponding HTML attribute to it.</p><p>Could transform a web page into a simple rich-text editor.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/Guide/HTML/Editable_content</p><h2 id="JavaScript-and-CSS">JavaScript and CSS</h2><h3 id="How-to-Include-CSS">How to Include CSS</h3><ul><li>inline stylesheet</li></ul><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">h1</span> <span class="attr">style</span>=<span class="string">"color:red"</span>&gt;</span>typeof<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li><p>using JavaScript</p></li><li><p>embedded stylesheet</p></li></ul><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line">        h1 &#123;</span><br><span class="line">            color: red</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li>linked external stylesheet</li></ul><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">href</span>=<span class="string">"style.css"</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li>imported stylesheet</li></ul><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@import</span> <span class="string">'custom.css'</span>;</span><br></pre></td></tr></table></figure><p>The styles which reside closest to the HTML tag will take the precedence, the above sequence is also the precedence order, JavaScript generated styles are same as the inline stylesheet.</p><h3 id="Comments-2">Comments</h3><p>Comments in CSS begin with <code>/*</code> and end with <code>*/</code></p><h3 id="Margin-and-Padding">Margin and Padding</h3><ul><li><code>margin</code> specifies space outside the border, provides visual space between an element and its neighbors in the normal document flow;</li><li><code>padding</code> specifies space inside the border, between the border and the element content;</li><li>no border, <code>padding</code> is not necessary. <code>margins</code> are irrelevant if the element is dynamically positioned;</li></ul><h3 id="How-to-Scripting-CSS">How to Scripting CSS</h3><h4 id="Scripting-Inline-Stylesheet-With-The-style-Property">Scripting Inline Stylesheet With The <code>style</code> Property</h4><p>The property is used to get as well as set the inline style of an element. The returned object is read-only, but can set value to its properties, all values must be string. Getting or setting the inline style as a single string to use <code>elt.setAttribute()</code> or <code>elt.style.cssText</code>.</p><p>Will get nothing if the element hasn't inline styles, only has value if setting it by JavaScript code before or the element has inline styles. in general, only useful for setting styles.</p><p>Setting a property of the style object to <code>null</code> or <code>&quot;&quot;</code> will reset it, prefer <code>&quot;&quot;</code>.</p><p>CSS properties in JavaScript:</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* like font-size */</span></span><br><span class="line">elt.style.fontSize</span><br><span class="line"><span class="comment">/* like float, it is a reserved word */</span></span><br><span class="line">elt.style.cssFloat</span><br></pre></td></tr></table></figure><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/style<br>https://developer.mozilla.org/en-US/docs/Web/API/CSS_Object_Model/Using_dynamic_styling_information</p><h5 id="Window-getComputedStyle">Window.getComputedStyle()</h5><p>Use it to query the styles that actually apply to an element.</p><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Window/getComputedStyle</p><h4 id="Scripting-CSS-Classes">Scripting CSS Classes</h4><p>Two properties of the <code>Element</code> object:</p><ul><li><code>className</code>, it is the HTML class attribute, class is a reserved word in JavaScript</li><li><code>classList</code>, is a live collection, has <code>add()</code>, <code>remove()</code>, <code>toggle()</code>, <code>replace()</code> methods</li></ul><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/API/Element/classList</p><h2 id="Web-Components">Web Components</h2><p>Web Components is a suite of different technologies to create reusable custom elements with encapsulated functionality.</p><p>It consists of three main technologies:</p><ul><li>Custom elements</li><li>Shadow DOM</li><li>HTML templates</li></ul><p>More details here:<br>https://developer.mozilla.org/en-US/docs/Web/Web_components</p>]]></content>
      <tags>
        <tag>Know</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Make a right choice among Java Collections</title>
    <url>/2018/12/17/make-a-right-choice-among-java-collections/</url>
    <content><![CDATA[<p><img src="/images/java-collections.png" alt="Java Collections"></p><a id="more"></a><h2 id="Overview">Overview</h2><p>This figure doesn't show all inheritance information, such as, LinkedList also extends Deque interface;</p><p>The primary purpose is to choose a right data structure when do Java programming.</p><h2 id="ENV">ENV</h2><p>Java 8</p><h2 id="HashMap">HashMap</h2><blockquote><p>Base on bucket + list/red-black tree, if the list size of a bucket is greater than 8, the list will be converted to a red-black tree;</p><p>Don't set a too high capacity if iteration performance is important, figure out a sufficiently large capacity will allow key-value pairs to be stored more efficiently than resizing the hash table;</p><p>Unordered, not thread-safe, fail-fast iterator(no guarantees);</p><p>Permits null keys and values;</p></blockquote><h2 id="LinkedHashMap">LinkedHashMap</h2><blockquote><p>Like HashMap, but maintains a doubly-linked list to make the map entries are in insertion order;</p><p>A special constructor is provided to support LRU(Least recently used), calling the get and put methods will make the key appear in the tail of the doubly-linked list;</p></blockquote><h2 id="ConcurrentHashMap">ConcurrentHashMap</h2><blockquote><p>Same functional specification as Hashtable;</p><p>Base on bucket + list/red-black tree, like HashMap, <a href="http://en.wikipedia.org/wiki/Compare-and-swap" target="_blank" rel="noopener">CAS algo</a>, no read lock, don't lock the entire table;</p><p>Iterators are designed to be used by only one thread at a time, the results of aggregate status methods including size, isEmpty, and containsValue are typically useful only when a map is not undergoing concurrent updates in other threads;</p><p>Resizing may be a relatively slow operation, estimate a appropriate hash table size;</p><p>Unordered, no null keys or values, thread-safe, fail-safe iterator;</p></blockquote><h2 id="ConcurrentSkipListMap">ConcurrentSkipListMap</h2><blockquote><p>Base on <a href="http://en.wikipedia.org/wiki/Skip_list" target="_blank" rel="noopener">SkipLists</a>, space-time tradeoff, appropriate for massive data access;</p><p><strong>It is sorted</strong>, natural order or defined by a Comparator, ascending key ordered views and their iterators are faster than descending ones;</p><p>The size method is inaccurate, the bulk operations putAll, equals, toArray, containsValue, and clear are not guaranteed to be performed atomically;</p><p>O(log(n)), thread-safe, no null keys or values;</p></blockquote><h2 id="TreeMap">TreeMap</h2><blockquote><p>Base on red-black tree, O(log(n)), ordered, not thread-safe, fail-fast iterator;</p><p>Natural order or defined by a Comparator;</p></blockquote><h2 id="WeakHashMap">WeakHashMap</h2><blockquote><p>Hash table based implementation of the Map interface;</p><p>An entry in a WeakHashMap will automatically be removed when its key is no longer in ordinary use. The value objects in a WeakHashMap are held by ordinary strong references;</p><p>Permits null keys and values;</p><p>Not thread-safe, fail-fast iterator;</p></blockquote><h2 id="EnumMap">EnumMap</h2><blockquote><p>An array accessed with enum’s ordinal value as an index. There is no need to calculate hash codes or resolve collisions;</p><p>Enum maps are maintained in the natural order of their keys (the order in which the enum constants are declared);</p><p>Iterators returned by the collection views are weakly consistent: they will never throw ConcurrentModificationException and they may or may not show the effects of any modifications to the map that occur while the iteration is in progress;</p><p>No null keys, permits null values;</p><p>Not thread-safe, O(1);</p></blockquote><h2 id="LinkedList">LinkedList</h2><blockquote><p>A doubly-linked list, insertion and deletion are fast;</p><p>Not thread-safe, fail-fast iterator, permits null elements;</p><p>Also implements Deque interface, slower than ArrayDeque and need more memory for small objects. It can use an index to get an element, remove the current element during iteration and supports null elements, but ArrayDeque can't;</p></blockquote><h2 id="ArrayList">ArrayList</h2><blockquote><p>Base on an array, has List interface</p><p>Access its elements in constant time by their index, inserting or removing an element may be slow if the array is huge and the inserted or removed element is close to the beginning of the list;</p><p>In most cases, however, ArrayList outperforms LinkedList. Even elements shifting in ArrayList, while being an O(n) operation, is implemented as a very fast System.arraycopy() call. It can even appear faster than the LinkedList‘s O(1) insertion which requires instantiating a Node object and updating multiple references under the hood. LinkedList also can have a large memory overhead due to a creation of multiple small Node objects;</p><p>Not thread-safe, fail-fast iterator;</p></blockquote><h2 id="CopyOnWriteArrayList">CopyOnWriteArrayList</h2><blockquote><p>A variant of ArrayList, thread-safe;</p><p>Iterator provides a snapshot of the state of the list when the iterator was constructed. No synchronization is needed while traversing the iterator, this array never changes during the lifetime of the iterator. Element-changing operations on iterators themselves (remove, set, and add) are not supported;</p><p>Perfer reading. Element-changing operations are costly. Don't block read, but read old value when writing. Be best suited for small size, read-only operations;</p><p>Permits null keys and values;</p></blockquote><h2 id="Stack">Stack</h2><blockquote><p>Use Deque's implementations instead of it, such as ArrayDeque, ConcurrentLinkedDeque;</p></blockquote><h2 id="Vector">Vector</h2><blockquote><p>Use ArrayList or CopyOnWriteArrayList instead of it;</p></blockquote><h2 id="TreeSet">TreeSet</h2><blockquote><p>Based on TreeMap;</p><p>O(log(n)) time for the basic operations (add, remove and contains);</p><p>Natural order or defined by a Comparator, not thread-safe, fail-fast iterator;</p></blockquote><h2 id="HashSet">HashSet</h2><blockquote><p>Based on HashMap;</p><p>Iterating over this set requires time proportional to the sum of the HashSet instance's size (the number of elements) plus the &quot;capacity&quot; of the backing HashMap instance (the number of buckets). Thus, it's very important not to set the initial capacity too high (or the load factor too low) if iteration performance is important;</p><p>Permits null elements, unordered, not thread-safe, fail-fast iterator;</p></blockquote><h2 id="LinkedHashSet">LinkedHashSet</h2><blockquote><p>HashSet + doubly-linked list;</p><p>It provides constant-time performance for the basic operations (add, contains and remove), assuming the hash function disperses elements properly among the buckets. Performance is likely to be just slightly below that of HashSet, due to the added expense of maintaining the linked list, with one exception: Iteration over a LinkedHashSet requires time proportional to the size of the set, regardless of its capacity. Iteration over a HashSet is likely to be more expensive, requiring time proportional to its capacity;</p><p>Has two parameters that affect its performance: initial capacity and load factor. They are defined precisely as for HashSet. Note, however, that the penalty for choosing an excessively high value for initial capacity is less severe for this class than for HashSet, as iteration times for this class are unaffected by capacity;</p><p>Permits null elements, insertion-order, not thread-safe, fail-fast iterator;</p></blockquote><h2 id="CopyOnWriteArraySet">CopyOnWriteArraySet</h2><blockquote><p>Set + CopyOnWriteArrayList;</p></blockquote><h2 id="ConcurrentSkipListSet">ConcurrentSkipListSet</h2><blockquote><p>Based on ConcurrentSkipListMap, O(log(n));</p></blockquote><h2 id="EnumSet">EnumSet</h2><blockquote><p>Are represented internally as bit vectors;</p><p>The iterator returned by the iterator method traverses the elements in their natural order (the order in which the enum constants are declared);</p><p>The returned iterator is weakly consistent: it will never throw ConcurrentModificationException and it may or may not show the effects of any modifications to the set that occur while the iteration is in progress;</p><p>No null elements, not thread-safe, O(1);</p></blockquote><h2 id="BlockingQueue">BlockingQueue</h2><blockquote><p>Insert/Remove/Examine operations come in four forms: wait, throw an exception, return a special value(null or false), timeout;</p><p>Support the Collection interface;</p><p>Primarily for producer-consumer queues;</p><p>No null elements;</p><p>Thread-safe;</p><p>The bulk Collection operations addAll, containsAll, retainAll and removeAll are not necessarily performed atomically unless specified otherwise in an implementation. May fail after adding only some of the elements;</p><p>remove()/clear()/contains() functions are O(n);</p></blockquote><h2 id="ArrayBlockingQueue">ArrayBlockingQueue</h2><blockquote><p>A bounded BlockingQueue backed by an array, FIFO, thread-safe(ReentrantLock), producer-consumer model, optional fairness policy;</p><p>Enqueue and dequeue operations are O(1);</p></blockquote><h2 id="PriorityBlockingQueue">PriorityBlockingQueue</h2><blockquote><p>A unbounded BlockingQueue, array + priority heap, natural order or defined by a Comparator, thread-safe(ReentrantLock);</p><p>It is unbounded, doesn't block put()/offer(e, time, unit), might cause OutOfMemoryError;</p><p>A priority queue relying on natural ordering also does not permit insertion of non-comparable objects (doing so results in ClassCastException);</p><p>iterator() is not guaranteed to traverse the elements in any particular order;</p><p>Enqueue and dequeue operations are O(log(n));</p></blockquote><h2 id="LinkedBlockingQueue">LinkedBlockingQueue</h2><blockquote><p>An optionally-bounded blocking queue based on linked nodes, FIFO, thread-safe(ReentrantLock);</p><p>Linked queues typically have higher throughput than array-based queues but less predictable performance in most concurrent applications;</p><p>Enqueue and dequeue operations are O(1);</p></blockquote><h2 id="DelayQueue">DelayQueue</h2><blockquote><p>An unbounded blocking queue;</p><p>An element can only be taken when its delay has expired;</p></blockquote><h2 id="SynchronousQueue">SynchronousQueue</h2><blockquote><p>A blocking queue in which each insert operation must wait for a corresponding remove operation by another thread, and vice versa;</p><p>Not have any internal capacity, acts as an empty collection, nothing to iterate;</p><p>Optional fairness policy to guarantee FIFO order;</p></blockquote><h2 id="LinkedTransferQueue">LinkedTransferQueue</h2><blockquote><p>An unbounded TransferQueue(a BlockingQueue) based on linked nodes, producers may wait for consumers to receive elements;</p><p>FIFO, thread-safe, <a href="http://en.wikipedia.org/wiki/Compare-and-swap" target="_blank" rel="noopener">CAS algo</a>;</p><p>size() is inaccurate;</p><p>Has a special function, transfer(), which transfers the specified element immediately if there exists a consumer already waiting to receive it (in take() or timed poll), else inserts the specified element at the tail of this queue and waits until the element is received by a consumer. In other worlds, if already have elements in the queue, transfer() will wait all of them to be received first;</p><p>LinkedBlockingQueue(unbounded) + SynchronousQueue(fairness) + ConcurrentLinkedQueue;</p></blockquote><h2 id="PriorityQueue">PriorityQueue</h2><blockquote><p>An unbounded priority queue based on a priority heap, but has an internal capacity governing the size of an array used to store the elements on the queue;</p><p>No null elements, not thread-safe;</p><p>Natural order or defined by a Comparator. The head of this queue is the least element with respect to the specified ordering. Natural ordering also does not permit insertion of non-comparable objects (doing so results in ClassCastException). if use a Comparator, the least element is depend on this Comparator, it may be the minimum or maximum if these elements are numbers;</p><p>iterator() is not guaranteed to traverse the elements in any particular order;</p><p>Enqueuing and dequeuing methods are O(log(n)), remove() and contains() are O(n), peek(), element() and size() are O(1);</p></blockquote><h2 id="ConcurrentLinkedQueue">ConcurrentLinkedQueue</h2><blockquote><p>An unbounded thread-safe queue based on linked nodes;</p><p>FIFO, no null elements, thread-safe, lock-free, <a href="http://en.wikipedia.org/wiki/Compare-and-swap" target="_blank" rel="noopener">CAS algo</a>. The thread-safe is relative:</p></blockquote><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>( !queue.isEmpty() ) &#123;  </span><br><span class="line">    queue.poll(obj);  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>No! need a lock</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">synchronized</span>( queue ) &#123;</span><br><span class="line">    <span class="keyword">if</span>( !queue.isEmpty() ) &#123;</span><br><span class="line">        queue.poll(obj);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>Iterators are weakly consistent;</p><p>The size method is inaccurate, the bulk operations addAll, removeAll, retainAll, containsAll, equals, and toArray are not guaranteed to be performed atomically;</p></blockquote><h2 id="LinkedBlockingDeque">LinkedBlockingDeque</h2><blockquote><p>An optionally-bounded blocking deque based on linked nodes, BlockingDeque extends BlockingQueue interface;</p><p>FIFO, LIFO, thread-safe(ReentrantLock), no null elements;</p><p>remove(), removeFirstOccurrence(), removeLastOccurrence(), contains(), iterator.remove(), and the bulk operations: O(n), others: O(1);</p></blockquote><h2 id="ArrayDeque">ArrayDeque</h2><blockquote><p>Resizable-array implementation of the Deque interface, has a circular array, the array length is powers of two;</p><p>FIFO, LIFO, no capacity restrictions, not thread-safe, fail-fast iterator, no null elements;</p><p>Faster than LinkedList when used as a queue. But allocate enough capacity before use it, otherwise need to enlarge the array and copy data;</p><p>As a stack instead of Stack;</p></blockquote><h2 id="ConcurrentLinkedDeque">ConcurrentLinkedDeque</h2><blockquote><p>An unbounded concurrent deque based on linked nodes;</p><p>FIFO, LIFO, no null elements, thread-safe, lock-free;</p><p>The size method is inaccurate, the bulk operations addAll, removeAll, retainAll, containsAll, equals, and toArray are not guaranteed to be performed atomically;</p><p>As a stack instead of Stack;</p></blockquote>]]></content>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>HTTP 1.1/2 Optimization</title>
    <url>/2018/05/18/http-optimization/</url>
    <content><![CDATA[<h2 id="Major-Features-of-HTTP-1-1-and-HTTP-2">Major Features of HTTP/1.1 and HTTP/2</h2><p><img src="/images/http.png" alt="HTTP/1.1 and HTTP/2"></p><a id="more"></a><h2 id="General-Optimizations">General Optimizations</h2><p>These common rules can be applied to HTTP/1.1 and HTTP/2.</p><ul><li><p>Reduce DNS lookups and latency</p></li><li><p>Make fewer HTTP requests</p><blockquote><p>Concatenate CSS, JS files and use image sprites properly;</p></blockquote></li><li><p>Reuse TCP connections</p></li><li><p>Parallelize requests and responses</p></li><li><p>Add cache-related headers</p></li><li><p>Reduce transferred data</p><blockquote><p>Minify HTML, JS, CSS files, compress other text-based assets;</p></blockquote><blockquote><p>Compress images, such as removing unnecessary metadata, reducing the numbers of colors, using SVG or Icon Font;</p></blockquote><blockquote><p>Use proper image dimensions. different scenarios, different images;</p></blockquote><blockquote><p>Use right image format, such as WebP, BPG;</p></blockquote><blockquote><p>Lazy loading some modules or images/videos/audio, users may not scroll to these parts, also could consider media queries;</p></blockquote></li><li><p>Inline resource properly</p><blockquote><p>such as JS/CSS code, images</p></blockquote></li><li><p>Use CDN or self-built cache system</p><blockquote><p>Caching every cacheable resource;</p></blockquote></li><li><p>Avoid HTTP redirects</p></li><li><p>Eliminate cookies partially or entirely</p></li></ul><p>See also:</p><blockquote><p><a href="https://developers.google.com/web/fundamentals/performance/why-performance-matters/" target="_blank" rel="noopener">Web performance optimization on the Google Developer website</a></p></blockquote><h2 id="HTTP-1-1-Optimization">HTTP/1.1 Optimization</h2><ul><li><p>HTTP Pipelining</p><blockquote><p>The default of a HTTP/1.1 connection is persistent, but it implies a strict FIFO queue, have <em>head-of-line blocking</em> problems;</p></blockquote><blockquote><p>Pipelining is complex to implement correctly, buggy proxies are still common;</p></blockquote><blockquote><p>Use it very carefully.</p></blockquote></li><li><p>Multiple TCP Connections</p><blockquote><p>Most modern browsers support opening up to six connections per origin(host name + port) usually;</p></blockquote></li><li><p>Domain Sharding</p></li><li><p>Concatenation and Spriting</p><blockquote><p>JS/CSS file concatenation and image sprites are so popular in HTTP/1.1 scenario;</p></blockquote><blockquote><p>Pros: fewer HTTP requests; fewer transferred HTTP header data; eliminating extra network latency;</p></blockquote><blockquote><p>Cons: extra application complexity (preprocessing, deployment, code); expensive cache invalidations; larger JS/CSS files need more parsing, rendering time; Bigger images need more resident memeory on the browser side; more complex update policy;</p></blockquote><blockquote><p>Don't combine unnecessary resources for the current page;</p></blockquote><blockquote><p>Seperate resources into several bundles according to different purposes, don't bundling independent resources;</p></blockquote><blockquote><p>Seperate and deliver first-paint critical JS, CSS bundles;</p></blockquote><blockquote><p>Apply them carefully, measure the results, figure out the proper file size, no ideal size;</p></blockquote></li><li><p>Resource inlining</p><blockquote><p>This technique is also popular, mostly embed JS, CSS code and images into a page;</p></blockquote><blockquote><p>Don't inline resources that being used by multiple pages;</p></blockquote><blockquote><p>Inlining resources that are small, and limited to specific pages. In practice, under 1-2KB are small, resources below this often incur higher HTTP overhead than itself;</p></blockquote><blockquote><p>Don't inline frequently updated resources;</p></blockquote><blockquote><p>Don't inline frequently accessed resources, then can use CDN and cache;</p></blockquote><blockquote><p>Base64 encoding has byte expansion as compared with the original resource, increased decoding time;</p></blockquote></li></ul><p>See also:</p><blockquote><p><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Connection_management_in_HTTP_1.x" target="_blank" rel="noopener">Connection management in HTTP/1.x</a></p></blockquote><blockquote><p><a href="https://http2.akamai.com/demo" target="_blank" rel="noopener">Akamai HTTP 1.1/2 Demo</a></p></blockquote><h2 id="HTTP-2-Optimization">HTTP/2 Optimization</h2><ul><li><p>TCP, TLS Optimizations</p><p>Minimum requirements for the HTTP/2 server:</p><blockquote><p>Server should start with a TCP cwnd(Congestion window size) of 10 segments;</p></blockquote><blockquote><p>Server should support TLS with ALPN negotiation;</p></blockquote><blockquote><p>Server should support TLS resumption to minimize handshake latency;</p></blockquote></li><li><p>Remove HTTP/1.1 workarounds</p><blockquote><p>No need Pipelining, Multiple TCP connections, Domain Sharding, put resources under a same origin;</p></blockquote><blockquote><p>Concatenation and Spriting are still useful, small files are bad for disk IO, can cache them? take care of your cache system, make it work like you think! don't remove them aggressively;</p></blockquote><blockquote><p>Resource inlining can be replaced by server push, to improve cache performance, especially in mobile network environment, it have high-cost roundtrips. Sometimes static assets are in CDN, how to push? it is awkward! Only support GET, HEAD methods; No URL fuzzy matching for the Node.js http2stream.pushStream() function at this moment; Don't support window.fetch(), use XMLHttpRequest; May push already cached resources; don't remove it aggressively also;</p></blockquote></li></ul><h2 id="Browser-Optimization">Browser Optimization</h2><ul><li><p>DNS prefetch</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"dns-prefetch"</span>  <span class="attr">href</span>=<span class="string">"//example.com"</span>&gt;</span></span><br></pre></td></tr></table></figure><blockquote><p>It is a hint to the browser, but Chrome always perform it.</p></blockquote></li><li><p>TCP preconnect</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"preconnect"</span> <span class="attr">href</span>=<span class="string">"//example.com"</span>&gt;</span></span><br></pre></td></tr></table></figure><blockquote><p>A hint to the browser that it open a connection to the linked web site in advance, also perform DNS lookups.</p></blockquote></li><li><p>prefetch</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"prefetch"</span> <span class="attr">href</span>=<span class="string">"//other-site.com/images/big.jpeg"</span>&gt;</span></span><br></pre></td></tr></table></figure><blockquote><p>Suggest the browser to fetch the linked resources that will be used in the next navigation/page load;</p></blockquote><blockquote><p>Will generally cause the cookies of the prefetched site to be accessed;</p></blockquote><blockquote><p>No same-origin restriction;</p></blockquote><blockquote><p>Has a lower priority than preload, preload is for the current navigation/page load;</p></blockquote></li><li><p>preload</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"preload"</span> <span class="attr">href</span>=<span class="string">"sample.mp4"</span> <span class="attr">as</span>=<span class="string">"video"</span> <span class="attr">type</span>=<span class="string">"video/mp4"</span>&gt;</span></span><br></pre></td></tr></table></figure><blockquote><p>Tells the browser to download a resource because this resource will be needed later during the current navigation;</p></blockquote><blockquote><p>It is not a suggestion to the browser, is a MUST;</p></blockquote></li><li><p>prerender</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"prerender"</span> <span class="attr">href</span>=<span class="string">"/next-page.html"</span>&gt;</span></span><br></pre></td></tr></table></figure><blockquote><p>Suggests that the browser fetch the linked resource in advance, and that it also render the prefetched content offscreen so it can be quickly presented to the user once needed.</p></blockquote></li></ul>]]></content>
      <tags>
        <tag>HTTP</tag>
        <tag>HTTP/2</tag>
        <tag>Optimization</tag>
      </tags>
  </entry>
  <entry>
    <title>Web开发者的简单颜色指南</title>
    <url>/2016/04/18/a-simple-web-developer-guide-to-color/</url>
    <content><![CDATA[<p>我从来不是一个颜色理论的爱好者，我想是因为我对它一直不太抱希望。我乐意坐在那儿，拿着色环，选取互补色(complementary colors)、分散的互补色(split-complementary colors)、三色(triad colors)等配色方案，让朋友、家人、客户印象深刻。</p><p>说实话，颜色理论对我很遥远，尝试在项目中使用颜色时我从未发现它有用。有点讽刺，我发现，颜色的选择、运用得越好，背后的理论我越有更好的认识。</p><p>刚起步时理论并没有真正得帮助，对吧？这就是为什么，在这篇文章里，你不会看到一个单一的色环，相反，我会展示一个简单的你能用在下一个Web项目的颜色工作流。</p><a id="more"></a><p>当然，过程中你是在潜意识地学习理论。为了好玩，我建议几个月后回来再重温一下。</p><p>随后你会有更深刻的理解，我保证。</p><p>译者注：</p><p>颜色理论基础相关：<a href="http://www.ruanyifeng.com/blog/2008/07/color_theory.html" target="_blank" rel="noopener">关于颜色理论</a>，<a href="http://mmmono.com/item/255724/" target="_blank" rel="noopener">三分钟帮你快速入门极简色彩学</a></p><h2 id="选择基本色">选择基本色</h2><p>我们总是能看到象<a href="http://www.rit-mcsl.org/fairchild/WhyIsColor/files/ExamplePage.pdf" target="_blank" rel="noopener">一千万种颜色</a>这样可笑的事情。想想，一千万种。</p><p>众多颜色中，只需选一种用于我们的品牌，它也是网站的基本色。</p><p>其他颜色均基于基本色，所以这很重要。</p><h3 id="如何选择起始色">如何选择起始色</h3><p>随意选取一种颜色很简单，但我们不这样做。对于任何与客户打交道的项目，你应该尽可能地证明你的选择，不这样，你喜爱的颜色可能与他们喜爱的相抵触。他们是客户，他们付钱，他们会赢。</p><p>不要过虑。确保你有选择的理由，会让你好看点。</p><p><strong>选择起始色的小贴士</strong></p><ul><li><strong>用你有的颜色</strong><br>如果客户有已确立颜色的LOGO，这个颜色通常是你的起始色。</li><li><strong>排除竞争对手的颜色</strong><br>如果你的一个主要竞争对手有一个有影响力的品牌颜色，不要拷贝即使你能改进。</li><li><strong>考虑你的目标受众</strong><br>殡仪馆网站的颜色与儿童俱乐部的是非常不同的。想想谁会使用网站，想让他们有什么样的感受（兴奋，严肃，关爱等）。</li><li><strong>不要落入思维定势</strong><br>如果你为年轻女孩设计网站，不是非要使用粉红色。避免使用老一套来取得认可。</li><li><strong>玩个文字游戏</strong><br>如果你在纠结，写下任何与客户业务相关的词语，这个词语列表应该可以给你一些有关颜色的主意。如果你还是实在纠结，登上讲解颜色的网站，看下哪个最合适。</li></ul><p>现在你心中应该有了一个基本色。它应该是一种简单的颜色，象红色，绿色，蓝色，黄色或粉红色。接下来我们说说色度。</p><p>假设你选择了蓝色（好选择！）</p><h2 id="选择（好的）基本色">选择（好的）基本色</h2><p>我们象艺术家一样去剽窃，使用其他人的选择，而不是为了寻找一种好色度的蓝色，被Photoshop的颜色拾取器所困扰。</p><p>首先，到<a href="https://dribbble.com/colors/" target="_blank" rel="noopener">Dribbble</a>和<a href="http://designspiration.net/" target="_blank" rel="noopener">Designspiration</a>，点击它们的&quot;Colors&quot;链接。</p><p>会显示相似的屏幕:<br><img src="/images/01-dribble-designspiration-screenshot-opt.jpg" alt="image"><br><em>使用灵感网站为你的设计寻找一种合适的颜色（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/01-dribble-designspiration-screenshot-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>你可以把这作为寻找一个合适色度的蓝色的下一步。</p><p>对于一个清新、充满活力的品牌，选择一个更浅、更明亮的蓝色（前5个中的一个），对于较商业、严肃一点的，底部的5个是更好的选择。<br><img src="/images/02-color-characteristics-opt.jpg" alt="image"><br><em>不同色度的蓝色有着不同的特质，选择要明智！（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/02-color-characteristics-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>选择一种色度，查看使用这种颜色的网站的实际设计，然后你可以使用<a href="https://css-tricks.com/grabbing-hex-codes-for-colors/" target="_blank" rel="noopener">CSS-Tricks的颜色拾取技术</a>从浏览器中提取精确的颜色值。<br><img src="/images/03-dribbble-color-results-opt.jpg" alt="image"><br><em>这么多的蓝色！你的工作就是挑一个最适合你品牌的。简单得很！（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/03-dribbble-color-results-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>你不仅会看到不同版本的基本色，也很容易看到与之匹配的颜色。</p><h2 id="创建色彩衔接的调色板">创建色彩衔接的调色板</h2><p>好了，你应该有了一个颜色的16进制值。我的是<code>#30c9e8</code>。现在我们使用这个颜色建一个调色板。会比你想像的容易。</p><p>当想到创建配色方案时，你可能有这样的想象：<br><img src="/images/04-colour-lovers-palettes-opt.jpg" alt="image"><br><em>你可能见过这种调色板（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/04-colour-lovers-palettes-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>这种调色板的主要问题是应用到实际设计时不太实际。很多调色板有比你想要的多得多的颜色，尤其是考虑到平均一个配色方案只需要3个中性色：</p><ul><li>白色</li><li>深灰色</li><li>浅灰色（可选）</li></ul><p>如果你试着添加5或6个中性色，就凌乱了。</p><p>真正需要的两种颜色：</p><ul><li>一个基本色（我们的例子中是<code>#30c9e8</code>）</li><li>一个强调色（马上就讲到这个）</li></ul><p>比起大肆使用互补色系，分散互补色系，三色系或其他色系，仅用这5种颜色创建网站，会得到更好的效果。<br><img src="/images/05-simple-color-schemes-opt.jpg" alt="image"><br><em>象<a href="https://thoughtbot.com/" target="_blank" rel="noopener">Thoughtbot</a>和<a href="http://tedtoddinsurance.com/" target="_blank" rel="noopener">TedTodd</a>，一个好看的网站不需要复杂的配色方案。（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/05-simple-color-schemes-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><h3 id="寻找强调色">寻找强调色</h3><p>强调色在网站上会少量使用，通常用在行为召唤（如：购买、下载按钮，译者注）页面元素上，所以需要引人注目。</p><p>下一步，把基本色的16进制值输入<a href="http://paletton.com/" target="_blank" rel="noopener">Paletton</a> 的颜色值方框中：<br><img src="/images/06-base-rgb-opt.jpg" alt="image"><br><em>在Paletton上输入你的基本色（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/06-base-rgb-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>这里，可用两种方式找到强调色。</p><p>首先，点击“Add Complementary”按钮，橙色就是你的强调色！<br><img src="/images/07-complimentary-opt.jpg" alt="image"><br><em>Paletton自动生成一个好的强调色（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/07-complimentary-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>或者，如果你不喜欢生成的颜色，可以点击位于顶部的几个按钮来发现更好的。<br><img src="/images/08-triad-opt.jpg" alt="image"><br><em>点击以发现喜欢的色系（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/08-triad-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>就本人而言，我十分喜欢三色系按钮（Triad按钮）生成的红色，所以我会用在我们的配色方案中。Paletton在背后所做的事情当然有科学理论依据，但是，现在先放一放。稍后你会学到其理论，所有的事情都会变得清晰。</p><p>已经有了漂亮的基本色，醒目的强调色，让我们加入白色，因为白色一直都好搭配。下面是我们现在的配色方案：<br><img src="/images/09-color-scheme-opt.jpg" alt="image"><br><em>目前的调色板，正在成形（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/09-color-scheme-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>现在缺失的是一些灰色。</p><h3 id="添加灰色">添加灰色</h3><p>我的大多数项目中，我发现两种色度的灰色永远有用，一种是深灰，一种是浅灰。你会经常用到它们。</p><p>深灰一般用于文本，当需要与白色区域有些细微差别时用到浅灰（一般是背景）。</p><p>可以通过两种方式选择灰色：</p><ul><li>再次用到<a href="https://dribbble.com/colors/" target="_blank" rel="noopener">Dribbble</a>和<a href="http://designspiration.net/" target="_blank" rel="noopener">Designspiration</a>，从之前匹配基本色的搜索结果中找一个不错的灰色。但在搜索条中输入<code>blue website</code>，会有更多有关灰色的结果；</li><li>如果有Photoshop或类似的软件，可以用<a href="http://methodandcraft.com/videos/creating-harmonious-color-schemes" target="_blank" rel="noopener">Erica Schoonmaker的技术</a> 和基本色来调和灰色。</li></ul><h3 id="创建和谐的灰色">创建和谐的灰色</h3><p>为通过Erica的方法取得我们发亮的新和谐灰，先随便选两个默认的灰色，然后采取如下步骤：</p><ol><li>创建两个色度的灰色，<code>#424242</code>和<code>#fafafa</code></li><li>在两个色度上插入一个颜色填充层</li><li>把填充层的颜色改成基本色（<code>#30c9e8</code>）</li><li>设置图层的混合模式为叠加，把不透明度降到5%到40%间（本例是40%）</li><li>使用颜色拾取器并拷贝新的颜色</li></ol><iframe src="https://player.vimeo.com/video/159766011" width="500" height="279" frameborder="0" title="nice-greys" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe><p>我需要指出当叠加的颜色是蓝色时这个方法工作得格外好，对于其他颜色，要么把不透明度降到5%至10%，要么坚持原来的灰色。<br><img src="/images/10-greys-opt.jpg" alt="image"><br><em>挑选调和了基本色的灰色是个造就大不同的小事（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/10-greys-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><h2 id="瞧！我们做到了！">瞧！我们做到了！</h2><p>我们的配色方案完成了，相信你会感到自豪。<br><img src="/images/11-color-palette-opt.jpg" alt="image"><br><em>漂亮的配色方案，能用了</em>（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/11-color-palette-opt.jpg" target="_blank" rel="noopener">大图</a>）</p><h2 id="应用配色方案">应用配色方案</h2><p>现在我们已经得到了配色方案，到用的时候了。这完全是另一个话题。但为让你有所了解，这里有一个灰度设计稿和一个着色后的版本。</p><p>小贴士：如果你纠结于颜色，一个不错的技巧是先用灰度模式创建网站的布局，弄清层次结构，再试用颜色。<br><img src="/images/12-colour-in-practice-greyscale-opt.jpg" alt="image"><br><em>以灰度模式布局网站有利于应用配色方案</em>（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/12-colour-in-practice-greyscale-opt.jpg" target="_blank" rel="noopener">大图</a>）<br><img src="/images/13-color-in-practice-opt.jpg" alt="image"><br><em>着色后的版本</em>（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/13-color-in-practice-opt.jpg" target="_blank" rel="noopener">大图</a>）</p><p>如你所见，蓝色是这里的特征色，大面积地使用，也用在图标中。</p><p>我们的强调色，红色，相对于基本色它引人注目，小面积地使用，用在按钮上和图标中。这个红色用的越少，越显得夺目。</p><p>深灰用在文本，LOGO和图标的轮廓。图标中别不放这种颜色，小细节造就大不同。</p><p>背景使用白色和浅灰色。浅灰色根本不需要，但我发现这是另外一个能让网站赏心悦目的小细节。</p><p>当使用颜色和文本时，确保背景和文本有足够的对比是很重要的，这有助于色盲和低视力者阅读你网站上的文字。有许多工具可用于查寻遵守<a href="http://webaim.org/blog/wcag-2-0-and-link-colors/" target="_blank" rel="noopener">WCAG准则</a>的颜色。两个流行的工具是<a href="http://webaim.org/resources/contrastchecker/" target="_blank" rel="noopener">WebAim Color Contrast Checker</a>和<a href="http://lea.verou.me/" target="_blank" rel="noopener">Lea Verou</a>写的<a href="http://leaverou.github.io/contrast-ratio/" target="_blank" rel="noopener">Contrast Ratio</a> 。</p><h2 id="结语">结语</h2><p>如你所见，除了今天创建的调色板中的颜色，我们真得不需要太多，但也不意味着局限于此！<br><img src="/images/14-color-palette-extended-opt.jpg" alt="image"><br><em>使用上述技术，当你需要时，你能扩展你的调色板来掌控更多的颜色（<a href="https://media-mediatemple.netdna-ssl.com/wp-content/uploads/2016/03/14-color-palette-extended-opt.jpg" target="_blank" rel="noopener">大图</a>）</em></p><p>设计时，你或许判定引入更多颜色进调色板是适当的，这绝对可以！只要细心，能用上述步骤找出更多能与你的配色方案一起工作的颜色。</p><p>美妙的地方在于这事你做的越多，选择颜色时你干得越好，你会知道哪些能一起工作，哪些不能。有时上述方法产出的结果不尽于人意，所以随时调整。学习颜色理论，却不见理论，玩儿得开心！</p><p>原文：<a href="https://www.smashingmagazine.com/2016/04/web-developer-guide-color/" target="_blank" rel="noopener">A Simple Web Developer’s Guide To Color</a></p>]]></content>
      <tags>
        <tag>translation</tag>
        <tag>翻译</tag>
        <tag>web design</tag>
        <tag>网页设计</tag>
      </tags>
  </entry>
  <entry>
    <title>如何使用CSS垂直对齐元素</title>
    <url>/2016/03/06/how-to-vertically-align-elements-using-css/</url>
    <content><![CDATA[<p>任何一个Web开发人员都会遇到垂直对齐这个老问题。我在Web领域工作了将近一年，已经被客户问了多次“把这个对齐到窗口中央”或“把那个放到那个的中部”。使用CSS容易实现水平对齐：</p><p>对于块级元素（block-level element）</p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">display</span>: <span class="selector-tag">block</span>; <span class="selector-tag">margin</span>: 0 <span class="selector-tag">auto</span>;</span><br></pre></td></tr></table></figure><p>对于内联元素（inline element，也叫行内元素）</p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">display</span>: <span class="selector-tag">inline-block</span>; <span class="selector-tag">text-align</span>: <span class="selector-tag">center</span>;</span><br></pre></td></tr></table></figure><p>为什么垂直对齐不能这么简单？问题在于网站传统上并不是为了垂直对齐，网页是横向缩放的，高度改变以适应内容。我们幸运的是CSS有多种方法可以绕过这个问题，有些方法好于其他，有些则是十分糟糕，不应该使用。记住这些都是相对的（有时是绝对的），一个可以在某种情况下解决问题的方法可能不能解决另外一种情况的问题。</p><a id="more"></a><h2 id="首先，vertical-align属性">首先，vertical-align属性</h2><p>开发人员都有这个时候：首先发现了<code>vertical-align</code>属性，几分钟后则疑惑它到底有什么含义，特别是在使用这个属性后该死的垂直对齐仍不奏效！！！的确，我们都曾经错误地使用它！</p><p><code>vertical-align</code>属性规定了排成一行的兄弟元素间彼此是如何对齐的。它适用于内联元素（inline element）和table cell元素。</p><p>适用于内联元素时的值：</p><ul><li>baseline</li></ul><blockquote><p>将子元素文本的基线（base line）与父元素的基线对齐。想象基线就是带有横线的写作纸上的横线。这是默认值；</p></blockquote><ul><li>bottom</li></ul><blockquote><p>将子元素的底部与<em><strong>行</strong></em> 的底部对齐；</p></blockquote><ul><li>middle</li></ul><blockquote><p>将子元素的中心线与父元素的中心线对齐（译者注：元素的中心线比基线高一点）；</p></blockquote><ul><li>sub</li></ul><blockquote><p>将子元素的基线与父元素的下标基线对齐；</p></blockquote><ul><li>super</li></ul><blockquote><p>将子元素的基线与父元素的上标基线对齐；</p></blockquote><ul><li>text-bottom</li></ul><blockquote><p>将子元素的底部与父元素字体的最底部对齐（译者注：即与父元素的文本内容区的最底部对齐）；</p></blockquote><ul><li>text-top</li></ul><blockquote><p>将子元素的顶部与父元素字体的最顶部对齐（译者注：即与父元素的文本内容区的最顶部对齐）；</p></blockquote><ul><li>top</li></ul><blockquote><p>将子元素的顶部与<em><strong>行</strong></em> 的顶部对齐；</p></blockquote><ul><li>length</li></ul><blockquote><p>将子元素文本的基线值设为父元素的基线值加上<em><strong>length</strong></em> 所给定的值；</p></blockquote><p class="codepen" data-height="268" data-theme-id="8525" data-slug-hash="neDlk" data-default-tab="result" data-user="edenagency">See the Pen <a href="http://codepen.io/edenagency/pen/neDlk/" target="_blank" rel="noopener">The vertical-align property</a> by Eden Agency (<a href="http://codepen.io/edenagency" target="_blank" rel="noopener">@edenagency</a>) on <a href="http://codepen.io" target="_blank" rel="noopener">CodePen</a>.</p><script async src="//codepen.io/assets/embed/ei.js"></script><p>当<code>vertical-align</code>被误用时，<code>vertical-align: middle</code>通常是罪魁祸首，最常见的原因是用在了块级元素上。记住，它仅在<code>inline</code>和<code>inline-block</code>元素上有效。如果用在了块级元素上，所有子元素都会继承它的值。</p><p><code>vertical-align</code>也可用于HTML表格的单元格。适用于单元格时的值：</p><ul><li>top</li></ul><blockquote><p>将单元格的内容与单元格的顶部对齐；</p></blockquote><ul><li>middle</li></ul><blockquote><p>将单元格的内容与单元格的中心对齐；</p></blockquote><ul><li>bottom</li></ul><blockquote><p>将单元格的内容与单元格的底部对齐；</p></blockquote><p>现在清晰了，让我们来看看怎么解决在块级元素上的问题。</p><p>译者注：<code>vertical-align</code>属性的含义和图解也可参考<a href="http://bitsofco.de/the-vertical-align-property/" target="_blank" rel="noopener">这里</a></p><h2 id="Line-height">Line height</h2><p>这是项简单的技术，你只想中心对齐一行文本时效果很好。也可用于图片，不过这是一种hack，尽量避免使用。</p><p class="codepen" data-height="347" data-theme-id="8525" data-slug-hash="GiLdm" data-default-tab="result" data-user="edenagency">See the Pen <a href="http://codepen.io/edenagency/pen/GiLdm/" target="_blank" rel="noopener">Vertical alignment using line height</a> by Eden Agency (<a href="http://codepen.io/edenagency" target="_blank" rel="noopener">@edenagency</a>) on <a href="http://codepen.io" target="_blank" rel="noopener">CodePen</a>.</p><p>这个例子包含了两个元素，一个是父元素，一个是子元素。<code>line height</code>设置在子元素上，反过来强制父元素调整到与之相同的高度。注意：为让这项技术凑效，<code>line height</code>的值必须大于字体大小或图片的高度。为了用在图片上，必须把<code>vertical-align: middle</code>添加到图片元素。</p><p>如我之前所说，这是一种hack，稍后提及能产生同样效果的方法。</p><p><strong>优点：</strong></p><p>容易实现</p><p><strong>缺点：</strong></p><p>只能用于一行文本，当用于文本之外时，它不够正规、不够优雅。</p><h2 id="CSS表格">CSS表格</h2><p>是CSS的表格不是HTML的表格。我们可以使用<code>display: table</code>让一个元素表现得像个表格。这些表格属性基于原来的HTML表格元素，且行为一致。</p><p class="codepen" data-height="268" data-theme-id="8525" data-slug-hash="iwBua" data-default-tab="result" data-user="edenagency">See the Pen <a href="http://codepen.io/edenagency/pen/iwBua/" target="_blank" rel="noopener">Vertical alignment using css table properties</a> by Eden Agency (<a href="http://codepen.io/edenagency" target="_blank" rel="noopener">@edenagency</a>) on <a href="http://codepen.io" target="_blank" rel="noopener">CodePen</a>.</p><p>在这个示例中，父元素当作表格，子元素作为表格的单元格。<code>vertical-align: middle</code>使单元格完美居中，所有的单元格继承了其父元素的高度。</p><p><strong>优点：</strong></p><p>完全像HTML表格一样运作，并且语义上正确；<br>不需要定义高度和宽度。</p><p><strong>缺点：</strong></p><p>除非定义了宽度，表格会展开到其内容的宽度；</p><p>如果你在父元素中需要另一个块级元素并且不想把它当成table cell，事情就变得混乱；</p><p>也可以使用HTML表格来实现，然而应该避免使用HTML元素去设计页面的样式，那是CSS的事情，语义上也是错误的，除非你的内容适用于表格，比如列车时刻表。</p><h2 id="绝对定位">绝对定位</h2><p>为了使用<code>position: absolute</code>来垂直对齐你选择的元素，需要使用像素或百分比定义一个高度。</p><p class="codepen" data-height="344" data-theme-id="8525" data-slug-hash="GuqjD" data-default-tab="result" data-user="edenagency">See the Pen <a href="http://codepen.io/edenagency/pen/GuqjD/" target="_blank" rel="noopener">Vertical alignment using absolute positioning</a> by Eden Agency (<a href="http://codepen.io/edenagency" target="_blank" rel="noopener">@edenagency</a>) on <a href="http://codepen.io" target="_blank" rel="noopener">CodePen</a>.</p><p>有两种方法可居中子元素。第一种选择，如上图所示，用<code>top: 50%</code>下移子元素，用<code>transform: translateY(-50%)</code>上移子元素高度的一半，以致子元素的中心与父元素的位于同一行。也可以使用负值的外边距（margin）实现；第二种选择是把子元素的<code>top</code>, <code>bottom</code>, <code>left</code>, <code>right</code>属性的值均设为<code>0</code>，外边距均设成<code>auto</code>，然而用这种方法我得到过不同的结果，最好还是选择第一种。</p><p><strong>优点：</strong></p><p>尺寸使用百分比能使元素自适应。</p><p><strong>缺点：</strong></p><p>需要定义一个高度，有时这是不可能的；</p><p>如果不止一个子元素，可能有重叠问题，除非它们也是绝对定位。</p><p>你必须记得相对地或绝对地定位你的父元素，否则子元素不会居中。</p><h2 id="幽灵元素">幽灵元素</h2><p>这项技术最初在CSS Tricks的博客文章<a href="http://css-tricks.com/centering-in-the-unknown/" target="_blank" rel="noopener">&quot;Centering in the Unknown&quot;</a>中提及。一个幽灵元素用于在父元素中居中子元素。因为幽灵元素被赋于了一个<code>100%</code>的高度，没有必要为父元素或子元素定义高度。当无法知道尺寸时，这是一个理想的方法。</p><p class="codepen" data-height="295" data-theme-id="8525" data-slug-hash="trKAu" data-default-tab="result" data-user="edenagency">See the Pen <a href="http://codepen.io/edenagency/pen/trKAu/" target="_blank" rel="noopener">Vertical alignment using the 'Ghost Element'</a> by Eden Agency (<a href="http://codepen.io/edenagency" target="_blank" rel="noopener">@edenagency</a>) on <a href="http://codepen.io" target="_blank" rel="noopener">CodePen</a>.</p><p>如同Chris Coyier在他的博客中提及的，Michal Czernow建议使用一个伪元素作为幽灵元素，不用另加一个<code>div</code>且语义上是正确的。</p><p><strong>优点：</strong></p><p>语义正确。</p><p><strong>缺点：</strong></p><p>完全像CSS表格一样运作，两项技术都不比另一个更好。</p><h2 id="Flexbox">Flexbox</h2><p>在CSS家族中Flexbox（CSS3 弹性盒模型）是项相对较新的技术。我不想深入太多细节，有很多细节，所以这里是一个对它是做什么的快速描述。</p><p>根据<a href="http://www.w3.org/TR/2014/WD-css-flexbox-1-20140325/" target="_blank" rel="noopener">www.w3.org</a>:</p><p>“在flex布局模型中，flex容器的子元素可以在任一方向展开，并且它们的大小具有相当的弹性，即能增长以填充未使用的空间，也可以缩小以防止溢出父元素。子元素在水平和垂直两个方向的对齐均容易操控。盒子的嵌套（水平的在垂直的中，或者垂直的在水平的中）可以在两个维度上构建布局。”</p><p>总的来说，Flexbox仅适用于小型的可伸缩布局。它很容易均等地（或不均匀地）把子元素分成多行或多列，改变元素流动的方向等等。在一个容器中垂直对齐一个单一元素十分地简单。</p><p class="codepen" data-height="268" data-theme-id="8525" data-slug-hash="DKCwz" data-default-tab="result" data-user="edenagency">See the Pen <a href="http://codepen.io/edenagency/pen/DKCwz/" target="_blank" rel="noopener">Vertical alignment using flexbox</a> by Eden Agency (<a href="http://codepen.io/edenagency" target="_blank" rel="noopener">@edenagency</a>) on <a href="http://codepen.io" target="_blank" rel="noopener">CodePen</a>.&nbsp;</p><p>当一个父元素从块级元素转变成一个flex元素，它的子元素也自动转变成flex子元素。设置子元素的<code>margin</code>属性为<code>auto</code>将把子元素推向其父元素的中心，添加更多的子元素不会影响它的垂直居中，虽然它会相对于它的兄弟元素重新排列。例如，如果有太多的子元素需要固定在同一行上，并且父元素有<code>flex-wrap: wrap</code>属性，多余的子元素会掉到下一行，所有的子元素均在它们的父元素中居中就像它们是一个整体。</p><p><strong>优点：</strong></p><p>Flexbox就是用来解决布局问题，因而对于垂直对齐它是理想的。</p><p><strong>缺点：</strong></p><p>通过使用前缀，它虽然和所有最新的浏览器兼容，但不支持旧版本的浏览器，如：IE9。<br>如果你想了解关于Flexbox的更多信息，看下CSS Tricks的<a href="http://css-tricks.com/snippets/css/a-guide-to-flexbox/" target="_blank" rel="noopener">&quot;A Complete Guide to Flexbox&quot;</a>。</p><h2 id="总结-。。。">总结 。。。</h2><p>有这么多的选择，真是奇怪为什么我们发现垂直对齐还是这么难！不幸的是没有一个最佳解决方案。Flexbox看上去更有前景，唯一的缺点是它与旧版浏览器的兼容性，然而我想一旦规范敲定它会是我们要转向的技术。</p><p>原文：<a href="http://createdineden.com/blog/2014/september/24/a-professional-guide-how-to-vertically-align-elements-using-css/" target="_blank" rel="noopener">A Professional Guide: How to Vertically Align Elements Using CSS</a></p>]]></content>
      <tags>
        <tag>CSS</tag>
        <tag>vertically align</tag>
        <tag>垂直对齐</tag>
        <tag>translation</tag>
        <tag>翻译</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo + Markdown issues</title>
    <url>/2015/08/01/hexo-markdown-issues/</url>
    <content><![CDATA[<h2 id="Backslash-escapes">Backslash escapes</h2><p>Hexo installs a plugin <em>hexo-renderer-marked</em> as the default Markdown renderer, but the plugin generates literal characters which are not what you want if have a backslash before these characters, such as <code>scrapy crawl \&lt;spider name\&gt;</code> becomes <code>scrapy crawl \</code>. To fix this issue by replacing <em>hexo-renderer-marked</em> with <em>hexo-renderer-markdown-it</em>.</p><a id="more"></a><h2 id="Read-More-link">&quot;Read More&quot; link</h2><p>Normally, we insert <code>&lt;!-- more --&gt;</code> into a post to display its summary, but don't work if using <em>hexo-renderer-markdown-it</em> with default configuration, <em>hexo-renderer-markdown-it</em> escapes HTML. To fix this issue by appending</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">markdown:</span></span><br><span class="line">  <span class="attr">render:</span></span><br><span class="line">    <span class="attr">html:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><p>to <em>Hexo</em>'s <code>_config.yml</code>.</p>]]></content>
      <tags>
        <tag>Hexo</tag>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>Using multiple spiders in a Scrapy project</title>
    <url>/2015/07/27/using-multiple-spiders-in-a-scrapy-project/</url>
    <content><![CDATA[<h2 id="Introduction">Introduction</h2><p>Different channel's structure in a websit are similar, sometimes we want to reuse source code and don't create a <a href="http://scrapy.org/" target="_blank" rel="noopener">Scrapy</a> project per channel. This is a tutorial how to use multiple spiders in a Scrapy project.</p><a id="more"></a><h2 id="ENV">ENV</h2><p>Python: 2.7.5<br>Scrapy: 0.24.2</p><h2 id="Tree-like-directories-of-the-tutorial-project">Tree-like directories of the tutorial project</h2><p>Source code in GitHub: <a href="https://github.com/lnxpgn/scrapy_multiple_spiders" target="_blank" rel="noopener">scrapy_multiple_spiders</a></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">scrapy_multiple_spiders</span><br><span class="line">├── commands</span><br><span class="line">│   ├── __init__.py</span><br><span class="line">│   └── crawl.py</span><br><span class="line">└── tutorial</span><br><span class="line">    ├── scrapy.cfg</span><br><span class="line">    └── tutorial</span><br><span class="line">        ├── __init__.py</span><br><span class="line">        ├── common_spider.py</span><br><span class="line">        ├── items.py</span><br><span class="line">        ├── pipelines.py</span><br><span class="line">        ├── settings.py</span><br><span class="line">        ├── spider_settings</span><br><span class="line">        │   ├── __init__.py</span><br><span class="line">        │   ├── spider1.py</span><br><span class="line">        │   └── spider2.py</span><br><span class="line">        └── spiders</span><br><span class="line">            ├── __init__.py</span><br><span class="line">            ├── spider1.py</span><br><span class="line">            └── spider2.py</span><br></pre></td></tr></table></figure><h2 id="Custom-project-command">Custom project command</h2><p>In Scrapy we can add our custom project commands by using the <code>COMMANDS_MODULE</code> setting item in <code>settings.py</code>, we will custom the standard <code>crawl</code> command. When call <code>scrapy crawl &lt;spider name&gt;</code>, the <code>run</code> function in <code>scrapy.commands.crawl.Command</code> is the entrance. Inherit <code>scrapy.commands.crawl.Command</code> and overwrite the <code>run</code> function in our project's <code>commands.crawl.CustomCrawlCommand</code> class.</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CustomCrawlCommand</span><span class="params">(Command)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self, args, opts)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> len(args) &lt; <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">raise</span> UsageError()</span><br><span class="line">        <span class="keyword">elif</span> len(args) &gt; <span class="number">1</span>:</span><br><span class="line">            <span class="keyword">raise</span> UsageError(<span class="string">"running 'scrapy crawl' with more than one spider is no longer supported"</span>)</span><br><span class="line">        spname = args[<span class="number">0</span>]</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># added new code</span></span><br><span class="line">        spider_settings_path = self.settings.getdict(<span class="string">'SPIDER_SETTINGS'</span>, &#123;&#125;).get(spname, <span class="literal">None</span>)</span><br><span class="line">        <span class="keyword">if</span> spider_settings_path <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            self.settings.setmodule(spider_settings_path, priority=<span class="string">'cmdline'</span>)</span><br><span class="line">        <span class="comment"># end</span></span><br><span class="line">                 </span><br><span class="line">        crawler = self.crawler_process.create_crawler()</span><br><span class="line">        spider = crawler.spiders.create(spname, **opts.spargs)</span><br><span class="line">        crawler.crawl(spider)</span><br><span class="line">        self.crawler_process.start()</span><br></pre></td></tr></table></figure><p>The commented part is new code, others are same as the <code>run</code> function in <code>scrapy.commands.crawl.Command</code> class. The Scrapy <code>settings</code> has four priorities: <code>default</code>, <code>command</code>, <code>project</code>, <code>cmdline</code>, the <code>cmdline</code> has a top priority, use it to overwrite default setting items which are in <code>settings.py</code>. <code>SPIDER_SETTINGS</code> is a setting item in <code>settings.py</code>, it is a dictionary, the key is the spider name, the value is the spider's custom setting file name.</p><h2 id="Create-common-spiders-and-settings">Create common spiders and settings</h2><p><code>tutorial.tutorial.common_spider.CommonSpider</code> is a spider which includes a normal parsing process for a website and some common functions. <code>settings.py</code> includes common setting items for all spiders, such as <code>LOG_LEVEL</code>, you can overwrite them in a spider custom setting file, such as <code>spider1.py</code> and <code>spider2.py</code> in <code>tutorial.tutorial.spider_settings</code> directory.</p><p><code>common_spider.py</code></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CommonSpider</span><span class="params">(Spider)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">        This is a common spider, including common functions which child spiders can inherit or overwrite</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    name = <span class="string">''</span></span><br><span class="line">    allowed_domains = []</span><br><span class="line">    start_urls = []</span><br><span class="line"></span><br><span class="line">    <span class="comment"># must add "kwargs", otherwise can't run in scrapyd</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, settings, **kwargs)</span>:</span></span><br><span class="line">        super(CommonSpider, self).__init__(**kwargs)</span><br><span class="line"></span><br><span class="line">        self._start_urls = []</span><br><span class="line">        self._start_urls.extend(settings.get(<span class="string">'START_URLS'</span>, []))</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> self._start_urls:</span><br><span class="line">            <span class="keyword">raise</span> Exception(<span class="string">'no urls to crawl'</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">    @classmethod</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">from_settings</span><span class="params">(cls, settings, **kwargs)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> cls(settings, **kwargs)</span><br><span class="line"></span><br><span class="line"><span class="meta">    @classmethod</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">from_crawler</span><span class="params">(cls, crawler, **kwargs)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> cls.from_settings(crawler.settings, **kwargs)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start_requests</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">for</span> url <span class="keyword">in</span> self._start_urls:</span><br><span class="line">            <span class="comment"># must append these hosts, otherwise OffsiteMiddleware will filter them</span></span><br><span class="line">            parsed_url = urlparse.urlparse(url)</span><br><span class="line">            parsed_url.hostname <span class="keyword">and</span> self.allowed_domains.append(parsed_url.hostname)</span><br><span class="line"></span><br><span class="line">            <span class="comment"># open('file name', 'a+') is different between OS X and Linux, </span></span><br><span class="line">            <span class="comment"># read an empty filter list from &lt;JOBDIR&gt;/requests.seen when launche the spider on OS X, </span></span><br><span class="line">            <span class="comment"># be careful "dont_filter"</span></span><br><span class="line">            <span class="keyword">yield</span> Request(url, callback=self.parse, method=<span class="string">'GET'</span>, dont_filter=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        self.log(<span class="string">'response url: %s, status: %d'</span> % (response.url, response.status), INFO)</span><br></pre></td></tr></table></figure><p><code>settings.py</code></p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">COMMANDS_MODULE = <span class="string">'commands'</span></span><br><span class="line"></span><br><span class="line">SPIDER_SETTINGS = &#123;</span><br><span class="line">    <span class="string">'spider1'</span>: <span class="string">'tutorial.spider_settings.spider1'</span>,</span><br><span class="line">    <span class="string">'spider2'</span>: <span class="string">'tutorial.spider_settings.spider2'</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">LOG_LEVEL = <span class="string">'INFO'</span></span><br></pre></td></tr></table></figure><h2 id="Create-multiple-spiders-in-a-project">Create multiple spiders in a project</h2><h3 id="Spiders-without-custom-parsing-process">Spiders without custom parsing process</h3><p>Like <code>tutorial.tutorial.spiders.spider1.Spider1</code>.</p><p>Spider1's setting file: <code>spider1.py</code> (in <code>spider_settings</code> directory)</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">LOG_FILE = <span class="string">'spider1.log'</span></span><br><span class="line"></span><br><span class="line">JOBDIR=<span class="string">'spider1_job'</span></span><br><span class="line"></span><br><span class="line">START_URLS = [<span class="string">'http://www.bing.com/news'</span>]</span><br></pre></td></tr></table></figure><p>Spider1's source file: <code>Spider1.py</code> (in <code>spiders</code> directory)</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> ..common_spider <span class="keyword">import</span> CommonSpider</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Spider1</span><span class="params">(CommonSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'spider1'</span></span><br></pre></td></tr></table></figure><h3 id="Spiders-with-custom-parsing-process">Spiders with custom parsing process</h3><p>Like <code>tutorial.tutorial.spiders.spider2.Spider2</code>.</p><p>Spider2's setting file: <code>spider2.py</code> (in <code>spider_settings</code> directory)</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">LOG_FILE = <span class="string">'spider2.log'</span></span><br><span class="line"></span><br><span class="line">JOBDIR=<span class="string">'spider2_job'</span></span><br><span class="line"></span><br><span class="line">START_URLS = [<span class="string">'http://www.bing.com/knows'</span>]</span><br><span class="line"></span><br><span class="line">TITLE_PATH = <span class="string">'html head title::text'</span></span><br></pre></td></tr></table></figure><p>Spider2's source file: <code>Spider2.py</code> (in <code>spiders</code> directory)</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> scrapy.log <span class="keyword">import</span> INFO</span><br><span class="line"><span class="keyword">from</span> ..common_spider <span class="keyword">import</span> CommonSpider</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Spider2</span><span class="params">(CommonSpider)</span>:</span></span><br><span class="line">    name = <span class="string">'spider2'</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># must add "kwargs", otherwise can't run in scrapyd</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, settings, **kwargs)</span>:</span></span><br><span class="line">        super(Spider2, self).__init__(settings, **kwargs)</span><br><span class="line"></span><br><span class="line">        self._title_path = settings.get(<span class="string">'TITLE_PATH'</span>, <span class="string">''</span>)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse_other_info</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        title = response.css(self._title_path).extract()[<span class="number">0</span>]</span><br><span class="line">        self.log(<span class="string">'title: %s'</span> % title, INFO)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        self.parse_other_info(response)</span><br><span class="line"></span><br><span class="line">        super(Spider2, self).parse(response)</span><br></pre></td></tr></table></figure><h2 id="Run-spiders">Run spiders</h2><ol><li>Set <code>PYTHONPATH</code> to <code>/&lt;path&gt;/scrapy_multiple_spiders</code>;</li><li>In <code>/&lt;path&gt;/scrapy_multiple_spiders/tutorial</code>, call <code>scrapy crawl spider1</code> or <code>scrapy crawl spider2</code>, check the log file <code>spider1.log</code> or <code>spider2.log</code>.</li></ol>]]></content>
      <tags>
        <tag>Scrapy</tag>
        <tag>Python</tag>
        <tag>Crawler</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
</search>
